# WujiaTea — Compact summary

**Mục đích:** context inject vào mọi session. Mỗi §section search-able qua `/recall`. History chi tiết → `chapters/*.tex` + git log.

**Cập nhật:** 2026-09-03 (**Audit hợp nhất D3a+D3b+D3c**: phát hiện D3c quên merge+push nên chưa từng lên UAT dù tưởng đã deploy — đã merge/push/deploy trong phiên; audit code + 183 test + đo lại UAT 74/74, 0 regression thật; `UI-CARDHEADER-001` vẫn `Ready for Dev` 47/103, D3d/e/f để phiên sau; §5) · 2026-09-02 (cụm D3c xong, 11 call site, phủ 47/103 — xem audit 03/09 để biết trạng thái deploy thật) · 2026-08-28 (cụm D3b xong, 26 call site, phủ 36/103) · 2026-08-27 (cụm D3a xong, dựng `wj_card_header` + 10 call site mẫu, phủ 10/103 — **D1 + D2 ĐÃ DEPLOY UAT + đo lại chỉ-đọc trên UAT**: D2 14/14 · D1 27/28 — 1 Fail là phát hiện thật, nhãn "tất cả" bộ lọc bù hàng lệch PC↔mobile, sửa kèm D3; sheet đã đánh dấu `ĐÃ DEPLOY UAT` cho cả 5 issue ⇒ BA retest được ngay; **hàng đợi deploy TRỐNG**, §5) · 2026-08-26 (**cụm D2 xong** — `WJ-PORTAL-UI-002` → `Ready for Retest`, font Portal về Inter toàn bộ + fallback Thái/CJK) · 2026-08-25 (**cụm D1 xong** — 4 issue bù hàng `UAT-BH-001/003/005/006` → `Ready for Retest`; lứa mới **D1–D6 + R1–R5** → `docs/next-session-clusters-D.md`) · 2026-08-23 · phiên 2 (**batch a11y WJ-PORTAL-UI-003/004 → `Ready for Retest`** + review 4 chiều toàn code trừ phần Thái → `docs/refactor-plan.md` cụm R1–R5 + trả nợ chapter S55 — **hàng đợi deploy có việc**, §5) · 2026-08-23 (**C8b xong ⇒ ĐỦ 10/10 cụm C1–C10**, `UI-SECTIONHEADER-001` → `Ready for Retest`, 19/19 call site — **hàng đợi deploy có việc**, §5) · 2026-08-19 (**merge nhánh `thai`**: tính năng giám sát cửa hàng + toolchain dịch `.po` — 21 module, sửa hộ 8 điểm gồm ACL mở toang & seed không chạy trên UAT, `docs/merge-thai-review.md`; **hàng đợi deploy có việc** — §5) · 2026-08-19 (C1→C6 + C9 + C10 xong; C1→C5/C9/C10/**C6 đã deploy UAT**; **C7 xong 19/08, CHỜ DEPLOY**; còn **C8** là cụm cuối — §5) · 2026-08-14 — Phiên **phân cụm C1–C10** cho 32 issue `Ready for Dev` mới (STT 83–114, BA audit UAT 11–13/08): 0 code, chỉ đối chiếu source + chia cụm + viết prompt từng session (§13 + `docs/next-session-clusters-C.md`). Sprint code gần nhất: **S55** (UI Báo cáo đặt hàng, `05269b4`+`d7a7ead`, **ĐÃ deploy UAT, còn nợ chapter**). State hiện tại → §5.

---

## §1 wujia-overview

**Project:** Odoo 19 ERP + custom Vuexy portal cho chuỗi nhượng quyền trà sữa (~1500 portal user). Migrate v14 → v19.

**Dir:**
- `WujiaTea/odoo19/` Odoo 19 Community (read-only) · `custom/` 18 module (§2) · `themes/` 8 Vuexy · `data/` seed · `scripts/` seed+deploy · `docs/` (`wujia-tea-doc.tex` master + `chapters/` + `wujia-design-system.md` + `0[1-3]_*` chuẩn QA/Task/OAuth).
- v14 reference: `/home/huyban/odoo-dev/wujia_tea_odoo14` — template ref, **không sửa**.

**BA spec = Google Sheet "Internal ERP Master Plan_Update"** online (§7 tab/gid). Xlsm local = legacy fallback.

**Figma (BA design, READ-ONLY):** file key = **BẢN COPY** `aoeiDYlg6vlhJZg2w6Q7o5` ("Wujia (Copy)"); gốc `vfVcqN5zPJvlcjZU4NYim0` bị throttle, không dùng. BA CHƯA xong Figma → **ưu tiên code chuẩn > Figma > xlsm (lag)**. Cách kết nối MCP + xử lý Figma phẳng: `docs/figma-mcp-setup.md`. Token/component: `docs/wujia-design-system.md`.

**Local dev:**
- Scripts: `init-db.sh` fresh · `start.sh` hot-reload · `upgrade.sh <mod>` giữ data · `reseed_full.sh` 1-shot.
- DB `wujia_tea_19`, user `odoo19/1`, `127.0.0.1:5432`.
- Log `logs/odoo.log` · config `config/odoo.conf` · python `/home/huyban/miniconda3/envs/odoo/bin/python3`.

**UAT:** → §12 (`http://113.161.187.126:8019/`, `admin/Wujia@2026`).

**Deploy:** → §6 (thủ công, Windows tay, module mới cần `-i`).

---

## §2 wujia-modules (21 active)

| Module | Vai trò |
|---|---|
| `wujia_core` | `res.area`, `res.ward` master data |
| `wujia_franchise` | `wujia.franchise.management` + `wujia.franchise.member` (có icon) |
| `wujia_sale` | `sale.order` ext + product portal fields + `wujia.product.category` |
| `wujia_fleet` | Nhà xe / loại xe / xe / bảng giá (có icon) |
| `wujia_delivery` | `stock.picking/batch` ext + cước vận chuyển |
| `wujia_portal_layout` | Vuexy shell + CSS vars + Inter + responsive + utility class |
| `wujia_portal_base` | `/portal` dashboard + `bus.bus` realtime + franchise switch |
| `wujia_portal_sale` | `/portal/order` catalog + cart (`wujia.portal.cart(.line)`) |
| `wujia_portal_purchase_history` | `/portal/purchase-history` |
| `wujia_portal_delivery` | `/portal/delivery` |
| `wujia_portal_return` | `/portal/return` single-product + duyệt + `wujia.compensation.allocation` + wizard SO 0đ FIFO + hook picking (Sprint K) |
| `wujia_portal_notification` | `/portal/notification` + **backend quản trị thông báo** (vòng đời draft/published/archived, `code` ANN/năm/số, thống kê đọc, Sprint 41) |
| `wujia_portal_exam` | `/portal/exam` + backend Đăng ký thi (7 model, Sprint M) |
| `wujia_portal_knowledge` | `/portal/knowledge` + backend admin |
| `wujia_portal_report` | `/portal/reports/orders` |
| `wujia_portal_support` | `/portal/support` + backend + POST + attachment |
| `wujia_portal_info_request` | `/portal/info-request` + HQ duyệt |
| `wujia_portal_order_window` | Khung giờ đặt hàng per-area + global fallback |
| `wujia_portal_debt` | `/portal/debt` (+ `/payment-history`, `/pay`) — 7 màn mobile Figma `5013:*` (S43) + **PC 1920 Figma v1.1 `5077:*`** (S49, khối `.wj-debt-pc`). Dữ liệu THẬT từ S48 qua seam `models.AbstractModel` `wujia.portal.debt` (không bảng/không migration); guard Owner/Manager |

| `wujia_portal_inspection` | `/portal/inspection` — xem phiếu khảo sát/chấm điểm cửa hàng + gửi khắc phục (merge nhánh `thai` 19/08) |
| ~~`wujia_portal_remediation`~~ | ❌ **ĐÃ GỠ** — anh Thái tự xoá code (`f789a56`), phiên 20/08 gỡ khỏi DB bằng script, UAT `uninstalled`. **Không `-u`, không `-i`** module này. |

> Backend giám sát nằm TRONG `wujia_franchise` (8 model: `wujia.franchise.inspection[.line]` · `.inspection.grade/.question/.template[.line]/.category/.exam.line` · `wujia.supervision.schedule`) + 2 group `group_supervision_inspector/admin`.

> Dashboard workstream riêng: `wj_ks_dashboard_ninja` + `wj_ks_dn_advance` (state = `docs/dashboard-migration-plan.md`). Chi tiết: `wujia-tea-doc.tex` §1.3/§1.5.

---

## §3 wujia-adr-summary (16 ADR)

ADR-001 odoo19 source độc lập / 002 venv conda `odoo` py3.10 / 003 PG role `odoo19` / 004 custom portal Vuexy thay `/my` / 005 `wujia.franchise.member` / 006 realtime `bus.bus` native / 007 tách Order+History / 008 URL kebab + 301 redirect / 009 block feature→defer / 010 3 field địa chỉ / 011 branch picker TẠI LOGIN / 012 *overruled by 015* / 013 `res.area`/`res.ward` ở `wujia_core` / 014 member UI độc lập / 015 gộp `wujia_franchise_management`→`wujia_franchise` / 016 dùng `mail.message` via `message_post()`.

→ Chi tiết: `wujia-tea-doc.tex` chap 2 + 13.

---

## §4 wujia-sprint-history (compact — chi tiết ở `chapters/`)

| Sprint | Date | Outcome (1 dòng) |
|---|---|---|
| 56 | 08-15..23 | **Batch C1–C10 — đóng 32 issue `Ready for Dev` (STT 83–114)**, 10 cụm theo gốc rễ, mỗi phiên 1 cụm 1 lần `-u`: C1 `b623b70` · C2 `c656b95` · C3 `0398fa3` · C4 `a292547` · C5 `94756ff` · C9 `9a8784b` · C10 `f99321b` · C6 `c2592b3` · C7 `b33b46f` · C8a `59299f1` + C8b `02f77ca`. Ba gốc rễ đáng nhớ: (a) C6 = **1 rule `a:hover{underline}` của `web.assets_frontend` nạp SAU CSS custom** ⇒ tầng `_interaction.css` nạp cuối, thắng bằng specificity; **local không tái hiện được** (UAT có `website_sale`) nên phải chứng minh bằng cách nhúng CSS thẳng vào UAT. (b) C7 = 5 issue một gốc: Home dựng dồn nhiều sprint ⇒ gom về 1 hằng `HOME_PREVIEW_LIMIT` + 1 bộ component, cao 2752→2634 @360. (c) C8 = 183 heading phân loại **theo TỔ TIÊN DOM** chứ không theo tên class ⇒ 19 call site thật; **Odoo 19 QWeb không có directive đặt tên thẻ động** (`ir_qweb.py:1705`) nên phải rẽ 3 nhánh. Hai lần bảng đo ra đỏ đều là **lỗi bộ đo/dữ liệu, không phải code** (L7/L9) — trong đó truy ra gốc '4 ô đỏ có sẵn' mà C6/C7 từng ghi nhận: ticket khác cửa hàng ⇒ redirect đúng thiết kế. ĐÃ DEPLOY UAT toàn bộ → ch.68 |
| 55 | 08-12 | **UI Báo cáo đặt hàng PC + Mobile** (`Tasks!STT12`, `05269b4`+`d7a7ead`). Trang portal cuối còn Bootstrap thô nay tách 2 khối PC/Mobile, tái dùng `wj_page_header` + `wj-pc-*`, 2 slot swap nên lọc-không-reload chạy cả hai. 3 lỗi lộ ra khi đo, sửa kèm: `yaxis.title=undefined` làm ApexCharts chết; `t-out` với `name=False` **bỏ luôn thẻ `<td>`** ⇒ lệch hàng; ký hiệu tiền hardcode. Đo 31/31, ĐÃ DEPLOY UAT. **Còn nợ chapter `.tex`** |
| 54 | 08-11 | **Batch B4 — BackPageHeader `CMP-BPH-001` + Current Store Strip mobile** (`70f3e4f`). Seam `ir.http._wj_back_url()` gọi từ `t-att-href` thay vì sửa 31 call site/14 file. Vá lỗ hổng thật: `?return_url=/portal/../web` qua được bộ lọc tiền tố rồi bị trình duyệt chuẩn hoá thành `/web` ⇒ cấm segment `..` TRƯỚC. Strip mobile thành trigger mở popup PC (chỉ khi >1 cửa hàng). Sửa kèm lỗi có sẵn: inline `<script>` QWeb dùng `&&` bị escape ⇒ chết cả khối im lặng (L11). 91 test, đo 14/14 + 31/31 + hồi quy 286/286. ĐÃ DEPLOY UAT → ch.67 |
| 53 | 08-11 | **Batch B3a+B3b — PageHeader `CMP-PG-001` đủ 17/17 route** (`4da3a57`+`30bed39`). Component thêm slot `ph_meta` render **markup thô không bọc element** (3 route dùng nó làm slot swap của `wj_ajax_list`). 3 chỗ spec không nói phải tự tính: gap 8 mobile đụng token chung → bù `-6px` scope thay vì đổi token; BA khoá cả 'header 64' lẫn 'nút 40' (mâu thuẫn hình học) → hạ padding theo biến thể; meta PC lệch mép do `flex:1 1 auto`. Mọi header tự dựng (debt, `pc_account_shell`, exam PC) về component. Đo 34/34, 60 test. ĐÃ DEPLOY UAT → ch.66 |
| 52 | 08-11 | **Batch B2** (`cc7570f`). **WJ-PROD-001**: field `description_ecommerce` trùng tên field Html của `website_sale` ⇒ ở DB có cài app đó cột thành **jsonb** còn ORM ghi varchar → lưu Quy cách nổ; đổi tên `wujia_packaging` + pre-migrate 3 nhánh theo `information_schema` (chưa có cột / varchar RENAME / jsonb copy `->>'en_US'`). Xác nhận kiểu cột UAT bằng XML-RPC chỉ đọc, KHÔNG cần SQL. **WJ-NOTI-001**: portal in thẳng UTC lệch −7h ⇒ helper `fmt_local_dt` + qcontext `wj_dt` tại 4 render (cố ý KHÔNG override `_prepare_qcontext` toàn cục). 44 test, đo 41/41. ĐÃ DEPLOY UAT → ch.65 |
| 51 | 08-10 | **Rule Issue List ưu tiên 1 + batch B0/B1 Exam** (`1821097`). B0 WJ-ORD-023 = 0 code, chỉ ledger (đã fix từ 08-04 mà sheet chưa sync). B1 WJ-EXAM-001…006: regex SĐT client↔server lệch nhau (PC qua client rồi chết lúc submit), tách 'Hết chỗ' khỏi 'Đã đóng', `birth_year` bỏ format nghìn, ảnh bỏ alt mặc định. 12/12 hồi quy. ĐÃ DEPLOY UAT → ch.64 |
| 50 | 08-09 | **Trạng thái trống dữ liệu Home Mobile** (`Tasks!STT11`). 5 khối rỗng gom về 1 component `.wj-empty-state--row` thêm mới (KHÔNG sửa selector cũ — 8 module dùng chung). KPI Công nợ phân biệt `—` (chưa có chứng từ) với `0 đ` (đã tất toán) bằng 1 `search_count` chỉ ở Home. Đọc mockup SVG Figma phải nhúng inline + `getBBox()` vì SVG là outline path, không có `<text>` (L9). ĐÃ DEPLOY UAT → ch.63 |
| 49 | 08-07 | Hai hạng mục cùng ngày. **(a) UI PC Công nợ & Thanh toán** (BA `Tasks!STT10`, Figma PC v1.1 node `5077:*`, commit `2347f97`): `wujia_portal_debt` `19.0.2.x`→`19.0.3.1.0`, khối desktop `.wj-debt-pc` (`d-none d-lg-block`) bám `wj-pc-*` + shell `pc_source_ui_v1_5` — 2 tab cố định, filter tuần + pill cửa hàng readonly, summary 3 cột **5 biến thể trong MỘT layout** (outstanding/partial/paid/empty_week/no_debt), bảng hoá đơn 8 cột phân trang 10/trang, tab lịch sử + ô tìm kiếm + phân trang, modal QR (nút/backdrop/Esc). Rule 10c/10d tách bằng `_empty_substate()` đọc field store `portal_debt_remaining` (**0 query**). Seam mở rộng **additive** (mỗi hoá đơn +`total/paid/remaining`; `get_payments` +`keyword`) + context PC ở controller — KHÔNG đổi field/rule/migration, **mobile byte-identical** (bọc `d-lg-none`), KHÔNG sửa `_pc_components.css` shared. QR = placeholder CSS, PDF cố ý no-op (đúng ghi chú BA). 19 test (+5 PC). **(b) `wj_ajax_list`** (commit `f83ccd1`): tổng quát hoá cơ chế lọc/phân trang không reload của Giao hàng lên `wujia_portal_base` cho **11 màn danh sách** (2 chế độ: swap block tự fetch / fragment route) + 2 helper controller `page_numbers`/`group_counts` (1 `_read_group` thay N `search_count`). Đo 136ms→32ms mỗi lần bấm. → ch.62 |
| 48 | 08-02 | **Nối Portal Công nợ & Thanh toán vào backend thật** (BA `Tasks!STT9`, controller CT-050…CT-055). Module MỚI `wujia_account` (`19.0.1.0.0`): 3 field custom đã chốt (`account.move.franchise_id` store+index; `account.payment.franchise_id` **stored compute** từ `reconciled_invoice_ids`, readonly=False; `res.partner.bank.portal_payment_enabled`) + `sale.order._prepare_invoice` copy `franchise_id` + 2 aggregate perf trên `wujia.franchise.management` (`portal_overdue_invoice_count`+`portal_debt_remaining`, store) tính bằng **2 `_read_group` group-by franchise** + cron daily + `post_init_hook` + hook recompute khi hoá đơn/đối soát đổi (chỉ khi vals chạm `{state,payment_state,amount_residual,invoice_date_due,franchise_id}`) + 3 view inherit backend. KHÔNG model mới → không ACL; portal đọc bằng `sudo()`. `wujia_portal_debt` `19.0.1.0.0`→`19.0.2.0.0` (+depends `wujia_account`; AbstractModel → **không migration**): thay ruột `_demo_*` bằng query thật **giữ 100% key/kiểu dict** (dates vẫn `date`), credit note giảm nợ, payment loại draft/canceled, bank min-sequence enabled + memo tự dựng, `get_shell_badge` đọc field store (**0 query/trang**); controller **guard Owner/Manager** (Staff→trang "Không có quyền xem", KHÔNG 500; no-store→empty) + template `portal_debt_no_permission`. Verify **DB copy cô lập `wujia_tea_s48`** (port 8048, không đụng 8019): `-i wujia_account` RC=0 (5 cột+cron+2 index qua SQL); `-u wujia_portal_debt --test-tags wujia_debt` **14 test 0 failed/0 error** (11 seam + 3 HttpCase access); `seed_debt_demo.py` sạch + seam end-to-end đúng (total 7.5tr sau credit note, overdue, bank memo). Gotcha: freezegun cũ → test KHÔNG import `account.tests.common` (dựng fixture ORM trên `generic_coa`); HttpCase 404 khi db-name≠config → pin `--db-filter`. Task→Ready for Retest (không tự Done). **LIMIT**: ảnh QR defer (chờ BA tĩnh/động CT-055); UAT thiếu data kế toán (cần gán `franchise_id`+bật bank+ghi payment để retest); PDF hoá đơn CT-053 ngoài scope. Deploy tay: `-i wujia_account` rồi `-u wujia_portal_debt`. → ch.61 |
| 47 | 08-02 | Giỏ hàng trống Portal **mobile** theo Figma 5028:710 (UI-only, `wujia_portal_sale` `19.0.4.6.0`, 3 file, không migration). Thay empty-state generic bằng superset Acceptance STT8 #4: bộ đếm "0 sản phẩm" + hàng "Tiếp tục chọn sản phẩm" + card empty **tái dùng** `wj-empty-state--rich` (icon giỏ SVG 72px, "Giỏ hàng đang trống" + 2 dòng phụ + CTA "Chọn sản phẩm"→/portal/order). Fork Figma-vs-Acceptance (node không vẽ counter+continue-row) → chủ dự án chốt dựng đủ acceptance. KHÔNG sửa shared `_components.css` (8 module chung); delta px scope `portal_order.css`. Verify **DB copy cô lập `wujia_tea_s47`** (không đụng S46): RC=0/0 ERROR, Playwright 391×844 khớp node JSON (card 357×255, icon 72, title 20/700, CTA 147×42 #28A9DF, footer 83 active, badge giỏ ẩn khi 0, overflow=0), regression giỏ-có-hàng OK. → ch.60 |
| 44 | 08-02 | i18n backend: **636 nhãn/81 file** VN→EN (chỉ `string=`/`help=`/`_description=`/Selection/menu — KHÔNG đụng portal QWeb, `data/`, `ValidationError`, docstring, `tests/`) + **14 `vi_VN.po`** sinh từ `trans_export` thật ⇒ đổi ngôn ngữ 2 chiều. Tách **13 va chạm động-từ-vs-trạng-thái** (nút=`Reject`/`Archive`, state=`Rejected`/`Archived`; vi_VN vẫn ra "Từ chối"/"Lưu trữ" y hệt trước sprint). Bịt **3 seam portal** đọc thẳng `_fields[].selection` (franchise status · priority popup chuông · request_type) bằng hằng nhãn VN pin ở controller. Fix crash kanban O19 `kanban-box`→`card`. Verify: RC=0/49 test, SQL thiếu=0, 31/31 menu, portal 391×844 giữ VN ở cả 2 lang. → ch.57 |
| 43 | 07-31 | Công nợ & thanh toán portal mobile: module MỚI `wujia_portal_debt` dựng 7 màn Figma `5013:*` (`WJ_Debt_*_MVP_v31`) — **UI-only**, mọi số đi qua 1 `AbstractModel` (0 query, không bảng). Hồi sinh 2 điểm vào chết: KPI Home + dòng đầu sheet "Thêm" (làm bằng **view inherit từ module mới** ⇒ `wujia_portal_layout`/`_base` bất biến). 14 test, harness đo bbox/computed-style vs node JSON ALL OK. Sidenav PC **chưa nối** (chờ Figma PC). → ch.56 |
| 42 | 07-31 | Thông báo: nhãn `normal`→"Thông thường" (+fix popup chuông thiếu key `important`) · chặn ghi read-status khi chưa chọn cửa hàng (message spec F dòng 884) · **chọn đối tượng nhận** `target_mode` all/filter/manual + tiêu chí khu vực–tỉnh–trạng thái–trừ tiệm + "Xem trước danh sách nhận", chốt danh sách tại publish. 35 test, migration 19.0.2.2.0. **Portal/ir.rule/controller bất biến.** → ch.55 |
| 41 | 07-30 | Backend quản lý thông báo nhượng quyền (BA spec F, `Tasks` row 3): `wujia.notification` thêm `code`(ir.sequence)/`state`/`portal_visible`/`is_published_portal`(store+index)/`published_by_id` + rename `date`→`published_date`, bỏ cờ `published`; menu + form/list/search backend đầu tiên cho HQ; thống kê đọc batched `_read_group`; migration 19.0.2.0.0 (30 record OK); 19 unit test. **Portal bất biến** (giữ key JSON `date`). → ch.54 |
| 40 | 07-26 | Đặt hàng Mobile — trạng thái xử lý + kết quả gửi đơn (Figma 4963:2, BA `Tasks` row 5): helper `_next_order_window()` + 2 route GET `/portal/order/submitted/<id>` `/portal/order/rejected` + overlay "Đang tạo đơn" chống double-submit + BFCache reset. **PC bất biến** qua hidden `flow=m`. → ch.53 |
| 39 | 07-25 | UI Figma 2 cụm cuối: Auth 4 màn (Login/Forgot × PC+Mobile, thay boilerplate Vuexy EN) + Đăng ký thi PC 7 màn (list/create/detail + 2 modal + 4 state chi tiết + 8 UI state), UI-only. Phát hiện **4 rule tag-level `!important` của shell nuốt mọi class PC** → trung hoà trong scope. → ch.52 |
| 38 | 07-23 | Portal Order logic+UI a11y (Batch A/B: qty validate, atomic step, CTA AA #0F7CA8, aria, order-window banner) + Shell/layout re-fix (Batch C: navbar specificity 0,4,1, sidebar 300px, font-smoothing) — 28 issue → Ready for Retest; +web_icon fleet/franchise. → ch.51 |
| M | 07-18 | Backend Đăng ký thi: 7 model (time.slot/course/session/registration.line), capacity FOR-UPDATE, publish gate, migration `exam 19.0.3.0.0`. → ch.50 |
| K | 07-17 | Backend Bù hàng/Return: redesign single-product + duyệt + `compensation.allocation` + wizard SO 0đ FIFO + hook picking + portal wire tiến độ. → ch.49 |
| 37 | 07-16 | 7 issue mobile Home/Đặt hàng + chuẩn hoá page-header 4 trang → `wj_page_header`. → ch.48 |
| 35–36 + 38-dash | 07-16 | Dashboard workstream (đánh số riêng, KHÁC Sprint 38 portal ở trên): port Ninja v18→v19 (`wj_ks_dashboard_ninja` S35) + scaffold advance (`wj_ks_dn_advance` S36) + hotfix ks_speak (S38). → ch.46 + `dashboard-migration-plan.md` |
| 34 | 07-16 | Global Shell header/footer/typography theo BA Issue List UI-01..06 (Top Bar, Current Store 430px, vi_VN default, account pill, footer, Inter). → ch.45 |
| 33 | 07-14 | `wj_page_header` — chuẩn hoá header mọi trang (mobile+PC), rollout ~40 site/11 module. → ch.44 |
| 28,PC-1..3 | 06-22..07-13 | PC desktop rework: Foundation/shell + Đặt hàng (PC-1), Notification (PC-3), Tài khoản 9 màn (28), Giao hàng (27). → ch.31/38/39 |
| 30–32 | 07-12..13 | Controller theo BA FINAL: Đặt hàng (giỏ chung store, 30) + Lịch sử + realtime cart (31) + Thông báo + toolchain ba_spec (32). → ch.41-43 |
| 10–27 | 06-06..07-03 | Mobile theo Figma từng màn: Home/Order/Cart/History/Shell/More/Knowledge/Dashboard/Ticket/Return/Notification/Exam/Delivery. → ch.19-38 |
| 9 | 06-03 | Portal UI refactor UI-01..18 + empty state + cleanup (test_sprint9 7/7). → ch.18 + §9 |
| 6–8 | 05-17..21 | 30 route (forgot/reset PW, profile, cart AJAX atomic, exam lock) + `info_request` + `order_window` + design token. → ch.4-17 |
| 1–5 | 04..05-16 | Core/franchise/sale/fleet/delivery + 9 portal skeleton + Knowledge/Support full BA + test 20/20. → ch.4-17 |

---

## §5 wujia-current-status

**State (2026-08-27) — CỤM D3a (CardHeader `CMP-CH-001`, mắt xích 3 của bộ component chung):** ✅ dựng xong **`wj_card_header`** + kiểm kê + **4 họ markup mẫu**; issue **`UI-CARDHEADER-001` (STT 125) CỐ Ý GIỮ `Ready for Dev`** vì mới phủ **4/103 chỗ gọi** (tiền lệ C8a→C8b), entry ledger soạn sẵn **dạng comment** cuối `docs/qa-issue-ledger.yaml`. Nhánh `dev/2026-08-27-d3a`; `wujia_portal_layout` **19.0.32.5.0** · support **19.0.3.13.0** · delivery **19.0.3.7.0** · base **19.0.7.5.0** · return **19.0.2.8.0**, `?v=1178`; deploy `-u wujia_portal_layout,wujia_portal_support,wujia_portal_delivery,wujia_portal_base,wujia_portal_return,wujia_sale`. 🔑 **Kiểm kê `docs/d3-cardheader-inventory.md`**: 218 ứng viên trong card → 91 loại theo vai trò → **127 CardHeader** (96 heading thật + **31 giả-heading** `<p>/<span>/<div>` = đúng bệnh BA nêu) → loại tay 23 → **103 chỗ gọi thật**. Con số thật **nhỏ hơn hẳn** kế hoạch D: `wj-pc-card__title` **23–25** (không phải 154), `wj-pc-acct-headcard` **19** (không phải 33). 🔑 **Phân loại theo TỔ TIÊN DOM, không theo tên class** (kế thừa C8) và **vị trí trong card KHÔNG phải tiêu chí** — bộ lọc "phải là con đầu của card" từng loại nhầm **31** header hợp lệ. 🔴 **Bẫy mật độ chỉ lộ khi đo:** gỡ `mb-*` ở call site **chưa đủ**, `/portal/support` vẫn cách 28px vì lớp body tự khai `margin-top` (`.wujia-content-card-table{margin:16px…}`) ⇒ trung hoà bằng `.wj-card-header + <lớp-body>{margin-top:0}`, danh sách này rút dần theo D3b…D3n. 🔴 **`--log-level=warn` nuốt dòng `odoo.tests.result` khi PASS** (INFO; chỉ FAIL mới ERROR) ⇒ dùng `--log-level=test`. 🔑 **A/B bằng 2 DB** (d2 = arch cũ :8066, d3 = arch mới :8067, clone nên dữ liệu y hệt) rẻ và sạch hơn A/B bằng CSS interception khi thay đổi nằm ở `arch_db`. **Đo — `docs/d3-acceptance-matrix.md` 19✅/1⚠️/1⏳ = 95%**: mật độ **16/16 ô không cao lên**, 3/4 route thẻ thấp đi **20.6 / 22.0 / 104.0px**, record trong viewport không giảm (2 ô tăng); số BA khớp tuyệt đối (PC compact **18/24/700** gap 12 · PC regular **20/28/700** gap 16 · mobile compact **16/22/700** gap 8 · mobile regular **18/24/700** gap 12 · subtitle 14/20/400 · `padding:0` 10/10 · màu `#111827`); giả-heading **8 → 0**; touch mobile `::after` **44px mà header vẫn 22→22**; **count 0 nay hiện "0 ticket"** (trước biến mất). Hồi quy: **B4 286/286**, tab-walk **175 stop giữ nguyên thứ tự + ring**, font **Inter 514/514 y hệt** (giữ kết quả D2), chữ hiển thị **12/12 ô không đổi** ⇒ `WJ-DELIVERY-006/007` không bị đụng, `wj_ajax_list` swap giữ nguyên header + `__meta` là **con trực tiếp** (bài học B3a), **105 test xanh** (5 module) + **26 test mới** đã chứng minh có ý nghĩa bằng **mutation** (trả `t-if="tickets"` vào ⇒ đúng 1 test đỏ). ⚠️ **4 chỗ treo chờ BA:** (1) `wj-pc-acct-headcard` **2 vùng trailing** mà spec cho tối đa 1 ⇒ **chưa đụng file**, lấy `wj-pc-acct-panel-title` làm mẫu thay; (2) spec tự chỏi *"wrap tối đa 2 dòng"* + *"không ellipsis"* ⇒ Dev chọn **hiện đủ chữ** (2 dòng chứa ~64 ký tự @360 / ~96 @1440, tiêu đề dài nhất trong mã nguồn **22 ký tự** ⇒ thực tế luôn 1 dòng); (3) 3 chỗ SectionHeader/CardHeader treo từ C8 **không tự quyết lại**; (4) **FilterCard = 0 call site ⇒ là dựng MỚI**, không phải migrate. 🆕 BA đổ thêm **6 issue STT 128–133** (StatusBadge · PageContainer · Pagination · Sidebar · Button · UI-MOB-HOME-004) — chủ dự án chốt **ghi nhận thành lứa D7+, chưa đụng**. **Việc tồn 27/08 XONG:** nhãn "tất cả" bộ lọc bù hàng gom về **một hằng** `FILTER_ALL_LABEL`, PC `— Tất cả —` → **`— Tất cả trạng thái —`** khớp mobile, `value=""` không đổi ⇒ kết quả lọc bất biến. ✅ **ĐÃ DEPLOY + ĐO LẠI TRÊN UAT 28/08** (bắt buộc vì L14/L10: UAT có `website`/`website_sale` ⇒ bundle frontend khác local, từng lật ngược kết quả ở C6 và D2 — lần này **không** lật): **14 header × 3 breakpoint 1440/390/360 · 0 lỗi**, khớp 100% số BA, 0 giả-heading, 0 tràn ngang, 0 JS error, HTTP 200; nhãn lọc PC = mobile; `?state=cancelled` ra `0 ticket` (count 0 vẫn hiện). 🔴 **Bẫy harness đo UAT:** kỳ vọng phải **theo nền tảng** — header PC bọc `d-none d-lg-flex` nên ĐÚNG RA phải vắng ở 390/360, harness mù nền tảng báo **7 lỗi giả**. 🆕 Phát hiện thêm khi quét UAT: **nhãn option rỗng của bộ lọc lệch GIỮA CÁC MÀN** (return `— Tất cả trạng thái —` · support `— Tất cả —` · delivery + purchase-history `Tất cả trạng thái` không gạch em · notification dùng tên trường) — **4 kiểu chữ cho một ý**, không màn nào lệch PC↔mobile ⇒ nợ nhất quán, ghép **D7+** hoặc làm kèm khi D3b/D3c động đúng file. **Kế tiếp: D3c** (xem §Bàn giao `docs/next-session-clusters-D.md`).

**State (2026-08-28) — CỤM D3b (CardHeader `CMP-CH-001`, nhóm màn thứ hai):** ✅ migrate **26 call site / 7 file / 7 module** ⇒ phủ **36/103**; issue **`UI-CARDHEADER-001` VẪN `Ready for Dev`** (đúng thiết kế, tiền lệ C8a→C8b), ledger vẫn **dạng comment**, **KHÔNG** chạy `qa_sync.py`. Nhánh `dev/2026-08-28-d3b`; layout **19.0.32.6.0** · base **19.0.7.6.0** · knowledge **19.0.3.10.0** · notification **19.0.2.8.0** · report **19.0.2.1.0** · portal_sale **19.0.4.14.0** · info_request **19.0.1.6.0** · return **19.0.2.9.0**, `?v=1179`; deploy `-u wujia_portal_layout,wujia_portal_base,wujia_portal_knowledge,wujia_portal_notification,wujia_portal_report,wujia_portal_sale,wujia_portal_info_request,wujia_portal_return,wujia_sale` (**bắt buộc kèm `wujia_sale`**). 🔴 **Vá 2 lỗ hổng component chưa ai dính:** (1) `.wj-card-header--flush` khai ở **(0,1,0)** nên **thua** `.wj-card-header--m.wj-card-header--compact` **(0,2,0)** ⇒ `--flush` không ăn ở mobile/`--any`, phải khai kèm `--m`/`--any`; (2) `--any` (một markup cho cả hai nền tảng, họ Bootstrap `.card-header`) chỉ có **số mobile ở mọi bề rộng** ⇒ thêm khối `@media (min-width:992px)`. 🔴 **Bắt được 1 lỗi thật khi đo:** `/portal/info-request` **mất hẳn tiêu đề ở mobile** vì view đó **không** có khối `d-none d-lg-block` (một markup phục vụ cả hai nền tảng) mà vẫn bake `ch_platform='pc'` ⇒ **trước khi đặt `ch_platform` phải `grep "d-none\|d-lg-"` trong CHÍNH view**; đã chốt bằng test `test_shared_markup_views_do_not_bake_platform` (assert theo *directive* `t-set="ch_platform"`, không theo chữ — comment cũng chứa chuỗi đó). 🔑 **Đo rồi mới thêm rule — và kết luận là KHÔNG thêm gì:** 6 lớp body "ứng viên" (`.card-body`, `.list-group`, `.wujia-mknow-body`, `.wj-rep-pccard__body`, `.wj-pc-kv-grid`, `.wj-pc-noti-detail-body`) **không lớp nào cộng chồng** — mọi `gapToBody` đều giảm (18→12, 14→12, 10→8); thêm rule mù là **bóp nhịp xuống dưới chuẩn BA**. Chỉ thêm 2 rule loại khác, bù khoảng cách với khối **PHÍA TRÊN** mà class cũ từng gánh (`.wujia-mknow-article > .wj-card-header{margin-top:16px}` · `.wujia-mnoti-detail-card > .wj-card-header{margin-top:18px}`). **Đo — `docs/d3b-acceptance-matrix.md` 21✅/1⏳ = 95,5%**: tổng `scrollHeight` **−24px** trên 44 ô, 19 thẻ thấp xuống / **4 thẻ cao lên 2–10px nhưng do CHUẨN BA** (tiêu đề cũ `h6` **12.3px**, `h5` **15.3px**, `h3` **14px** — đều dưới chuẩn); giả-heading **0/44 ô**; 4 ô số BA khớp (`--pc/--any --compact` **18/24** gap 12 · `--any --regular` **20/28** gap 16 · `--m/--any --compact` **16/22** gap 8 · `--any --regular` mobile **18/24**); outline **8 route giữ nguyên / 3 tốt lên** (H6→H4 ×2, H5→H4 — component chỉ có `h2/h3/h4`); chữ hiển thị **43/44 ô giống hệt**, 1 ô đổi **đúng chủ đích** (`/portal/info-request` mọc thêm `0 kết quả` = yêu cầu BA "count 0 vẫn hiển thị"). Hồi quy: **B4 286/286**, **chạy lại nguyên bảng D3a = 356 phép so, 0 lệch** (chứng minh 2 nhóm rule CSS mới **trơ** với call site cũ), tab-walk **433 stop · số stop 16/16 · ring 16/16** (3 stop chỉ đổi TÊN CLASS, cùng index, hình học lệch ≤4px), font **66 tiêu đề 0 lệch** family/weight/color, `wj_ajax_list` swap giữ đúng 1 header/slot + trailing là **con trực tiếp**, 0 `pageerror` / 0 tràn ngang trên 44 ô, **170 test xanh** + 4 test mới có **mutation check** (trả `t-if="returns"` vào ⇒ đúng 1 test đỏ). 🔴 **Bẫy log (L15):** `wujia_core` **dời logfile giữa chừng** sang `<thư-mục-logfile>/<năm>/<tháng>/<ngày>.log` ⇒ file `--logfile` chỉ có ~49 dòng đầu, **không** có dòng `odoo.tests.result`; traceback duy nhất trong đó là `py.warnings` dump stack của một `DeprecationWarning` (`wj_ks_dashboard_ninja/.../ks_domain_fix.py:8`), **không phải lỗi**. 🔴 **Dữ liệu rỗng làm call site "tàng hình":** 0 sản phẩm có `public_categ_id` ⇒ `related` luôn rỗng ⇒ header `--any` **duy nhất** không đo được; phải bơm dữ liệu **giống hệt nhau trên CẢ HAI DB copy**. ⚠️ **LIMIT giữ nguyên:** 36/103 (còn D3c…) · 6 chỗ treo chờ BA (thêm mới: `/portal/notification/41` có **H3 trước H2** — lỗi outline **có sẵn**, đề xuất R2) · `/portal/reports/orders` **500 khi tz = `Asia/Saigon`** (lỗi có sẵn, cụm R3 — đã đổi tz user thành `UTC` trên **cả 2 DB copy** để đo, KHÔNG sửa) · `portal_home.xml` còn `rr.request_date.strftime(...)` thay vì `wj_dt` (đề xuất R2) · chưa xoá class CSS cũ (ràng buộc thứ tự C8a→C8b) · đo trên local ⇒ **phải đo lại chỉ-đọc trên UAT sau deploy** (L14/L10) rồi mới chạy `qa_deploy_mark.py`. 🔴 **ĐÃ ĐO LẠI TRÊN UAT (28/08) — bắt được lỗi thật thứ hai:** title trong `.card > .card-body` ra **`#212529`** thay vì `#111827`. Giả thuyết "thiếu token ⇒ `var()` invalid ⇒ inherit" **SAI** (token có ở cả `:root` lẫn element). Thủ phạm truy bằng CDP `CSS.getMatchedStylesForNode` + `matchingSelectors`: theme Vuexy có `:where(.card:not([data-vxml])) .card-body:not(.card[data-vxml] .card-body) h4 { color: inherit }` — **(0,4,1)** vì `:not()` **mang độ đặc hiệu của THAM SỐ**, đè `.wj-card-header__title` (0,1,0). Chỉ `/portal/order/product/<id>` dính (card duy nhất là `.card > .card-body` thật); lỗi **có cả trước lẫn sau migrate** nên không phải hồi quy nhưng vẫn **sai số BA**. Vá: `color: var(--wujia-text-primary) !important` + test `test_title_colour_beats_theme_card_body_rule` (**mutation đỏ đúng 1 test**) ⇒ layout **19.0.32.7.0**, **`?v=1180`**, cần **deploy lượt 2 chỉ `-u wujia_portal_layout`**. Sau vá: **46/46 header, 0 vấn đề** trên chính UAT (13 route × 3 breakpoint) ⇒ nghiệm thu **21✅/1⏳ = 95,5%**. 🔑 **Harness đo sai còn nguy hơn không đo:** lượt UAT đầu báo 9 cảnh báo thì **chỉ 3 là lỗi thật** — 3 do slug `quy-trinh-bao-tri` **không tồn tại** (Odoo trả trang danh sách, đo nhầm trang; phải lấy `href` thật từ trang list, đã đổi sang `ui12-01`), 3 do kỳ vọng `padding: 0` **quá rộng** (header cố ý giữ class container cũ `wj-rep-pccard__head` mang `padding:16px` — chính là lý do phải `--flush`). **Kế tiếp: D3c** = 15 chỗ còn lại của 4 file D3a (support 6 · delivery 3 · franchise-information 6).

**State (2026-09-02) — CỤM D3c (CardHeader `CMP-CH-001`, nốt 4 file của D3a):** ✅ migrate **11 call site / 3 file / 3 module** (support 6 · delivery 1 · franchise-information 4) ⇒ phủ **47/103**; issue **`UI-CARDHEADER-001` VẪN `Ready for Dev`** (đúng thiết kế, tiền lệ C8a→C8b), ledger vẫn **dạng comment**, **KHÔNG** chạy `qa_sync.py`. Nhánh `dev/2026-09-02-d3c`; base **19.0.7.7.0** · support **19.0.3.14.0** · delivery **19.0.3.8.1** · layout **19.0.32.7.0** (không đổi, chỉ thêm test); deploy `-u wujia_portal_layout,wujia_portal_base,wujia_portal_support,wujia_portal_delivery` — **không** cần kèm `wujia_sale`, **không** cần bump `?v=` (rule CSS mới nằm trong asset bundle của delivery, không phải `<link>` tay). 🔴 **Đính chính con số bàn giao:** doc D3b ghi "D3c = 15 chỗ", chạy lại `d3_inventory.py` thì **11** — con số cũ cộng cả dòng đã loại ở §3 inventory và 2 chỗ đang chờ BA ở §6 ⇒ **đừng cộng cột 'Call site' của bảng-theo-file để suy ra việc còn lại**. 🔴 **Số đo đạt hết vẫn có thể VỠ BỐ CỤC — phải chụp ảnh:** sau migrate, badge trạng thái `/portal/delivery/<id>` **trôi ra mép phải cột trái** vì `.wj-card-header__lead` là `flex:1 1 auto` nở hết bề rộng, trong khi mọi `fontSize/lineHeight/color/gapToBody` đều Pass. Sửa bằng **1 rule scope module** `.wj-pc-dlv-head .wj-card-header__lead{flex:0 1 auto}` trong `portal_delivery.css`, **không** đụng component chung ⇒ `cardH` summary head **105 → 92**. 🔑 **3 fork chủ dự án chốt** (ghi vào `d3-cardheader-inventory.md` §6, đừng mở lại): (1) `wj-pc-acct-staff__title` **là CardHeader**, icon/`__line`/`__tag` ở lại ngoài header; (2) **ĐỔI thứ tự DOM** mobile `/portal/franchise-information` — tên cửa hàng lên làm `ch_subtitle`, badgerow xuống dưới, **khớp bản PC của chính card đó**; (3) delivery PC: title = mã chuyến · subtitle = "Chuyến giao cho…" · trailing = badge, khối 3 cặp KV **ở lại là nội dung card** (spec cho **tối đa MỘT** trailing). **Đo — `docs/d3c-acceptance-matrix.md` 20✅ + 1 phần = 97,6%**: tổng `scrollHeight` **−19px**/16 ô; giả-heading **0** trên cả 4 route (1→0, 1→0, 2→0, 4→0); số BA khớp tuyệt đối (PC compact **18/24/700**, mobile compact **16/22/700**, màu **`rgb(17,24,39)` 11/11 call site**); `line-height` cũ lệch khắp nơi (19,2 · 21 · 26,4 · 28,8 · **14,7**) nay về đúng **22/24**; outline **2 route tốt lên** (`H1 H5 H6 H6 H6`→`H1 H3 H3 H3 H3`; `H1 H2 H3 H2 H2`→`H1 H2 H2 H2`) + 1 route mọc heading thật; nhịp `gapToBody` **14→8, 12→8, 0→0, 12→12** ⇒ **KHÔNG thêm rule nhịp nào** (lặp lại kết luận D3b); chữ hiển thị **multiset token bằng nhau 16/16 ô**, 4 ô lệch **đúng kiểu hoán vị** của 2 chỗ đổi thứ tự đã duyệt. ⚠️ **3 thẻ PC `/portal/support/40` cao thêm 9,3px mỗi thẻ** — do `h6` cũ render **12,3px** (chuẩn BA 18px), là **hệ quả của chuẩn**, không phải lỗi (tiền lệ D3b). Hồi quy: **B4 286/286** · tab-walk **250 stop, 12/12 giữ số/thứ tự/ring** · chạy lại bảng **D3b 0 lệch**, bảng **D3a 0 lệch ngoài ý muốn** (6 trường lệch đều là thay đổi D3c cố ý) · 0 `pageerror` · 0 tràn ngang · HTTP 200 toàn bộ · unit test **32, 0 failed/0 error** + 2 test mới, **mutation đúng 1 đỏ**. 🔴 **4 bẫy mới đã trả giá:** (1) comment XML **không được chứa `--`** ⇒ viết `--flush` trong `<!-- -->` là `XMLSyntaxError`; (2) test `post_install` nằm ở module khác ⇒ `-u wujia_portal_support --test-tags …` ra **"0 tests"** mà RC vẫn 0, mutation phải `-u wujia_portal_layout`; (3) `/portal/support/1` `/2` **âm thầm redirect** về trang list vì thuộc cửa hàng khác cookie đang ghim ⇒ **scrape `href` thật từ trang danh sách** (bẫy D3b lặp); (4) bơm attachment cho ticket phải **insert bảng nối M2M** `wujia_support_ticket_attachment_rel` (đặt `res_id` là không đủ), và `mail_message_subtype.name` là **jsonb** ⇒ `WHERE name->>'en_US'='Discussions'`. 🔑 **Harness báo động giả lần nữa:** bảng D3b báo `/portal` 360px `cardH` 97→122,4; đo lại thì **cả hai server** đều 122,4 — thẻ chứa **đồng hồ**, cao theo phút xuống dòng, không phải hồi quy. ⚠️ **LIMIT giữ nguyên:** 47/103 (còn D3d…) · `wj-pc-acct-headcard` 2 vùng trailing **vẫn chờ BA** · 3 chỗ SectionHeader↔CardHeader treo từ C8 · FilterCard **0 call site ⇒ dựng mới** · nhãn option lọc lệch 4 màn (D7+) · `/portal/reports/orders` 500 khi tz `Asia/Saigon` (R3) · `portal_home.xml` còn `.strftime()` + `/portal/notification/41` H3 trước H2 (R2) · chưa xoá class CSS cũ (khoá tới 100%) · **đo trên local ⇒ PHẢI đo lại chỉ-đọc trên UAT sau deploy** (L14/L10) rồi mới `qa_deploy_mark.py`. **Kế tiếp: D3d = `portal_exam.xml`, 21 call site** (một mình một session).

**State (2026-09-03) — AUDIT hợp nhất D3a+D3b+D3c, KHÔNG code call site mới:** 🔴 **Phát hiện đầu phiên: D3c chưa từng lên UAT** — kiểm phiên bản module qua XML-RPC (đọc-chỉ) ra `wujia_portal_base/support/delivery` vẫn đúng số **D3b**, không phải D3c; `git` lộ nguyên nhân: commit D3c (`b6cfcbe`) code xong nhưng **quên `merge`+`push`**, nằm một mình trên nhánh `dev/2026-09-02-d3c` cả 1 ngày trong khi mọi module khác (`wujia_sale`/`wujia_franchise`/`wujia_portal_return`) khớp tuyệt đối đĩa↔UAT ⇒ **không phải deploy hỏng, chỉ là bỏ sót bước cuối phiên**. Đã `git merge --ff-only` (xác nhận trước fast-forward sạch, 0 conflict) + `push origin main`; chủ dự án deploy `-u wujia_portal_layout,wujia_portal_base,wujia_portal_support,wujia_portal_delivery` ngay trong phiên. 🔑 **Audit code 2 kiểu lỗi đã từng dính, quét lại trên cả 47/103 call site** (không code thêm, chỉ soi): (a) tràn `.wj-card-header__lead{flex:1 1 auto}` (gốc D3c: `width:100%` của component "kéo" div cha giành hết chỗ trong `.wj-pc-order-head{display:flex;justify-content:space-between}` không khai flex-basis con) — quét 13/13 call site còn lại dùng slot `ch_control`/`ch_meta` (rủi ro cao nhất), **không chỗ nào khác tái diễn** vì `.wj-pc-card`/`.wj-pc-cart` là `display:block`, `.wujia-content-card` là `flex-direction:column`, `.wj-rep-pcrow` dùng CSS Grid cột khai cứng — chỉ `wj-pc-order-head` (dùng chung với `portal_history.xml` CHƯA migrate, để dành D3e) có cấu trúc này; (b) màu bị theme đè — fix đã lên **component** (`!important` tại `_components.css:3189`) nên tự phủ mọi call site, không cần quét từng chỗ; (c) rò CSS ra ngoài scope (bài học C6/D2) — `git diff` D3a→D3c chỉ đụng 4 file CSS, không file nào là `_wujia_theme.css`/`_variables.css`, mọi rule mới khoá bằng lớp `.wj-card-header*` hoặc combinator cha-con cụ thể. **Kết luận: không có regression tiềm ẩn nào chưa lộ ra bằng số đo.** Regression: DB copy `wujia_tea_d3c` upgrade lại `main` mới merge, **183 test 0 failed/0 error** trên 10 module bị đụng (bẫy `dbfilter` S48 tái hiện: 10 test `wujia_portal_knowledge` "fail" ban đầu hoá ra bị đá về login vì config `dbfilter=^wujia_tea_19$`, không phải lỗi code — sửa bằng `--db-filter` khớp tên DB). ✅ **Đo lại chỉ-đọc ngay trên UAT sau deploy** (L14/L10) bằng `scratchpad/d3c_uat_verify.py` (gộp 13 route D3a+D3b regression + 6 route mới D3c, 3 breakpoint 1440/390/360, thêm kiểm `trailOverflowsCard`): **74/74 header đạt toàn bộ số BA**, 0 tràn ngang, 0 JS error, HTTP 200 toàn bộ. 9 dòng "vấn đề" script tự in ra ban đầu **cả 9 đều false-positive của chính script audit** (đối chiếu source xác nhận): 7 dòng "giả-heading `card-header`" là do tự thêm lớp đó vào danh sách quét nhưng nó vừa là div Bootstrap bao ngoài component đã migrate vừa là `ch_class` gắn thêm lên chính component, không phải tiêu đề cạnh tranh; 2 dòng "KHÔNG THẤY" ở `/portal/support/new` là do tự chép nhầm 4 tiêu đề của màn "Tạo yêu cầu bù hàng" (`wujia_portal_return`) vào route support — lỗi gõ route trong file audit, không phải lỗi sản phẩm. Chi tiết đầy đủ → `docs/d3-consolidated-audit-2026-09-03.md`. `UI-CARDHEADER-001` **vẫn `Ready for Dev`** (47/103, đúng tiền lệ C8a→C8b) ⇒ **KHÔNG** chạy `qa_sync.py`. **Kế tiếp: D3d = `portal_exam.xml`, 21 call site** (một mình một session, chưa đụng phiên này).

**State (2026-08-26) — CỤM D2 (font Portal):** ✅ **`WJ-PORTAL-UI-002` (STT 115) → `Ready for Retest`** (`b70d26a`, nhánh `dev/2026-08-26-d2`, `wujia_portal_layout` 19.0.32.3.0→**19.0.32.4.0**, `?v=1177`). **Gốc rễ đo bằng CDP `CSS.getMatchedStylesForNode`, không đoán**: bundle Odoo/website đặt `h1..h6,.h1..h6{font-family:"Inter Tight",…}` — specificity chỉ (0,0,1), **không `!important`**, nhưng **một rule khớp element luôn thắng kế thừa từ `html body{…!important}`, bất kể specificity**. Rule ép Inter cũ (UI-06/S39) neo `.content-wrapper` ⇒ mọi heading **ngoài** neo giữ Inter Tight: BlankShell mobile `.wujia-mpage` (cố ý ngoài `.content-wrapper` từ pattern S15), vùng `wj_ajax_list` swap của `wujia_portal_inspection`, popup chuông. 🔑 **Cùng một class ra hai font tuỳ trang** (`wj-pc-card__title` = Inter ở `/portal/debt`, Inter Tight ở `/portal/inspection`) — **tổ tiên DOM quyết định, không phải tên class** (đúng bài học kiểm kê C8). Fix **2 dòng, 2 file**: `_wujia_theme.css` đổi neo `.content-wrapper` → **`html body`** + phủ thêm class `.h1..h6`; `_variables.css` nối `--wujia-font-family` fallback **tên font hệ thống** Thai/CJK (chủ dự án chốt 26/08: **không tự host**, 0 byte tải thêm, perf-first 1500 user). 🔑 `wj_section_header` là component chung C8 (**19 call site**) ⇒ sửa ở tầng `h1..h6` fix một lần, không đụng 19 chỗ. 🔴 **CỐ Ý không nới rule `font-weight:700 !important`** ở cạnh (cũng neo `.content-wrapper`): nới sẽ ép heading mobile về 700, **phá weight 800 của `CMP-SH-001`** (C8a/C8b) + bảng B3a/B4 đã Pass. 🔴 **L14/L10 lặp lại lần 3**: local **không có `website`/`website_sale`** ⇒ không tái hiện được lỗi ⇒ toàn bộ đo trước–sau làm **chỉ-đọc ngay trên UAT** bằng `page.add_style_tag` (phương pháp C6), **16 route × 5 breakpoint = 80 ô**. Kết quả: chỗ ≠ Inter **145 → 0** (49 hiện + 96 ẩn), `"Inter Tight"` **0** lần trong computed, icon font `feather`+`FontAwesome` **giống hệt 80/80**, chiều cao/số dòng heading/tràn ngang/`font-weight`/`pageerror` **80/80 y hệt** ⇒ **14/14 acceptance (100%)**. ⚠️ **3 bẫy đo đã trả giá**: (1) quét chỉ leaf ⇒ bỏ sót heading có `<span>` con — phải `TreeWalker` mọi text node; (2) khối PC nằm sẵn trong DOM mobile (`d-none`) ⇒ phải tách **hiện/ẩn** bằng `getClientRects()`; (3) 8 ô lệch chiều cao trong bản "sau" là **artifact của việc nhúng lặp `_variables.css`** — chứng minh bằng **run đối chứng nhúng bản HEAD chưa vá** (số y hệt), rồi mới so `control ↔ after` = **0 lệch** (bẫy C6 tái diễn). ⚠️ Chữ Trung ra **`Noto Sans CJK JP`** — sai biến thể vùng — vì tên family theo fontconfig Linux là **`Noto Sans CJK SC`**, thiếu alias này là rơi sang bản JP; thêm vào stack ⇒ đúng SC. `'Odoo Unicode Support Noto'` của Odoo chỉ phủ Cyrillic/Hebrew/Arabic/Telugu, **không** phủ zh/th ⇒ copy nguyên stack Odoo là vô ích. Build `-u wujia_portal_layout` trên DB copy **`wujia_tea_d2`** (nền `wujia_tea_d1`, **port 8066**) → **RC=0, 0 ERROR** (1 ERROR `wujia_portal_remediation` inconsistent là **có sẵn từ DB nền**, mirror UAT bằng cách set `uninstalled` ⇒ sạch), lưới B4 **286/286**, tab-walk A/B **331 stop** thứ tự 12/12 + ring 12/12 y hệt → `docs/d2-acceptance-matrix.md` + `docs/d2-font-inventory.md`. ✅ **ĐÃ DEPLOY UAT 27/08** (XML-RPC: `wujia_portal_layout 19.0.32.4.0 installed`, trang thật phục vụ `?v=1177`) **+ đo lại chỉ-đọc ngay trên UAT: 14/14**. Lượt này **không nhúng CSS** nên là phép so sạch nhất cả cụm: chỗ ≠ Inter **145 → 0**, `"Inter Tight"` 0/80 ô, **`scrollHeight` 0/80 lệch · số dòng heading 0 lệch · `font-weight` 0 lệch · 4.965 glyph icon giống hệt 80/80**, fallback thật trên server ra **Inter / Noto Sans Thai / Noto Sans CJK SC**, chỗ BA than (desktop Khảo sát "Danh sách phiếu khảo sát") nay **Inter/700**; hồi quy trên chính UAT: **B4 286/286**, tab-walk 6 route × 2 viewport **346 stop** (342 có ring — 4 chỗ thiếu là có sẵn từ trước, **0 điểm dừng còn font ≠ Inter**) → `docs/d2-acceptance-matrix.md` §"Đo lại trên UAT sau deploy" (LIMIT-6 đã đóng). **Lứa mới D1–D6 + R1–R5 → `docs/next-session-clusters-D.md`** (gõ "làm cụm D&lt;n&gt;"); ✅ **D1 xong 25/08** (`8613813` code + `98fb01c` docs): `UAT-BH-001/003/005/006` → `Ready for Retest`, 26/26 đo đạt, **ĐÃ DEPLOY UAT 27/08 cùng chuyến** + đo lại chỉ-đọc **27/28** (BH-003 0 dòng mẫu `REF####` + view riêng 6 cột; BH-005 đủ 4 control 2 viewport; BH-006 8 nhãn phân biệt, lọc 8/8 giữ lựa chọn, `2+2+2=6` đúng tổng). 🔴 **BH-001 cố ý KHÔNG đo trên UAT** (chạy wizard = tạo SO thật, vi phạm QA §10) — để BA retest. 🔴 **1 phát hiện thật, chưa sửa**: option "tất cả" của bộ lọc bù hàng lệch chữ giữa PC (`— Tất cả —`) và mobile (`— Tất cả trạng thái —`) vì gõ tay 2 chỗ (`portal_return_list.xml:42` / `:187`) — 8 nhãn trạng thái thật thì khớp 100%; đề xuất ghép vào chuyến deploy D3. 🚚 **Hàng đợi deploy: TRỐNG.** ✅ **Sheet đã ghi xong 27/08**: cả 5 issue (`WJ-PORTAL-UI-002` + 4 `UAT-BH-*`) nay ghi `ĐÃ DEPLOY UAT 27/08/2026 — sẵn sàng retest` ở cột `Build / Deploy` + ngày 27/08 + 5 dòng `7. ISSUE HISTORY`, verify bằng CSV công khai ⇒ BA retest được ngay. 🔧 **Công cụ mới `scripts/ba_spec/qa_deploy_mark.py`** — sinh ra vì `qa_sync.py` **cố ý idempotent** (issue đã `Ready for Retest` thì không ghi đè) nên cột Build/Deploy **mắc kẹt ở "Chờ deploy UAT" sau mỗi lần cài**, BA tưởng chưa deploy được; script chỉ đổi phần đầu cột P + ngày + dòng History, không đụng trạng thái/owner. **Chạy nó sau MỖI lần deploy UAT.** ⚠️ **3 bài học công cụ (27/08)**: (1) **bridge trả 404 KHÔNG có nghĩa là chết** — ping lỗi 404 vài lần, GET ra trang "Không tìm thấy trang — Drive", đã kết luận nhầm deployment bị xoá; vài phút sau ping lại **4/4 OK không sửa gì**, là lỗi tạm của Google ⇒ **thử lại sau vài phút** trước khi nhờ deploy lại bridge; (2) **404 không có nghĩa là chưa ghi** — lần `--apply` bị 404 đó **đã ghi xong ô**, chỉ là response không về nên raise trước khi thêm History ⇒ ghi một nửa mà log báo fail; bridge lỗi thì **đọc lại sheet để kiểm**, đừng chạy lại mù; (3) **tra dòng phải gọi đúng tên `"Issue List"`** (có trong `KNOWN_GID` ⇒ CSV, số dòng tuyệt đối) — `read_values("5. Issue List")` rơi xuống `gviz` và **ăn theo filter đang bật** ⇒ 25 dòng, issue ở dòng 7 thay vì 108, ghi theo đó là **đè nhầm issue khác**. Bàn giao đầy đủ → `docs/next-session-clusters-D.md` §Bàn giao.

**State (2026-08-23 · phiên 2) — BATCH A11Y + REVIEW SWEEP:** ✅ **WJ-PORTAL-UI-003/004 (STT 116/117) → `Ready for Retest`** (`36188e1`, `wujia_portal_layout` 19.0.32.2.1). UI-003 = **1 rule CSS** (`_wujia_theme.css`: `@media ≤1199.98px` `body:not(.menu-open) .main-menu{visibility:hidden}` — `visibility` gỡ khỏi cả tab order lẫn a11y tree, `.menu-open` khôi phục, desktop bất biến). UI-004 = nút "Thêm" `<a href="#">`→`<button>` (Space chạy) + `aria-expanded`/`aria-modal` + focus-trap trong `wujia_mobile_more_sheet.js` (mở → focus nút đóng, Tab vòng, đóng → trả focus nút Thêm). ⚠️ Gotcha: `.wujia-msheet` base transition cả `visibility` ⇒ `focus()` cùng frame trượt — state `.is-open` chỉ transition `transform`. Đo trên DB copy `wujia_tea_a11y` (port 8062, nền `wujia_tea_mt2`): acceptance **6/6 + 6/6 (100%)**, tab-walk 6 route × 2 viewport 298 stop 12/12, lưới B4 **286/286**, 24 unit test 0 failed → `docs/a11y-batch-acceptance-matrix.md`; ledger + `qa_sync --apply` xong. ⚠️ Env gotcha S48 tái hiện: `freezegun` 0.3.15 làm MỌI test chết từ registry (`as_kwarg` TypeError) — `pip install freezegun==1.1.0` theo `odoo19/requirements.txt`. ✅ **Review 4 chiều** (perf 1500-user / clean / security / spec-drift, TRỪ phần anh Thái theo chốt 23/08) → **`docs/refactor-plan.md` cụm R1–R5** (gõ "làm cụm R<n>"). Kết luận: **code KHÔNG chấp vá** — guard fail-closed trước sudo nhất quán, hot path đã batch, helper tập trung `utils.py`, 0 `csrf=False`; nợ là dọn dẹp (168 comment sử ký, 63 hex cứng) + spec-drift cần BA phân loại (50 field sheet chưa build ≈ Phase 2 Employee, `ticket_max_file_*` config vs hardcode, ADR-016). Quick-win đã làm: **xoá 42 rule/190 declaration CSS chết** khỏi `_components.css` (`2d323fe`, semantic-diff 0 đổi ngoài xoá, đo lại toàn bộ xanh, `?v=1175`). ✅ **Chapter S55 đã trả** (`chapters/69-sprint55-report-ui.tex`, PDF rebuild OK). ✅ **ĐÃ DEPLOY UAT 23/08** (`-u wujia_portal_layout`, XML-RPC xác nhận 19.0.32.2.1) + **đo lại chỉ-đọc trên chính UAT**: a11y **24/24** (UI-003 11/11 + UI-004 13/13, asset `?v=1175` ăn), hồi quy dead-CSS 6 trang × 2 viewport sạch, **lưới B4 286/286** với ID thật UAT (SO 41/batch 2/notif 19/ticket 16/return 9 — login form `anh.owner` không dùng được trên UAT, phải admin + POST authenticate) → `docs/a11y-batch-acceptance-matrix.md` §"Đo lại trên UAT". ⇒ quick-win xoá CSS chết KHÔNG ảnh hưởng issue đã fix. 🔴 Chủ dự án chốt 23/08 thêm **luật cho mọi cụm R**: refactor không được phá issue đã đóng — tra `qa-issue-ledger.yaml` + acceptance matrix trước khi đụng file, đo trước–sau bằng B4 grid + tab-walk + semantic CSS diff (đã ghi thành khối 🔴 đầu `docs/refactor-plan.md`).

**State (2026-08-23) — CHỐT SPRINT 56:** **đủ 10/10 cụm C1–C10**, 32 issue `Ready for Dev` (STT 83–114) đã về `Ready for Retest` và **ĐÃ DEPLOY UAT** (mới nhất: merge `aa26e0e`, `?v=1173`). Đã viết chapter `ch.68` + recompile PDF. Đo lại trên UAT sau deploy C8b: acceptance 58/60 (2 ô không đo được vì UAT không có hoá đơn đến hạn ⇒ `/portal/debt/pay` redirect `no_due`, đúng rule C2) + C8a 46/46 + lưới B4 286/286 + đi Tab 448/448. ⚠️ **Dữ liệu UAT: trong 14 cửa hàng chỉ cửa hàng id 3 có hoá đơn + đăng ký thi** — tester chọn sai cửa hàng sẽ thấy màn rỗng và tưởng là lỗi.
**Còn nợ / chờ:** (a) BA retest 32 issue, Dev **không** tự đóng `Done`; (b) 3 câu hỏi `CMP-SH-001` chờ BA (weight 800 vs 700 · tiêu đề trong khung có tính SectionHeader không — **đang chặn đúng 1 chỗ ở `wujia_portal_inspection`, cố ý chưa đổi** · quy tắc count ở PageHeader); (c) ~~nợ chapter S55~~ **đã trả 23/08** (`ch.69`); (d) ~~batch WJ-PORTAL-UI-003/004~~ **xong 23/08 phiên 2** (xem State phiên 2) — Issue List hiện **0 `Ready for Dev`**; việc kế = cụm R1–R5 trong `docs/refactor-plan.md`.

**State (2026-08-14):** Phiên 08-14 **không code** — đọc đủ 32 issue `Ready for Dev` (STT 83–114), đối chiếu source, chia **10 cụm C1–C10** + viết prompt sẵn cho từng session (§13 + `docs/next-session-clusters-C.md`); reconcile cả 32 ID ⇒ 0 issue đã fix sẵn. ✅ **S55 (UI Báo cáo đặt hàng, `Tasks!STT12`, commit `05269b4` + hotfix `d7a7ead` ngày 08-12) ĐÃ DEPLOY UAT** (`-u wujia_portal_report`, không migration, không `-i`) — **còn nợ chapter `.tex`**. Trang portal cuối còn Bootstrap thô nay có đủ PC + mobile theo mockup SVG BA, 31/31 đo đạt. ⇒ **Hàng đợi deploy hiện TRỐNG.**

**State (2026-08-11):** 19 module active (+ `mcp_server` bên thứ ba, **chưa cài**). S50 · S51 · S52 · S53 (B3a+B3b) · S54 (B4) **đều ĐÃ DEPLOY UAT 11/08** — đợt full 12 module, đối chiếu XML-RPC 12-08 khớp 12/12; smoke UAT chỉ-đọc 104/105 (điểm còn lại là harness bắt nhầm chuỗi trong chú thích của chính mình, đã sửa). `wj_ks_dashboard_ninja` + `wj_ks_dn_advance` **đã installed** trên UAT; `mcp_server` vẫn `uninstalled` (cần `pip install packaging` + `-i` một lần). Chi tiết từng sprint → §4 + `chapters/`.

**Câu hỏi treo với BA (từ S50–S54, chưa trả lời):** (1) `CMP-BPH-001` ghi title mobile **800** còn `CMP-PG-001` ghi **700** — Dev giữ 700, đổi sẽ phá bảng 34/34 đã Pass; (2) matrix ghi `/portal/exam` 'Title only' nhưng trang đang có nút 'Đăng ký mới' và spec cấm đổi action sẵn có — Dev giữ nút; (3) 4 `wj-count-meta` rời ở trang matrix ghi 'Title only' — Dev giữ nguyên tại chỗ; (4) mockup Home mobile không vẽ link 'Xem tất cả ›' và 2 khối 'Hỗ trợ nhanh'/'Thông tin cửa hàng' — Dev giữ; (5) ảnh QR tĩnh hay động (CT-055, treo từ S48); (6) CTA Figma `#28A9DF` chữ trắng trượt WCAG AA — đang dùng `--wujia-cta` `#0F7CA8`, cần BA confirm.

⚠️ **DB local chính `wujia_tea_19` vẫn ở `wujia_sale 19.0.4.1.0`** — pre-migrate S52 chưa từng chạy trên local (UAT đã chạy), nên `-u` module nào kéo theo `wujia_portal_return` sẽ đỏ cho tới khi chạy `-u wujia_sale` trên local.

⚠️ **UAT có cài `website_sale` + 5 module `website_sale_*`** (không module custom nào depend — cài tay). Đây là nguồn của WJ-PROD-001. **Rule: field custom PHẢI có tiền tố `wujia_`** (L10).

**Pending sống (hàng đợi):**
- 🔴 **[25/08] Lứa hiện hành = cụm D1–D6 + R1–R5** (`docs/next-session-clusters-D.md`, gõ "làm cụm D&lt;n&gt;" sau `/wujia-start`) — mỗi phiên **một cụm**, xong thì commit + push `main` để chủ dự án deploy UAT rồi BA retest. **D1 ✅ 25/08 · D2 ✅ 26/08 · D3–D6 ⬜ · R1–R5 ⬜** ⇒ **CHƯA được end sprint**.
- 🔴 **[08-15] KHÔNG end sprint cho tới khi xong ĐỦ 10 cụm C1–C10** (chủ dự án chốt 15/08). Mỗi phiên 1 cụm → commit + push `main` để chủ dự án deploy UAT → BA retest. Chỉ khi cả 10 cụm ✅ mới chạy `/wujia-end-sprint` (chapter `.tex` + PDF + tổng kết). ✅ **C1 XONG 15/08** (`b623b70` code + `7704149` docs, nhánh `dev/2026-08-15-c1` đã merge `main`): WJ-FRANCHISE-001/002 + WJ-DEBT-006 → `Ready for Retest`; franchise suy từ partner bằng **stored compute readonly=False** trên `sale.order`/`account.move`/`stock.picking` (helper `res.partner._wujia_unique_franchise()` ở `wujia_franchise`, + index `wujia_franchise_management.partner_id`), chặn ở `action_confirm`/`button_validate`/`action_post` (KHÔNG dùng constrains để không khoá chứng từ cũ), credit note kế thừa qua **2 seam** `_reverse_moves` + `_modify_default_reverse_values` (giữ `copy=False` ⇒ Duplicate không đổi). ⚠️ **Odoo KHÔNG tự backfill khi field thường thành stored compute** (đo thật: sau `-u` dữ liệu cũ y nguyên) ⇒ 3 post-migrate chỉ điền bản ghi **TRỐNG** (9 SO local), bản ghi lệch giữ nguyên. 106 test 0 failed trên 2 DB copy, đo backend 14/14 + WJ-DEBT-006 7/7, acceptance 95%/100%/100% → `docs/c1-acceptance-matrix.md`. **CHƯA DEPLOY UAT** — `-u wujia_franchise,wujia_sale,wujia_delivery,wujia_account` (**CÓ bước cập nhật dữ liệu**). ✅ **C2 XONG 15/08** (`c656b95`, nhánh `dev/2026-08-15-c2`): WJ-DEBT-001/004/007/010 → `Ready for Retest`; card tuần và dòng chứng từ dùng CHUNG rule với `_invoice_status` (+ state mới `unpaid`/`credit`), giấy báo có hết bị gắn nhãn nợ, `/portal/debt/pay` **302** về `/portal/debt?notice=no_due` khi `amount_due<=0`, memo kẹp `max(...,0)`, lịch sử thanh toán format theo `payment.currency_id` + `totals` **mỗi currency một dòng** (không quy đổi), tuần mặc định = quá hạn cũ nhất → tuần trước → tuần hiện tại bằng **1 `search_read` cho cả 6 tuần**. Payload chỉ THÊM key (`amount_due`/`credit_amount`/`payment_due_*`/`totals`/`amount_label`) ⇒ mobile+PC không vỡ. 38/38 đo Playwright 2 viewport, 113 test 3 module xanh trên DB copy `wujia_tea_c2` (port 8052) → `docs/c2-acceptance-matrix.md`. ✅ **C3 XONG 15/08** (`0398fa3`, nhánh `dev/2026-08-15-c3`): WJ-DEBT-002/003/005/008/009 → `Ready for Retest`; `get_bank_info` thêm cờ `configured` (chưa cấu hình → MỘT empty state, 0 nút copy), trang `/portal/debt/pay` tách hẳn khối mobile `d-lg-none` + khối PC gọn tái dùng `wj-pc-card`/`wj-debt-pc-bank` (component QWeb dùng chung với modal), copy hết nuốt lỗi (ô `role=status` + `setSelectionRange` cho iOS + cấm copy chuỗi rỗng), **gỡ hẳn khối QR** mobile + modal PC + CSS + 3 msgid `.po`. Gốc WJ-DEBT-009: slot `#wj-debt-hist-mbody` của `wj_ajax_list` là `<div>` thường ⇒ **nuốt `gap:12` của `.wj-debt`** (đo UAT: mọi section cách nhau 0px, đúng số BA) → trả nhịp 12/24 scope theo id. 115/115 đo ở **6 breakpoint** 360/391/430/500/768/1440, 38 test `wujia_debt` + 116 hồi quy xanh trên DB copy `wujia_tea_c3` (port 8053) → `docs/c3-acceptance-matrix.md`. ✅ **C4 XONG 15/08** (`a292547`, nhánh `dev/2026-08-15-c4`): WJ-KNW-001/002/003/004 → `Ready for Retest`; `category_id`/`tag_id` rác hoặc inactive validate bằng `exists()+active` **trước** khi vào domain (hết `MissingError` in tên model + uid ra portal), bỏ lọc rồi redirect kèm `?notice=` — **key ở controller, câu chữ ở template**; 6 chỗ `publish_date.strftime()` về `wj_dt`/`fmt_local_dt` của `wujia_portal_base` (hết lệch −7h); search mở sang `summary` + field stored MỚI `wujia_content_text` (`html_to_inner_content`) nên keyword bị `<strong>` cắt ngang vẫn ra, không dính rác markup — **field mới tự compute lúc `-u`, 25/25 bài, KHÔNG migration**; detail + route đính kèm dùng chung `_visible_domain()` (thêm nhánh `publish_date <= now`, chủ dự án chốt) + thông báo thân thiện thay redirect trần. ⚠️ `html2plaintext` KHÔNG dùng được: nó chèn `*…*` cho `<strong>` ⇒ vẫn hỏng đúng ca cần fix — phải là `html_to_inner_content`. 60/60 đo Playwright 391×844 + 1920×1080, 14 test mới + 87 test hồi quy 3 module khác xanh trên DB copy `wujia_tea_c4` (port 8054) → `docs/c4-acceptance-matrix.md`. ✅ **C1 + C3 + C4 ĐÃ DEPLOY UAT 15/08** (đối chiếu XML-RPC 6/6 module khớp đĩa). Smoke UAT chỉ-đọc C4 **42/42 đạt** ở 2 viewport: giờ portal đúng (bài `ui12-01` lưu 27/05 17:36 UTC in ra 28/05/2026 00:36), filter rác ra thông báo không lộ model/uid, slug bài BA để nháp (`uat-1208-knowledge`) ra đúng câu thông báo, search tóm tắt + nội dung ra bài, 26/26 bài có bản text. C1 kiểm read-only: `sale.order` 39/39 và `stock.picking` 14/14 có cửa hàng; `account.move` còn 3 bản ghi trống nhưng đều nằm ngoài phạm vi (2 bút toán ngân hàng `entry` + 1 hoá đơn nháp chưa có khách). ✅ **C5 XONG 15/08** (`94756ff` + follow-up `6ac7807`, merge `c77e335`): WJ-DELIVERY-005/006/007 + WJ-HOME-003 → `Ready for Retest`; Home và `/portal/delivery` nay dùng **cùng bộ helper** ở `wujia_portal_base/controllers/utils.py` (`batch_franchise_domain` / `own_pickings` / `departure_value` / `count_undelivered_orders`) thay vì 4 bản chép tay. `departure_value` = chỗ DUY NHẤT quyết định in giờ nào (`actual_departure or planned_departure`) + nhãn đổi theo dữ liệu "Xuất phát (thực tế)/(dự kiến)" vì đổi mỗi con số thì BA vẫn không phân biệt được. Chip đếm chạy trên domain **base + keyword** (bỏ `bs` để chọn chip không làm 0 các chip khác), vẫn 1 `_read_group`. Home loại `done`/`cancelled` NHƯNG **giữ chuyến `loading`/`delivering` dù lịch đã qua** — chỉ lọc "lịch từ hôm nay" thì chuyến đang chạy dở biến mất, trái WJ-DELIVERY-005. Đơn "chưa giao" đếm theo **state của picking** (`draft/waiting/confirmed/assigned`), tổng toàn cửa hàng bằng 1 `_read_group` group-by `sale_id`. 20/20 đo Playwright 391×844 + 1920×1080, 9 test mới (`--test-tags wujia_delivery_c5`, `tests/` đầu tiên của module) + 75 test hồi quy 4 module xanh trên DB copy `wujia_tea_c5` (port 8055) → `docs/c5-acceptance-matrix.md`. ⚠️ Bẫy khi viết test: `stock.picking.batch.name` do **ir.sequence** cấp, tên tự đặt lúc create bị ghi đè ⇒ assert phải đọc lại `.name`; và Home in mã SO ở 2 block nên `assertNotIn` phải cắt đúng block "Giao hàng sắp tới". **ĐÃ DEPLOY UAT 15/08** (đo lại 2 viewport: `BATCH/OUT/00002` ra đúng **13:47 (thực tế)** thay 15:30 dự kiến — chính con số BA than; search `S00035` badge Tất cả 1). Lộ ra ca UAT bản đầu chưa phủ: chuyến chưa hoàn thành mà **chưa đặt ngày dự kiến** bị loại khỏi Home ⇒ đếm "1 đơn chưa giao" nhưng khối trống. Sửa ở `6ac7807` (cho vào, xếp cuối vì PG để NULL cuối ở ASC) + 2 test, **đã deploy UAT** và đo lại đạt (trang chủ ra `BATCH/OUT/00001` + `S00031`). ⇒ **C9 XONG 15/08** (`9a8784b`, đã deploy UAT). ✅ **C10 XONG 18/08** (`1fa9a6b` code, merge `f99321b`, nhánh `dev/2026-08-18-c10`): WJ-EXAM-007 + WJ-LANG-001 → `Ready for Retest`. Cùng một bệnh: **số/danh sách chép cứng vào template + JS thay vì đọc cấu hình**. EXAM: thêm `_max_per_reg()` + `_max_hint()` ở `wujia_portal_exam/controllers/portal.py` làm **nguồn duy nhất** (ca thi có cấu hình riêng thì thắng, để trống mới lấy của khoá), gỡ hết 5 fallback `|| 4` trong JS + `or 4` ở controller; câu hướng dẫn nay `t-out pc_summary['max_hint']` và JS viết lại chính nó khi đổi khoá/ca ⇒ hướng dẫn, chip quota, thông báo chặn, validate server luôn cùng một số. LANG: thêm seam `ir.http._wj_portal_langs()` + `_wj_html_lang()` (đúng pattern `_wj_back_url()` của B4) đọc `res.lang._get_active_by('code')` — **đã ormcache ⇒ 0 query/trang**; 3 chỗ hiển thị (navbar PC, header mobile, màn đăng nhập) đổi sang `t-foreach`, pill màn login thành dropdown thật, route `/portal/set-lang` hạ xuống `auth='public'`. ⚠️ `Session.authenticate()` **ghi đè `context['lang']` bằng `res.users.lang`** (`odoo/http.py:1233`) ⇒ chọn ngôn ngữ ở màn login sẽ mất; xử bằng cách nhớ vào session (`wj_pre_login_lang`) rồi `auth.py` áp vào `res.users.lang` ngay sau khi đăng nhập. **Tính mở rộng đã chứng minh bằng đo thật**: bật `zh_TW` trên DB copy → mục thứ 4 `繁體中文 (台灣)` + cờ `flag-icon-tw` tự hiện, **0 dòng code** (đã tắt lại). 33/33 đo trên DB copy `wujia_tea_c10` (port 8056), 20 test mới (`wujia_exam_c10`, `wujia_lang_c10`) + 82 hồi quy xanh → `docs/c10-acceptance-matrix.md`. **ĐÃ DEPLOY UAT 18/08** (exam 19.0.5.6.0 + layout 19.0.31.18.0), đo lại chỉ-đọc trên UAT **30/30 đạt** (3 ngôn ngữ ở cả 3 chỗ, chọn Thái ở màn login giữ được sau khi đăng nhập và qua nhiều trang, khoá BA test ra đúng `0 / 2`, hồi quy 6/6); riêng nhánh "ca thi thắng khoá" **chưa đo được trên UAT** vì ca duy nhất hết hạn 16/08 — đã ghi LIMIT nhờ BA mở thêm ca. ✅ **C6 XONG 19/08** (`c2592b3`, nhánh `dev/2026-08-19-c6`): WJ-PORTAL-UI-001 → `Ready for Retest`. **Blast radius toàn portal nhưng gốc rễ chỉ MỘT rule**: `a:hover{text-decoration:underline}` của `web.assets_frontend.min.css` (BS5 Odoo 19), specificity (0,1,1), **nạp SAU mọi CSS custom** ⇒ ~45 dòng `text-decoration:none` rải 12 file ở (0,1,0) đều thua, 4 dòng phải `!important` mới sống. Focus ring bị `bootstrap-extended.css:4 a:focus{outline:none}` + ~20 rule `outline:0` xoá sạch. Xử bằng **một layer cắt ngang** `_interaction.css` nạp CUỐI `asset_style_css` (4 khối: underline / focus-visible / hover bề mặt trung tính / pressed), thắng bằng **specificity** chứ không `!important`; hover chỉ đổi `background-color` + `border-color` + `box-shadow` (cấm `border-width`/`padding`/`transform`) nên **0 dịch layout**; bọc `@media (hover:hover) and (pointer:fine)` chống sticky hover. 3 ngữ cảnh prose (`.article-content`, `.wujia-mknow-body`, `.wujia-mticket-bubble`) + `.wj-link`/`.wj-pc-link` giữ underline. Token mới chỉ 4, màu **tái dùng** `--wujia-primary-soft`/`--wujia-primary` sẵn có. 🔴 **L14 — local KHÔNG tái hiện được lỗi**: cùng `.wujia-mknow-row` hover ra `underline` trên UAT nhưng `none` trên DB copy, vì **UAT có `website_sale` + 5 `website_sale_*`** ⇒ bundle frontend khác (L10 lặp lại). Phải chứng minh bằng `page.add_style_tag` nhúng `_variables.css` + `_interaction.css` vào **chính UAT** rồi đo lại. ⚠️ Nhúng thiếu `_variables.css` thì `var(--wujia-focus-ring)` vô định ⇒ `outline` invalid, **xoá luôn ring đang có** (46→51 tệ hơn). ⚠️ 6 trang lệch 4–12px chiều cao là **artifact của việc nhúng lặp `_variables.css`**, chứng minh bằng run đối chứng chỉ nhúng token (số y hệt). Kết quả UAT: underline **18→0** @1920 / **34→0** @391, thiếu ring **46→0** / **43→0** (đồng loạt `2px solid rgb(15,124,168)` offset 2px), 0 đổi kích thước, tím Vuexy 1→0. Đo đối chứng local (chặn/bỏ chặn `_interaction.css` cùng phiên) 13/13 @1920 + 12/12 @391, 0/22 route đổi chiều cao. **Đi Tab THẬT 5 trang × 25 stop: 7/124 → 124/124** có ring, thứ tự stop y nguyên. Hồi quy B4 **282/286 trước = sau** (4 ô đỏ sẵn có: `/portal/support/40` admin không thấy màn chi tiết). 148 test xanh trên DB copy `wujia_tea_c6` (port 8057). Ring dùng `--wujia-cta #0F7CA8` **thay** `primary_dark #168FC2` BA viết (4.71:1 vs 3.32:1 + đồng bộ ring CTA sẵn có) — **đã ghi LIMIT**; nhóm nút primary/CTA + chip đang chọn + action trên header tối **cố ý giữ** hover đã thiết kế (chủ dự án chốt 19/08 "chỉ bề mặt trung tính"). 8/8 acceptance → `docs/c6-acceptance-matrix.md`. **CHƯA DEPLOY UAT** — `-u wujia_portal_layout,wujia_portal_sale`, **không** cập nhật dữ liệu, **không** module mới; `?v=1170` đã bump nên còn thấy giao diện cũ là cache trình duyệt/proxy. ⚠️ `-u` chạm `wujia_portal_return` thì **bắt buộc kèm `wujia_sale`** (rename `description_ecommerce`, §5) — thiếu là build RC=255 tại `backend_product_views.xml:5`. ✅ **C7 XONG 19/08** (`b33b46f`, nhánh `dev/2026-08-19-c7`): WJ-HOME-001/002/006/007/008 → `Ready for Retest`. **5 issue một gốc rễ**: Home mobile dựng dồn qua nhiều sprint nên mỗi block một container, một dấu hiệu bấm, một limit riêng. Gộp về **1 hằng `HOME_PREVIEW_LIMIT=2`** dùng cho cả 5 nguồn (đơn/thông báo/đổi trả/bài viết/chuyến giao — chủ dự án chốt **hạ cả desktop**, không tách đôi nguồn) + **1 bộ component** `.wujia-mdash-card/-list/-row`: Giao hàng và Đổi trả thôi mỗi bản ghi một card, Thông báo bỏ kiểu dải màu riêng (chủ dự án chốt), còn 6 card thay 8. 11 chevron cấp dòng → **0** (dòng vốn đã là `<a>` bọc trọn ⇒ hover/pressed/focus thừa hưởng thẳng `_interaction.css` của C6, **0 dòng CSS trạng thái mới**; modifier `.is-stacked` đặt trên chính `.wujia-mdash-row` nên không phải thêm tên vào 3 danh sách). Nhãn KPI bỏ hẳn `text-overflow:ellipsis`, hạ letter-spacing + padding ở `@media ≤380px` ⇒ 4 KPI vẫn **1 hàng, mỗi nhãn 1 dòng ở 360px** (thiếu đúng **2px** nên KHÔNG cần lưới 2×2 — đúng thứ tự ưu tiên BA). Mã đơn chiếm trọn dòng 1 (`.is-code`), ngày + tiền xuống dòng phụ; mã chuyến `BATCH/...` cũng hết cắt; `date_order` chuyển sang `wj_dt` (hết lệch −7h). Chiều cao Home **2752→2634** @360 cùng dữ liệu. ⚠️ **Đo trước/sau đúng cách**: bản "trước" lấy bằng cách chạy DB copy C6 (view cũ nằm trong DB) trên cùng đĩa code mới; và nhãn KPI **local không cắt mà UAT cắt** (L10/L14 lặp lại) ⇒ phải nhúng CSS vào chính UAT mới chứng minh được **1→0**. 27/27 acceptance, 5 test mới (`wujia_home_c7`) + 43 hồi quy xanh trên DB copy `wujia_tea_c7` (port 8058), lưới B4 **282/286 y hệt C6**, đi Tab **124/124** → `docs/c7-acceptance-matrix.md`. **CHƯA DEPLOY UAT** — `-u wujia_portal_base,wujia_portal_layout`, không cập nhật dữ liệu, không module mới; `?v=1171` đã bump. ✅ **C8a XONG 22/08** (nhánh `dev/2026-08-22-c8`): UI-SECTIONHEADER-001 — **VẪN GIỮ `Ready for Dev`** vì mới migrate **13/18 call site**; ledger đã soạn sẵn entry ở dạng comment, C8b bỏ `#` là `qa_sync` được. **Kiểm kê trước, code sau**: 183 heading thô, phân loại theo **TỔ TIÊN DOM** chứ không theo tên class ⇒ 89 CardHeader + 3 PageHeader + 24 Khác nằm ngoài scope, còn đúng **18 call site** SectionHeader (`docs/c8-heading-inventory.md`). Component `wj_section_header` ép rule "tối đa MỘT right slot" bằng ưu tiên `action > control > meta`; meta render **markup thô không bọc element** để `wj_ajax_list` swap được (bài học B3a). ⚠️ **Odoo 19 QWeb KHÔNG có directive đặt tên thẻ động** — `t-tag-open`/`t-tag-close` là directive kỹ thuật sinh từ thẻ TĨNH (`ir_qweb.py:1705`) ⇒ heading phải rẽ 3 nhánh `t-if/t-elif/t-else`, không có cách nào khác. ⚠️ **`.wj-section-header:first-child{margin-top:0}` là bẫy**: mỗi header là con đầu của `<section>` riêng nên selector đó khớp TẤT CẢ, giết luôn nhịp 16/8 — phải làm modifier opt-in `--flush`. 🔴 **2 spec BA mâu thuẫn trực tiếp**: `CMP-SH-001` ghi weight **800** nhưng `UI-06` (S35) ép `.content-wrapper h1..h6{font-weight:700!important}` ở `_wujia_theme.css:35` (specificity (0,1,1)+important, tìm ra bằng CDP `CSS.getMatchedStylesForNode`) ⇒ Dev giữ **700** theo tiền lệ, đã ghi LIMIT chờ BA. 46/46 acceptance @391/360/1920, 15 test mới (`wujia_section_header_c8`) + 63 hồi quy xanh trên DB copy `wujia_tea_c8` (port 8059), lưới B4 **286/286**, đi Tab **248/248**. ⚠️ **Bẫy harness a11y**: `input[type=date]` đưa focus vào field con trong shadow root nên host **không match `:focus`** ⇒ computed của host ra `outline:none` (240/248 giả). Ép `:focus-visible` bằng CDP `forcePseudoState` thì ra đúng `2px solid rgb(15,124,168)` — sửa harness, không sửa code. → `docs/c8-acceptance-matrix.md`. **CHƯA DEPLOY UAT** — `-u wujia_portal_layout,wujia_portal_base,wujia_portal_delivery,wujia_portal_sale,wujia_portal_purchase_history`, không cập nhật dữ liệu, không module mới, `?v=1172` đã bump. 🔴 **Ngoài scope nhưng CHẶN BUILD, đã sửa trong nhánh này**: `wujia_franchise_inspection_question.py` seed trong `init()` ⇒ `UndefinedTable`, RC=255 trên MỌI build — **bug #2 của L15 tái diễn** (review merge `thai` sửa 3 chỗ, sót chỗ thứ 4 do `f09082a`); có sẵn trên `main` và **đang chặn hàng đợi deploy merge `thai` (`538f75e`)**. ✅ **C8b XONG 23/08** (nhánh `dev/2026-08-23-c8b`): UI-SECTIONHEADER-001 → **`Ready for Retest`**, bullet #2 từ 13/18 lên **19/19 (100%)** ⇒ **đủ 10/10 cụm C1–C10**. 6 call site: debt 209 (right slot có điều kiện — `action` khi còn hoá đơn ẩn, không thì `meta` count, component tự ép "tối đa MỘT"), debt 512, debt 688, exam **759** + 1036, return 189. ⚠️ **Kiểm kê C8a SÓT 1 chỗ**: `portal_exam.xml:759` cùng class + cùng chữ "Danh sách nhân sự" với :1036 nên script kiểm kê **gộp hai dòng trùng làm một**; tổ tiên chỉ có `.wujia-mexam-panel` (flex column không nền) ⇒ đúng là SectionHeader ⇒ tổng thật là **19**, không phải 18. Bỏ nó lại thì hai dòng chữ y hệt render hai cỡ (16 vs 20) — đúng loại lỗi issue đang dẹp. 🔑 **Ca dung hoà debt:688** "THÔNG TIN CHUYỂN KHOẢN": `.wj-debt-bank` có nền + bo góc + padding + **`height:150px` cố định** ⇒ theo **chính phép thử tổ tiên DOM** đã hạ `portal_home.xml:292` xuống CardHeader thì đây cũng là CardHeader, lại còn là nhãn 11px viết hoa gõ tay. Chủ dự án chốt: **vẫn chuyển sang component** (heading thật, một khuôn) nhưng tắt nhịp bằng modifier flush + **giữ dáng nhãn bằng CSS scope trong `portal_debt.css`**, KHÔNG đẻ modifier ở `_components.css` dùng chung ⇒ thẻ vẫn cao đúng 150px, số đo S43/C3 bất biến; BA chốt sau chỉ gỡ 1 khối CSS. 🔴 **Nhịp debt đổi CÓ CHỦ Ý**: C3/WJ-DEBT-009 từng chốt riêng 24/12 cho `#wj-debt-hist-mbody`, nay bỏ rule riêng để dùng đúng 16/8 của `CMP-SH-001` y hệt 13 chỗ C8a ⇒ thị giác thành 28/20 (margin + gap container 12, **cùng cách tính toàn portal**). Chủ dự án chốt 23/08 "đồng bộ thì theo BA" — đổi con số BA đã duyệt ở C3, đã ghi rõ lý do trong ledger. Xoá hẳn `.wujia-mhist-listhead*` + `.wj-debt-section*` + `.wujia-mexam-sectitle` (ràng buộc thứ tự đã thoả) + **unit test `test_retired_heading_classes_are_gone`** quét `arch_db` mọi view qweb chống tái phát. `wujia_portal_inspection` (nay installed UAT ⇒ vào phạm vi) kiểm kê xong: 3 PageHeader + 12 CardHeader + **1 chỗ lửng lơ** (`list:34` "Danh sách phiếu khảo sát", cấu trúc giống hệt `/portal/delivery` mà BA tự promote) ⇒ **để lại, ghi `Need Clarification`** vì rơi đúng LIMIT-2 đang chờ BA; `wujia_portal_remediation` **cố ý bỏ ngoài** (code đã xoá `f789a56`, UAT `uninstalled`). 62/62 acceptance C8b + **46/46 C8a chạy lại** (không hồi quy dù đã xoá CSS dùng chung), 64 test 4 tag 0 failed, lưới B4 **286/286**, Tab **447/447** (9 trang × 2 viewport) trên DB copy `wujia_tea_c8b` (port 8061, nền `wujia_tea_mt2` = DB đã cài sẵn + merge thai, gần UAT nhất) → `docs/c8-acceptance-matrix.md` §6. ⚠️ **2 lần harness sai, không phải code sai (L7/L9)**: (1) 50/55 lượt đầu — DB copy chỉ có **1 hoá đơn nháp franchise NULL** nên trang công nợ ra empty state, chạy `seed_debt_demo.py` ⇒ 62/62; (2) B4 282/286 — ticket 40 **thuộc cửa hàng khác** nên admin bị đẩy về danh sách (đúng thiết kế), đổi sang ticket đúng cửa hàng ⇒ 286/286 — **đây chính là "4 ô đỏ sẵn có" C6/C7 từng ghi, nay đã truy ra gốc, KHÔNG phải lỗi phân quyền**. ⚠️ **Lỗi tự gây**: comment XML chứa `--flush` ⇒ `XMLSyntaxError: Comment must not contain '--'`, RC=255 — **XML cấm hai gạch nối trong comment**, đừng dán tên class BEM vào comment. **CHƯA DEPLOY UAT** — `-u wujia_portal_layout,wujia_portal_debt,wujia_portal_exam,wujia_portal_return,wujia_sale,wujia_portal_inspection` (chạm return thì **bắt buộc kèm `wujia_sale`**), không cập nhật dữ liệu, không module mới, `?v=1173` đã bump.
- 🔴 **[08-14] Issue List = ưu tiên 1 mọi phiên (`Tasks!STT1`)** — **32 issue `Ready for Dev`** (STT 83–114), BA audit UAT 11–13/08 đổ thêm ~30 issue. Batch B0–B4 ✅ đóng hết 08-10/08-11 (ch.64–67). Lứa mới chia **10 cụm C1–C10**, thứ tự chạy **C1 → C2 → C3 → C4 → C5 → C9 → C10 → C6 → C7 → C8**; ✅ **ĐỦ 10/10 CỤM sau C8b (23/08)** ⇒ **32/32 issue lứa này đã đóng** (`Ready for Retest`), mới được chạy `/wujia-end-sprint`. Batch kế = **WJ-PORTAL-UI-003/004** (STT 116/117, BA audit 20/08, a11y focus — chủ dự án chốt 22/08 để sau C8). Bảng cụm + điểm đã soi trong source → **§13**; **prompt sẵn từng session + ô tiến độ → `docs/next-session-clusters-C.md`** (gõ "làm cụm C&lt;n&gt;" sau `/wujia-start`). **Đã reconcile cả 32 ID** (`git log --all -S` + `grep custom/`) ⇒ **0 issue đã fix sẵn**, không cái nào chỉ cần `qa_sync`. Chờ BA viết spec cho 20/23 component còn lại (`CMP-SH-001` nay đã `BA Confirmed`).
- 🚚 **[19/08] HÀNG ĐỢI DEPLOY: merge nhánh `thai`** (**đã merge + push `main` = `538f75e`** 19/08) — tính năng **giám sát/chấm điểm cửa hàng** (8 model trong `wujia_franchise` + 2 module portal MỚI `wujia_portal_inspection`, `wujia_portal_remediation`) + **bộ toolchain dịch `.po`**. Lệnh: `-u wujia_core,wujia_franchise,wujia_sale` rồi `-i wujia_portal_inspection,wujia_portal_remediation` (**module mới bắt buộc `-i`**), bump `?v=`. Merge 0 conflict; **sửa hộ anh Thái 8 điểm** (chi tiết + lý do → `docs/merge-thai-review.md`, đã viết để gửi lại): 🔴 ACL `group_id` **để trống** + `1,1,1,1` ⇒ 1500 portal user write/unlink được cửa hàng (khôi phục 6 dòng theo group, giữ nguyên 22 dòng inspection của anh Thái); 🔴 seed trong `init()` ⇒ `-i` **RC=255** `UndefinedTable ..._template_line` (init() chạy lúc đang dựng bảng — gỡ cả 3, XML `<function>` đã gọi đúng và chạy sau khi có bảng); 🔴 `noupdate="1"` ⇒ seed **không bao giờ chạy** trên DB đã cài sẵn như UAT (đo: 0/0/0/0 → **1 mẫu MM01 + 77 tiêu chí + 4 hạng + 4 danh mục**); 🔴 `vi_VN.po` bị script đạp thành msgid (`Store code`→`Store code` thay vì `Mã cửa hàng`) **và** hỏng cú pháp (`msgfmt -c` hàng chục `end-of-line within string`, do regex cắt tại `\"` + chèn xuống dòng thật) ⇒ khôi phục từ `main`, `zh_CN.po` của anh Thái parse sạch nên **giữ**; IDOR fail-open `if franchise_ids and ...` (rỗng ⇒ **ngắt mạch, bỏ qua kiểm tra** — đúng ca user không thuộc cửa hàng nào) ⇒ fail-closed, đo thật cũ **LỌT**/mới **CHẶN**, danh sách 1→0 phiếu; `csrf=False` trên POST upload (template đã có sẵn `csrf_token`) ⇒ `csrf=True`; `_order` của `wujia.franchise.management` đổi toàn cục ⇒ trả `'code, name'` (list view giám sát **đã có** `default_order`); 6 `scripts/*.sh` trỏ `/home/dev/...` máy anh Thái + mất bit `+x` ⇒ revert; bỏ 8 file rác (có `clear_data.py` + `reset-db.sh`). **Toolchain dịch tổng quát hoá** → `scripts/sync_translations.py` (`--modules/--langs/--db/--glossary/--dry-run/--import`, babel thay regex, **giữ msgstr cũ** thay vì lấy msgid, ghi `.pot` cùng lượt (L8/3), cổng `msgfmt -c` trước khi nạp, tự `_activate_lang`); glossary về `docs/i18n-glossary.csv` + cột `TH`. 🔑 **Cầu nối vi_VN**: glossary đánh khoá tiếng Việt nhưng msgid từ S44 là tiếng Anh ⇒ tra 3 nấc `glossary[msgid]` → `glossary[vi_VN(msgid)]` → msgstr cũ (zh_CN 174→183). **th_TH đã dịch xong pilot** `wujia_franchise` (250 dòng glossary + 183/487 chuỗi, MÁY DỊCH — cần BA rà). Đo: 171 test 0 failed, RC=0, ACL AccessError, nhãn 4 ngôn ngữ 4/4, `msgfmt` sạch toàn repo. **Chưa sửa, chờ anh Thái xác nhận**: `confirmed_member_id required=True` (không tạo được phiếu nếu chưa chỉ định người ký, ngược luồng draft→done) · `controllers/main.py` dịch bằng CSV runtime song song i18n Odoo.
- 🚚 **[22/08] HÀNG ĐỢI DEPLOY — merge nhánh `thai` ĐỢT 2** (`b846c6c` trên `main`, nhánh `dev/2026-08-22-merge-thai-2`). `origin/thai` `ffa927b`→`d9a39a6`, **11 commit**, gồm cả tính năng ngoài dịch thuật: 2 model mới `wujia.franchise.inspection.attendance.line` (điểm danh NV đang làm / đã thi đậu) + `wujia.franchise.document`, `inspection.py` +525 dòng, `wujia_inspection_chart.js` +272 dòng. Lệnh deploy: **`-u wujia_sale,wujia_franchise,wujia_portal_inspection`** — 3 module, **không** cần `-i`, bump `?v=`. ⚠️ **KHÔNG đưa `wujia_portal_remediation` vào lệnh**: anh Thái đã tự xoá code module đó (`f789a56`), phiên 20/08 đã gỡ khỏi DB bằng script, UAT hiện `uninstalled` — **đúng trạng thái mong muốn**; `-i` sẽ lỗi vì không còn code. Đối chiếu UAT 22/08 trước deploy: `wujia_core` 19.0.1.0.0 · `wujia_sale` 19.0.4.3.0 · `wujia_franchise` 19.0.3.1.0 · `wujia_portal_inspection` 19.0.1.0.0 — cả 4 installed, khớp manifest trên đĩa. Chủ dự án chốt 22/08: **merge cả gói, KHÔNG sửa hộ, không viết doc gửi lại** — chỉ sửa cái ảnh hưởng mình.
  🔑 **Cái bẫy đã chặn**: `controllers/main.py` của anh Thái tra bảng dịch bằng khoá `label:model:field` trong **`docs/i18n-glossary.csv` lúc chạy**, mà `be814da` (20/08) đã dựng lại file đó theo khoá tiếng Anh (**431 dòng, 0 khoá `label:`**). File này **KHÔNG nằm trong 30 file conflict** ⇒ git lặng lẽ lấy bản của `main`, không hỏi ⇒ `_t()` tra trượt 100%, trang khảo sát hiện nguyên mã khoá. Xử: **gộp glossary** 431 + 249 = **680 dòng**, 129 khoá `label:` nguyên vẹn, 0 khoá trùng (hai bộ khoá khác hệ hoàn toàn). ⚠️ **Đây là loại lỗi merge sạch nhưng chạy sai** — khi hai nhánh cùng sửa một cơ chế, phải soi cả file KHÔNG conflict.
  ✅ **90 msgid trùng trong `.po` = báo động giả, KHÔNG phải lỗi.** `msgfmt -c` đỏ cả 4 file (`vi/zh/th/.pot`), 17–31 cặp có msgstr khác nhau (`Name` → 'Tên phiếu khảo sát' / 'Tên cửa hàng' / 'Tiêu đề'). Nhưng **Odoo nạp `.po` định vị bằng dòng tham chiếu `#:`, không theo gettext thuần** ⇒ đo thật sau `-u`: cả 3 model nhận **đúng 3 bản dịch riêng**. ⇒ Không đụng `.po` của anh Thái. Bài học: `msgfmt -c` là cổng cú pháp, **không phải** trọng tài ngữ nghĩa của Odoo.
  Bản dịch anh Thái **đầy đủ hơn main rõ rệt**: 533 msgid, `vi/zh/th` mỗi bản chỉ sót 1 msgstr trống (main: 506, trống 102/310/312) ⇒ lấy bản anh Thái. Anh ấy cũng **tự chuyển msgid sang tiếng Anh**, cùng hướng với main.
  ✅ Anh Thái **đã tiếp thu** review đợt 1: 6 dòng ACL mới đều có `group_id`, `_order='code, name'` giữ nguyên, 0 đường dẫn `/home/dev`, không `csrf=False` mới. C1 nguyên vẹn (`_wujia_unique_franchise`, `_wujia_assert_document_franchise`, `index=True` trên `partner_id`).
  🟡 **CỐ Ý KHÔNG SỬA** (chủ dự án chốt "kệ nó"): `init()` seed tái phát ở `wujia_franchise_inspection_question.py:108` — trùng `<function>` trong `data/wujia_inspection_bootstrap.xml:10`, nhưng chỉ tạo bản ghi model của chính nó nên **không vỡ build** (khác ca L15/A2 cũ) · `res_partner.py` có `_()` dịch dở (tiêu đề EN, thân VN) · trang khảo sát vẫn dùng **CSV runtime** thay `.po` ⇒ **`docs/i18n-glossary.csv` từ nay là phụ thuộc LÚC CHẠY**, sửa nó trong phiên i18n sẽ đổi chữ trên server.
  **Nghiệm thu trên DB copy `wujia_tea_mt2` (cổng 8072, nền `wujia_tea_merge_thai` = DB đã cài sẵn, đúng kịch bản UAT):** `-u` 4 module **RC=0, 0 lỗi Odoo** (5 dòng log là docutils parse mô tả manifest) · seed trên DB đã cài: `question` 0→**15**, template/line/grade/category giữ 1/77/4/4, **`-u` lần 2 y hệt ⇒ idempotent** · trang khảo sát 3 ngôn ngữ **0 chuỗi `label:` lộ ra** (129 lần xuất hiện đều nằm trong `<script>` = bảng tra `trans_json`, đúng thiết kế) · **9/9** chuỗi mẫu ra đúng chữ theo glossary ở vi/zh/th · ACL portal user `em.hcm`: read OK, **write/create/unlink đều CHẶN** · smoke portal **13/14 route trả 200**; cái 500 là `/portal/reports/orders` do `tz='Asia/Saigon'` — **lỗi cũ đã ghi ở §5, không phải do merge**.
  ✅ **ĐÃ DEPLOY UAT 22/08** (chủ dự án chạy `-u all`) + **đo lại chỉ-đọc ngay trên UAT**: 2 model mới có mặt · seed chạy trên DB đã cài sẵn (**câu hỏi thi 0→15**, mẫu 1 / tiêu chí 77 / hạng 4 / danh mục 4) · **msgid trùng ra đúng 3 nhãn riêng ở CẢ vi_VN, th_TH, en_US** (xác nhận trên server thật, không chỉ DB copy) · menu giám sát đúng tiếng Việt + Thái · portal **12/12 route 200**, 0 tràn ngang, 0 pageerror · trang khảo sát `/franchise/inspection/do/1` **0 mã `label:` lộ**, **21/21 chuỗi đúng** ở vi/th/zh. Phiếu + lịch thử đã **xoá sạch** (0 bản ghi còn lại). ⚠️ `zh_CN` **chưa bật** trên UAT (chỉ `en_US`/`th_TH`/`vi_VN`) — file `.po` đã có sẵn trong repo, bật lang là chạy.
  ✅ **PHÁT HIỆN + ĐÃ SỬA 22/08 — lỗi L11/(1) trên trang khảo sát** (`49a1d2b`, `wujia_franchise` 19.0.3.1.0→**19.0.3.1.1**, **ĐÃ DEPLOY + ĐÃ NGHIỆM THU TRÊN UAT 22/08**). Gốc: `inspection_survey_web_templates.xml:26` viết `window.SURVEY_TRANS = <t t-out="tj"/>;` **bên trong `<script>` inline**. `t-out` escape JSON (`"`→`&#34;`, `>`→`&gt;`), mà **HTML5 không giải mã thực thể bên trong `<script>`** ⇒ **cả khối JS chết** (`2 pageerror: Unexpected token '&'` đo thật trên UAT) ⇒ `window.SURVEY_TRANS` không bao giờ được gán, **22 chỗ gọi `_t()` rơi hết về mặc định** ⇒ chữ do JS sinh không dịch được. ⚠️ **Lỗi này KHÔNG lộ khi nhìn trang** — chữ server render vẫn đúng (21/21 xanh), chỉ bắt được bằng `pageerror`.
  ⚠️ **`<script type="application/json">` KHÔNG cứu được** — mọi phần tử `<script>` đều là raw-text, trình duyệt không giải mã thực thể bất kể `type`. Chỗ **được** giải mã là **thuộc tính** ⇒ chuyển bảng dịch sang `<div id="surveyTransData" t-att-data-trans="tj"/>` + `JSON.parse(el.dataset.trans)`, nạp một lần ở `DOMContentLoaded` (còn giữ `window.SURVEY_TRANS` cho tương thích). Không cần đụng controller, không cần `markupsafe.Markup`. Đo trên `wujia_tea_mt2`: **pageerror 2→0**, `SURVEY_TRANS` **677/458/457 khoá** (vi/th/zh), **15/15 khoá JS trả đúng chữ glossary**, server render vẫn **21/21**, hồi quy portal 5/5.
  ✅ **Đo lại trên chính UAT sau deploy (22/08, bản `19.0.3.1.1` xác nhận qua XML-RPC)** — đúng bài học L14 (UAT có `website_sale` nên bundle frontend khác local, chứng minh ở local KHÔNG thay được chứng minh ở server): `/franchise/inspection/do/2` ở **vi_VN / th_TH / zh_CN** đều **HTTP 200, pageerror = 0**, `window.SURVEY_TRANS` = **677 / 458 / 457 khoá**, **0 mã `label:` lộ**, **0px tràn ngang**, **15/15 khoá JS** trả đúng chữ glossary, **21/21 chuỗi server render** đúng. Phiếu + lịch thử `[PROBE2]` (id=2) **đã xoá sạch, 0 bản ghi còn lại** — lưu ý tìm theo **id**, không tìm theo `name` vì `name` của lịch bị compute lại thành mã `LGS-…`.
  ⚠️ **2 bẫy harness đã trả giá khi đo UAT** (ghi để khỏi lặp): (1) điền form login rồi `goto` ngay ⇒ chưa kịp có session, trang **redirect về `/web/login`** mà vẫn trả **HTTP 200** ⇒ 15/15 assert "fail" đều là **đo rỗng**; phải `POST /web/session/authenticate` + kiểm cookie trước khi đo (L13/3). (2) so chuỗi **phân biệt hoa/thường** trong khi trang có `text-transform:uppercase` ⇒ `Tổng điểm` vs `TỔNG ĐIỂM` báo sai; và một "pass" của th_TH là **trùng chuỗi ngẫu nhiên** với tiêu đề cột — harness sai thì sửa harness (L7/L9).
  ⏳ **Chưa làm**: BA rà bản dịch máy zh/th (audit đã bắt lỗi thật `Nháp`→`ตี` = *đánh*).
- 🚚 **Hàng đợi deploy (cũ): TRỐNG** — C7 (`b33b46f`) **đã deploy UAT 19/08** (`wujia_portal_base` 19.0.7.4.0 + `wujia_portal_layout` 19.0.31.20.0, xác nhận qua XML-RPC) và **đã đo lại chỉ-đọc ngay trên UAT**: chevron cấp dòng 11→0, card 8→6, nhãn KPI bị cắt @360 1→0, mã đơn bị cắt @360 3→0, mọi block preview ≤2 dòng, 5 "Xem tất cả" còn nguyên, tràn ngang 0 ở 360/391/430/500, `pageerror` rỗng. Hồi quy trên UAT: lưới B4 **286/286** (dùng ID thật của UAT), Tab walk C6 **124/124**, PC 1366/1920 sạch. Chi tiết: `docs/c7-acceptance-matrix.md` §"Đo lại trên UAT sau deploy".
- 🌐 **[19/08] i18n — mới xong 1/22 module** (`wujia_franchise`: vi 160 / zh 183 / th 183). 10 module **chưa có `i18n/` nào**, trong đó có cả khung portal `wujia_portal_layout/_base/_sale/_delivery/_report`; zh_CN + th_TH **ngoài `wujia_franchise` là trống**. Ước còn **3.500–4.000 chuỗi**. Chặn KHÔNG phải ở lệnh (30 giây/module) mà ở **glossary mới 250 dòng, toàn từ vựng giám sát** ⇒ việc thật = mở rộng glossary. **Chạy LOCAL trên DB copy, KHÔNG chạy trên server** (kết quả là file `.po` trong repo → commit → deploy `-u` là Odoo tự nạp). **Chủ dự án CHỐT 19/08: agent dịch máy + BA rà, KÈM FILE EXCEL** ⇒ mỗi đợt có **2 sản phẩm**: `.po` trong repo **và** file Excel `Module|EN|VN|CN|TH|Nguồn|BA sửa thành|Ghi chú` cho BA rà (cột **Nguồn** phân biệt `glossary` đã duyệt / `máy dịch` cần rà kỹ). BA trả file → đọc ngược vào glossary → sinh lại `.po` ⇒ glossary dày lên, đợt sau dịch đúng hơn. ⚠️ **Phần Excel CHƯA CÓ CODE** — phiên sau viết `--review-out/--review-in` (openpyxl). Kế hoạch 3 đợt + 4 bẫy đã trả giá + bảng nghiệm thu → **`docs/next-session-i18n.md`**.
- **S44 — tồn i18n (defer)**: 11 finding 'dịch chưa sát nghĩa' chờ BA chốt từ vựng (`scripts/ba_spec/i18n_sprint44/review_findings_raw.txt`) · nhãn `member.role` vẫn tiếng Việt (nằm trong `display_name` store, đổi cần migration) · menu vốn tiếng Anh chưa có bản dịch `vi_VN` · 2 chỗ portal vốn đã tiếng Anh từ trước (`portal_support.xml:430`, `portal_franchise_information.xml:248`) · UAT chưa đổi `lang`.
- **S41/S42 — Thông báo còn tồn** (chi tiết + prompt: `docs/next-session-tasks-notification.md`): NOTI-02 bước 2 (11 row `franchise_id` NULL local / 6 UAT — gán hay xoá là quyết định chủ dự án, rồi mới `required=True`) · NOTI-04 vặt · `Tasks` row 6 cột Out-of-scope vẫn ghi 'không target theo cửa hàng' — **ngược quyết định 07-31, chưa ai sửa**.
- ⚠️ **[07-31] Bài học ghi sheet BA**: Dev từng ghi sai rồi phải thu hồi. **Trước khi nói tài liệu BA mâu thuẫn hay cái gì 'đã làm', phải đọc cột trạng thái (`Feature Status`, `Trạng thái (AI)`) và Out-of-scope của task.** Ghi lên sheet thì ghi ít, đúng cái BA cần; phần còn lại để file local.
- **K — Bù hàng**: (b) guard size video minh chứng (1500 user); (d) SO bù để `draft`, HQ tự confirm. Deploy `-u wujia_portal_return,wujia_sale`. Plan: `functional-brewing-quill.md`.
- **Dashboard (workstream riêng)**: Step 2b (Query render + JS widget + 4 layout + PDF cho `wj_ks_dn_advance`) + Step 3 `wj_ks_dn_formula` chưa port. Nguồn: `docs/dashboard-migration-plan.md` (skill `/wujia-dashboard`).
- **Pre-existing**: `/portal/reports/orders` 500 do user tz `Asia/Saigon` — **đã xếp vào cụm R3** của `docs/refactor-plan.md` (fix ở `portal_tz()` helper).
- **NEXT sau 10 cụm**: trang desktop còn legacy theo `pc_source_ui_v1_4` (history/report/return/knowledge/support/home); mobile cũ: batch status thật (S13), khung giờ cart submit (S11), field bù hàng Phase 2 (S20), exam Phase 2 (S26).

**Phase 2 (future):** account.move Công nợ (CT-014) / Employee Mgmt / Payment History / Training Reports / User Invitations.

**Non-negotiable rules (mọi session):**
- ⚠️ **ĐỌC SOURCE TRƯỚC KHI SỬA — KHÔNG ĐOÁN model/field/method.** `grep -rn "_name = '" custom/<mod>/models/`. `wujia.franchise.management` (NOT `res.franchise`) — tên thực → §11 đầy đủ. Helper portal → §11.
- ⚠️ **REGRESSION CHECK trước khi sửa CSS/token/template**: `grep -rn "<selector|token>" custom/` xem blast radius (token global `--wujia-*` ảnh hưởng MỌI page); sau ship smoke 3-5 page khác.
- CSS bắt buộc `var(--wujia-*)` + class share `_components.css`. Không hex cứng.
- Demo data KHÔNG vào manifest XML → `scripts/seed_*.py` local-only.
- Odoo 19 view: không `attrs=`, không `decoration-secondary`, `_sql_constraints`→`models.Constraint`, search group `name="group_by"`, bỏ `expand="0"`.
- Commit English Conventional Commits, KHÔNG `--no-verify`. Comment GỌN (1 dòng đủ ý).
- Field rename: pre-migrate trước `-u`. i18n: code English, BA dịch `vi_VN.po`.
- ⚠️ **Field custom PHẢI tiền tố `wujia_`** (S52/L10) — trùng tên field core (`description_ecommerce` của `website_sale`…) làm đổi kiểu cột ở DB có cài app đó, local không cài thì không bao giờ lộ. Datetime portal: dùng `fmt_local_dt`/`wj_dt` (§11), KHÔNG `.strftime()` thẳng.

→ Chi tiết: `wujia-tea-doc.tex` §1.4.

---

## §6 wujia-deploy

**Deploy = thủ công** (dev server nội bộ, chưa CI/CD). Push `main` → user `git pull` + restart Odoo service Windows `D:\wujia-tea` tay. CSS change → bump `?v=NNNN` (§9 gotcha #1).

**⚠️ GOTCHA — MODULE MỚI KHÔNG TỰ CÀI:** restart chỉ load module ĐÃ cài + upgrade module bump version. Module HOÀN TOÀN MỚI phải `-i` 1 lần: `python odoo-bin -c <conf> -d <db> -i <module> --stop-after-init` (hoặc UI Apps → Install). Áp cho mọi sprint có module mới (vd dashboard `wj_ks_*`).

**Windows reseed 1-lệnh:** `nssm stop Odoo; reseed_full.ps1; nssm start Odoo` (git pull → drop+create DB → install chain → seed → test). UTF-8 env bắt buộc (`PYTHONUTF8=1`, `chcp 65001`). → `DEPLOY_SPRINT5.md` + `CHECKLIST.tex`.

---

## §7 wujia-start-instruction

- v19 active `/home/huyban/odoo-dev/WujiaTea`; v14 ref → §1.
- **BA spec = Google Sheet** `1HRiRLAZ9FlErRTLvwMaGhsOlYNPJHdf5AEMPvdLkQNE` (owner `huyhunggnguyen@gmail.com`, anyone-view, BA edit trực tiếp). Đọc tab qua CSV public: `curl -sL "https://docs.google.com/spreadsheets/d/1HRiRLAZ9FlErRTLvwMaGhsOlYNPJHdf5AEMPvdLkQNE/gviz/tq?tqx=out:csv&gid=<gid>"`.
- **Tab + gid:** `Tasks` (by name) · `MILESTONE` `1864615110` · `FEATURE CHECKLIST` `729461563` · `1.Model/Field` `2041118658` · `2.FE-Portal` `1002946158` · `3.Controller` `643561224` · `4.BE-Workflow` `1703696097` · **`5.Issue List`** `335593633` · `WORK LOG` `1388773997`. (Lấy gid: `curl .../htmlview | grep gid`.)
- Issue List: BA cập nhật mỗi ngày (đánh số hiện hành UI-01…06 = GLOBAL SHELL, khác `WJ_PageHeader` Sprint 9). Trạng thái → §5 + §12.
- ⚠️ **ƯU TIÊN SỐ 1 MỌI PHIÊN — `Tasks!STT1` "Fix lỗi issue list"** (task thường trực, trỏ tab `5. Issue List`). Đầu mỗi session PHẢI chạy `cd scripts/ba_spec && python3 issue_queue.py --dev`; có issue `Ready for Dev`/`Retest Failed` thì đề xuất làm trước mọi task khác. **Reconcile code↔sheet trước khi nhận** (`git log --all -S"<ID>"`) — issue có thể đã fix mà sheet chưa sync. **Không làm hết một lượt**: bám bảng batch §13, mỗi session một batch. Xong 1 issue phải đối chiếu acceptance BA ≥90% (§13) rồi mới ghi ledger.
- **Controller task (S32+):** BA gửi spec qua chat GPT share → `scripts/ba_spec/fetch_ba_chat.py <url>` + `read_xlsm.py <sheet> <kw>`, đối chiếu source model THẬT (BA hay đặt tên lý tưởng hoá ≠ thật), hỏi ở fork. Toolchain gitignored, KHÔNG lên server (`scripts/ba_spec/README.md`).
- **QA/Task workflow (2026-07-21):** xem §12.
- Sprint log `wujia-tea-doc.pdf` (compile `chapters/*.tex` qua `scripts/build-doc.sh`).
- **UI-only** (button chưa cần wire, miễn layout đúng BA). **Perf-first 1500 user** (ormcache, store+index, cron). **Ask-don't-assume + Read-before-write.**
- End session: `/wujia-end-sprint` (test → doc → PDF → ledger/qa_sync → commit → push).

Slash: `/wujia-start` `/wujia-load-feature <letters>` `/wujia-save-insight` `/wujia-end-sprint` `/wujia-dashboard`.

---

## §8 wujia-session-template

```
Session này em làm <1 câu>.
1. Ref: v14 <path>, BA <Sheet!Section>, chapter <XX>.
2. Task A/B: <mô tả>.  3. Out-of-scope: <không làm>.
Discovery → plan → user approve → code → upgrade RC=0 → screenshot → commit.
Perf: <lưu ý query 1500 user>.  Xong: /wujia-end-sprint.
```

---

## §9 wujia-sprint9-history + gotchas

**Sprint 9 (24 sub-sprint UI-01..18 + empty state + cleanup)** = DONE 2026-06-04, chi tiết → `chapters/18-*.tex`. Issue table + file-touched table đã gỡ khỏi summary (giữ trong chapter 18 + git).

**Gotchas còn tái dùng (đọc kỹ trước khi sửa UI/UoM):**
1. **Cache 7 ngày** — Odoo static `Cache-Control: max-age=604800`. CSS change PHẢI bump `?v=NNNN` trong `assets.xml` (chỉ file load qua manual `<link>`; `web.assets_frontend` auto-bundle không cần). Bump cao hơn lần user thấy cuối.
2. **CSS/màu = FILE TRÊN ĐĨA, KHÔNG ở DB.** "local khác server" về CSS = CACHE (browser / `?v=` chưa `-u` / proxy), KHÔNG bao giờ là data → **đừng drop/copy DB để sửa CSS**. Debug: `curl .../file.css` + view-source `?v=` + check proxy.
3. **Global heading `!important` đè class** (`h1,.wujia-h1{...!important}`) ép mọi `<h1>/<h2>` bare → 32/24px, đè cả class đơn. Fix: scope 2 lớp `.wujia-mpage .wujia-mxxx-h1` **+ `!important`**. Server không `--dev` → sửa CSS xong phải `-u`/`--dev=all` regen bundle.
4. **Vuexy navbar `.badge` cascade** — cùng specificity base `.badge` → env flaky (local đỏ, server tím). Fix: scope `.header-navbar … .wujia-header-badge` + `!important` bg+color, digit `inline-flex` center.
5. **Odoo 19 UoM (Sprint K)** — `uom.uom` bỏ `category_id` → cây `relative_uom_id`. `_compute_quantity(...,'UP')` không kiểm nhóm → kết quả vô nghĩa nếu khác nhóm; kiểm nhóm = so gốc cây. Chiều ngược dùng `'DOWN'` (tránh double-UP). `sale.order.line` ĐVT = `product_uom_id`; `stock.move` đã giao = `move.quantity`; backorder tạo trong `_action_done`.
6. **BA hex typo** lệch ≤4 ký tự (`#28A9DF` vs `#22A9DE`) → coi typo, dùng token; lệch nhiều → hỏi.

---

## §10 wujia-lessons (11 lesson cốt lõi)

Postmortem chi tiết → `chapters/18-*.tex`.
- **L1 — Extract full images từ xlsm + MAP image→cell** qua openpyxl `img.anchor._from.row/col` (KHÔNG cherry-pick theo số file). Annotation BA: khoanh đỏ=target / gạch chéo=xóa / gạch chân=highlight.
- **L2 — Check v14 trước khi build mới**: `grep -rln <kw> /home/huyban/odoo-dev/wujia_tea_odoo14/modules/`. Có → adapt; không → ghi rõ "v14 KHÔNG có X" + build từ đầu.
- **L3 — Visual design hỏi explicit 4 câu** trước khi code: bg color? text color? layout (inline/stacked)? icon (feather name)? KHÔNG assume hex từ ảnh.
- **L4 (S39) — Dựng Figma xong PHẢI đo computed-style, đừng nhìn ảnh.** Shell Vuexy có 4 rule **tag-level + `!important`** thắng mọi class: `_components.css` `h1/h2 {font-size !important}` · `style.css` `table th {font-size:16px !important}` · `dashboard.css` `select {width:100%;padding:5px !important}` · `bootstrap-extended` `label {padding-left:.2rem}`. Cách phát hiện: Playwright duyệt `document.styleSheets`, `el.matches(rule.selectorText)`, in ra mọi declaration + cờ `!important`. Trung hoà trong **scope component**, KHÔNG sửa 4 file shared (blast radius = toàn portal). Đồng cấp specificity thì **thứ tự source quyết định** — modifier `--sm` phải đặt sau base, và `.x .y` (0,2,0) sẽ đè `.z` (0,1,0) dù `.z` mang ý nghĩa cụ thể hơn.
- **L5 (S41) — Schema change trên bảng ĐANG có dữ liệu, 3 bẫy.** (1) `unique(a,b,c)` **không chặn gì** ở nhánh `c IS NULL` (Postgres coi mọi NULL khác nhau) → thêm `models.UniqueIndex('(a,b) WHERE c IS NULL')`. (2) Field mới có `default` + `unique` = Odoo backfill cùng một giá trị rồi constraint vỡ → bỏ default, để pre-migrate lấp giá trị phân biệt TRƯỚC khi ORM tạo constraint. (3) Rename field Python nhưng **giữ key JSON** trả client ⇒ 0 dòng JS phải sửa.
- **L7 (S43) — Harness đo Figma: chân lý là NODE JSON, không phải kỳ vọng mình gõ ra; harness sai thì sửa harness.** 2/7 phát hiện là báo động giả (nút back 40 vs 42 = `border-box`; gap 8 vs 12 = header kết ở y=202). Chỗ Figma **cố ý** lệch thì khai báo miễn trừ cho đúng element, đừng nới ngưỡng cả trang; chỗ Figma tự mâu thuẫn thì giữ nguyên + ghi lý do. 5 lỗi số đo thật đều do **shell tag-level đè** (`body{letter-spacing}`, `line-height:1.8`, `form{margin-bottom}`) → trung hoà trong scope component. Sửa CSS không thấy đổi ⇒ **nghi cache `ir.attachment` của bundle trước khi nghi selector**. Và **tiêu đề rỗng trong spec BA là một câu trả lời** ('chưa quyết') → dựng UI-only + chừa MỘT seam đổi nguồn.
- **L6 (S42) — Muốn "chọn theo tiêu chí" mà không phá tầng phân quyền: tiêu chí là CÁCH CHỌN, M2M là KẾT QUẢ.** Lưu domain-string rồi eval lúc đọc thì `ir.rule` (vốn là domain ORM) không diễn đạt nổi "thông báo có domain khớp cửa hàng tôi" → phải lọc Python, mất index, chết ở 1500 user; và chạy lại tiêu chí trên dữ liệu đã đổi cho kết quả khác lúc gửi. Giải: `target_mode` all/filter/manual, tiêu chí resolve **1 lần tại `action_publish`** rồi `Command.set` vào M2M sẵn có → portal/ir.rule/controller/index **không đụng dòng nào**, có snapshot để audit. Đánh đổi phải hỏi chủ dự án và ghi vào spec: cửa hàng mở sau ngày gửi KHÔNG nhận thông báo cũ (có nút *Cập nhật danh sách nhận* khi cần). Kèm bài học giao tiếp: user nói **"khó hiểu quá"** = lỗi ở người giải thích — bỏ tên field/thuật ngữ, kể bằng thao tác thật ("chọn Miền Bắc → Xem thử → 137 cửa hàng → Gửi") và gom về **một** câu hỏi nghiệp vụ.
- **L8 (S44) — Dịch nhãn hàng loạt: 5 bẫy, cái nào cũng PASS im lặng.** (1) Một từ Việt = hai từ Anh: nút = **động từ**, state/filter = **tính từ** (`Reject`/`Rejected`); `.po` không cấm 2 msgid → 1 msgstr nên chiều `vi_VN` giữ nguyên. (2) Template portal không chứa chữ Anh vẫn rò tiếng Anh — chỗ hở là `_fields[].selection` đọc từ model ⇒ **pin hằng nhãn VN trong controller portal**. (3) ⚠️ Odoo **tự merge `<module>.pot`** khi nạp `.po` ⇒ `.pot` cũ làm msgid mới thành obsolete, DB không đổi mà không lỗi không warning — **sinh `.po` phải sinh `.pot` cùng lúc**. (4) Lọc chuỗi Việt theo DẤU bỏ sót tiếng Việt không dấu ('Ca thi', 'Xe'). (5) Assert bằng `in` là assert rỗng khi chuỗi này là con chuỗi kia. Kèm: overlay che trang làm mọi assert pass rỗng; `pkill -f 'odoo[-]bin'` gộp chung dòng với lệnh chạy server thì **shell tự giết chính nó** (exit 144).

- **L9 (S50) — Đọc mockup BA dạng SVG + dựng empty-state chung: 6 bẫy.** (1) **Đính kèm của BA nằm ở HYPERLINK của ô**, `export?format=csv` mất hyperlink ⇒ tải `xlsx` rồi openpyxl đọc `cell.hyperlink.target`. (2) SVG Figma là **outline path, không có `<text>`** ⇒ nhúng inline vào HTML rồi Playwright `getBBox()`, tự parse path cho bbox sai hoàn toàn. (3) **Comment XML không được chứa `--`** (dán tên class modifier vào comment làm hỏng file). (4) Icon trong flexbox phải pin `flex: 0 0 <size>`, chỉ có `width` sẽ bị bóp. (5) Harness sai thì sửa harness (nhãn bị `text-transform:uppercase` nên so chuỗi trượt). (6) 'Rỗng' của Home không rỗng theo cửa hàng — Thông báo + Kiến thức là nguồn toàn hệ thống. Kèm: `\text{}` thiếu trong preamble và `language=CSS` không tồn tại làm `build-doc.sh` **RC=0 nhưng PDF cụt giữa chương** ⇒ phải grep `^! ` trong `.log` + `pdftotext` kiểm.

- **L10 (S52) — Trùng tên field với core là bom hẹn giờ chỉ nổ ở server.** (1) **Field custom PHẢI có tiền tố `wujia_`** — `description_ecommerce` trùng field Html của `website_sale` ⇒ DB nào cài app đó thì cột thành **jsonb** trong khi ORM ghi varchar; local không cài nên **không bao giờ lộ** ('local chạy ngon' không chứng minh gì về UAT). (2) **Đọc kiểu cột của server không cần quyền SQL**: `search([('field','=','__probe__')])` qua XML-RPC, cột jsonb trả `InvalidTextRepresentation`. (3) Không tái hiện được NGUYÊN NHÂN thì nói thẳng — tái hiện TRIỆU CHỨNG là đủ để chứng minh fix. (4) Migration đổi tên phải **phân nhánh theo `information_schema.columns`** và test cả hai nhánh, kiểm dữ liệu còn nguyên chứ không chỉ RC=0.
- **L11 (S54) — 3 bẫy "đúng cú pháp nhưng chết im lặng" ở tầng portal.** (1) **KHÔNG dùng `&&`/`<`/`>` trong `<script>` inline của QWeb**: lxml serialize template ra HTML thành thực thể (`&amp;&amp;`), mà HTML5 **không giải mã thực thể bên trong `<script>`** ⇒ **cả khối JS chết, không log, không đỏ build** — `//<![CDATA[` KHÔNG cứu được (serializer gỡ marker). Viết `if` lồng hoặc đưa hẳn ra `static/src/js/`. Quét bằng script tìm thực thể *nằm trong* `<script>`, đừng grep `&amp;` trần (trong **thuộc tính** thì `&amp;` là đúng). (2) **Validate redirect phải nghĩ tới bước chuẩn hoá của trình duyệt**: kiểm tiền tố `/portal/` là chưa đủ — `?return_url=/portal/../web` qua được rồi bị normalize thành `/web`; cấm segment `..` **trước** khi xét tiền tố. (3) **Blast radius lớn thì tìm seam, đừng sửa N chỗ**: `ph_variant='back'` gọi ở 31 chỗ/14 file ⇒ 1 helper trên `ir.http` gọi từ `t-att-href` cho toàn bộ hưởng hành vi mới, 0 dòng controller, 0 chỗ để quên. (4) **A11y: đo để chứng minh, đừng code mò** — 2 variant ẩn bằng `d-none` vốn đã ngoài accessibility tree, đếm ra 1 link rồi mới kết luận, không thêm `aria-hidden` thừa.

- **L12 (C1) — Chuyển field thường thành stored compute KHÔNG tự sửa dữ liệu cũ.** Đo thật sau `-u`: bản ghi cũ y nguyên (9 SO trống vẫn trống, bản ghi lệch vẫn lệch) — Odoo chỉ mark-to-compute cho field MỚI. Muốn sửa dữ liệu cũ phải viết post-migrate, và nên **chỉ điền bản ghi TRỐNG**, đừng ghi đè giá trị người dùng đã chọn. Kèm: chặn nghiệp vụ nên đặt ở **method hành động** (`action_confirm`/`button_validate`/`action_post`) chứ không `@api.constrains` — constrains khoá luôn chứng từ cũ đã xác nhận, chỉ sửa ghi chú cũng bị chặn. Và harness Playwright: dropdown Many2one hiện **danh sách mặc định trước khi lọc xong** ⇒ click `li:first-child` bắt nhầm bản ghi; Odoo 19 **tự lưu bản nháp khi rời form** ⇒ mở form 'chỉ để xem' trên UAT vẫn đẻ bản ghi, phải kiểm và dọn.

- **L13 (C3) — Thêm một `<div>` slot là đủ để giết `gap` của cả trang.** `wj_ajax_list` (S49) bọc vùng lọc-không-reload bằng `<div id=...>` thường; `.wj-debt` là flex column `gap:12` nên gap chỉ áp cho **con trực tiếp** ⇒ mọi section bên trong slot dính 0px (WJ-DEBT-009, BA đo đúng). Sửa ở **wrapper** (cho slot cùng `display:flex` + gap), đừng vá `margin` từng khối. Kèm: (1) trước khi tin "trang X vỡ ở 500px", **đo lại** — sidebar Vuexy vốn off-canvas ở mọi bề rộng mobile, cái sai thật là trang pay dựng markup mobile ở MỌI bề rộng vì thiếu `d-lg-none`; (2) `bank` rỗng thì trả **cờ `configured`**, đừng để template đoán bằng chuỗi rỗng — và đừng nhét câu thông báo vào field dữ liệu (`name`), UI là việc của template; (3) harness Playwright login được cả khi form login bị `d-none`: `POST /web/session/authenticate` rồi gắn cookie `session_id` vào context.

- **L15 (merge `thai`) — Nhận code của dev khác: 4 lớp lỗi chỉ lộ khi CHẠY, không lộ khi đọc diff.** (1) **`group_id` để TRỐNG trong `ir.model.access.csv` = áp cho MỌI user**, không phải "chưa gán ai" — kèm `1,1,1,1` là mở cửa write/unlink cho 1500 portal user; đọc diff chỉ thấy "gọn hơn 6 dòng". (2) **`init()` KHÔNG phải chỗ seed dữ liệu** (chạy giữa lúc Odoo dựng bảng theo thứ tự từng model ⇒ `create()` sang model khác = `UndefinedTable`), **nhưng `<odoo noupdate="1">` cũng sai** vì chỉ chạy lần cài đầu ⇒ DB đã cài sẵn (UAT) thì `-u` không seed, tính năng lên server với 0 dữ liệu cấu hình. Đúng = data XML **không** `noupdate` + hàm seed idempotent. ⚠️ Phải đo trên DB **đã cài sẵn module**; `-i` trên DB trắng thì cả hai cách đều xanh. (3) **Fail-open đọc rất giống fail-safe**: `if ids and x not in ids` — `ids` rỗng thì `and` ngắt mạch **bỏ qua kiểm tra**, mà rỗng chính là ca nguy hiểm nhất (user không thuộc cửa hàng nào). Viết `x not in (ids or ())`. Cùng họ: `try: import ... except ImportError: return []` biến lỗi import thành **tắt im lặng phân quyền**. (4) **Chỉ đổi `_order` khi model không dùng chung** — `_order` đổi thứ tự ở MỌI màn; muốn sắp riêng thì `default_order` trên view. Kèm 2 bài học vận hành: đường dẫn máy cá nhân lọt vào `scripts/*.sh` dùng chung (+ `chmod` 755→644) ⇒ tách config khỏi script; handler log tự chế **gỡ hẳn** handler theo `logfile` ⇒ file trong `odoo.conf` rỗng, người debug tưởng server chết — chuyển hướng log phải để lại một dòng chỉ đường. Và về i18n: **glossary đánh khoá tiếng Việt + msgid tiếng Anh (sau S44) = trượt sạch** ⇒ bắc cầu qua chính `vi_VN.po`; sinh `.po` bằng regex tự chế thì cắt chuỗi tại `\"` và chèn xuống dòng thật vào giữa chuỗi — **dùng `babel.messages.pofile`**, và luôn `msgfmt -c` trước khi nạp DB.
- **L14 (C6) — CSS: thắng bằng specificity, và đừng tin cascade của local.** (1) `web.assets_frontend` nạp **SAU** mọi `<link>` custom ⇒ hoà specificity là Odoo thắng; muốn đè `a:hover{text-decoration:underline}` (0,1,1) thì rule phải ≥(0,2,1), muốn đè descendant rule Vuexy `.list-group .list-group-item-action:focus{outline:0}` (0,3,0) phải bám **cùng tổ tiên** hoặc leo `:not()` — **đo `c6_why.py` rồi mới leo**, đừng rải `!important`. (2) Blast radius toàn portal thì **một layer nạp cuối** rẻ hơn vá N chỗ, và revert được bằng 1 file. (3) **L10 lặp lại**: DB local KHÔNG có `website_sale` nên **không tái hiện được** lỗi underline của UAT — bằng chứng phải lấy bằng `page.add_style_tag` nhúng CSS vào **chính UAT**; nhúng thì phải nhúng **cả `_variables.css`**, thiếu token thì `outline: var(--…)` invalid và **xoá luôn ring đang có** (tưởng fix hoá ra tệ hơn). Nhúng lặp file token cũng tự đẻ lệch chiều cao ⇒ phải có **run đối chứng chỉ nhúng token** mới biết lệch đó không phải do mình. (4) Ép trạng thái bằng CDP `CSS.forcePseudoState` chứ đừng `page.hover()` (overlay/actionability chặn), nhưng **trạng thái ép rò sang node kế** ⇒ sau mỗi element phải reset **rồi đọc lại và so** với default, lệch thì báo `leak`. (5) Chốt cuối cùng phải là **đi Tab thật**, không chỉ ép pseudo-class.

---

## §11 wujia-shared-utils-cheatsheet

**CSS class chung** (`_components.css`): `.wujia-btn[-primary/-secondary]` (h42/h38) · `.wujia-badge[-success/warning/danger/info/muted]` · `.wujia-empty-state` · `.wujia-two-pane` · `.wujia-kpi-card[+ -icon-*/-separator]` · `.wujia-content-card[-header/-body/-row/-table/-empty]` · `.wujia-container/-grid-responsive/-stack-mobile`. Canonical: `wj-filter-chip[--soft/--wrap/--clear]` · `wj-count-meta[--bold/--primary]` · `wj-empty-state[--card/--compact/--rich]` · `wj_page_header` (title/back/create) · `wj-pc-*` (PC components).

**Token** (`_variables.css`): `--wujia-primary #28A9DF` (BA CẤM #22A9DE) · `--wujia-bg-page #F3F6F8` · `--wujia-text-primary #111827` · `--wujia-text-secondary #374151` · `--wujia-text-subtitle #6B7280` · `--wujia-border #E5E7EB` · danger `#EF4444` · success `#16A34A` · `--wujia-text-muted #8A939E` · `--wujia-card-radius 16px` · `--wujia-btn-height 42px`. Font Inter self-host (weight 700).

**Datetime portal (BẮT BUỘC dùng, đừng `.strftime()` thẳng):** `portal_tz()` / `to_local_dt(dt, tz)` / `local_day_range_utc(d1, d2, tz)` / **`fmt_local_dt(dt, fmt, tz=None)`** (S52) ở `wujia_portal_base/controllers/utils.py`. Template lấy qua key qcontext **`wj_dt`** — controller nào render datetime phải tự inject `'wj_dt': fmt_local_dt`. Odoo lưu naive UTC ⇒ in thẳng = lệch −7h (WJ-NOTI-001, WJ-PH-002).

**Python helper (portal):** `get_active_franchise_id()` / `get_active_franchise_ids_filter()` ở `wujia_portal_base/controllers/portal.py` (KHÔNG `utils.py`) · `wujia.franchise.member.find_active_membership(user_id, franchise_id)` → membership record (hay `False` nếu không có); để lấy role dùng `.role` trên record trả về · `res.config.settings._is_within_order_window(area_id)` (dùng qua `self.env['res.config.settings']._is_within_order_window(area_id=...)`) · `rate_limit` + `attach_files_to_record` ở `controllers/utils.py`.

**Model names thực:** `wujia.franchise.management` / `.member` / `wujia.order.window` / `wujia.notification` / `wujia.notification.type` / `wujia.notification.read` / `wujia.knowledge.article` / `wujia.support.ticket` / `wujia.info.update.request` / `wujia.compensation.allocation` / `wujia.exam.*` / `wujia.portal.cart[.line]` / `res.area` / `res.ward`.

---

## §12 wujia-qa-uat-nightly

**QA Operating Standard** = `docs/01_NGO_GIA_QA_OPERATING_STANDARD.md`. Luồng: `New → Ready for Dev → Dev In Progress → Ready for Retest → BA Retesting → Done`. **Dev KHÔNG tự đóng `Done`** — tối đa `Ready for Retest`. Fork/thiếu spec → `Need Clarification` (owner BA), KHÔNG đoán.

**UAT** `http://113.161.187.126:8019/` (`admin/Wujia@2026`) — tự smoke-test được. Giới hạn: không tạo đơn/hoá đơn/email thật, không đổi quyền, không drop data. Server info: `docs/CREDENTIALS.md`.

**Sheet tabs BA log:** → §7 (Tasks + 5.Issue List gid=335593633). Chuẩn lên task cho AI = `docs/02_TASKS_INTAKE_SPEC_FOR_GPT.md` (đã gửi BA/GPT).

**Ghi ngược sheet (dev-only, `scripts/ba_spec/`, gitignored):** ĐỌC = CSV công khai (không auth). GHI = POST tới **Apps Script bridge** chạy as editor (Google chặn OAuth scope Sheets nên KHÔNG dùng gcloud/token). `sheet_io.py` đọc CSV + ghi qua `sheet_endpoint.json` (webapp_url+secret).
- Setup 1 lần: deploy `qa_nightly/WujiaSheetBridge.gs` (Extensions→Apps Script→Web app, execute as editor) → dán URL vào `sheet_endpoint.json`. Chi tiết `docs/03_OAUTH_SHEET_SETUP.md`.
- **Làm xong 1 issue** → thêm entry vào `docs/qa-issue-ledger.yaml` (chỉ khi code khớp expected HIỆN TẠI) → `cd scripts/ba_spec && python3 qa_sync.py --dry-run` (xem) → `--apply` (set `Ready for Retest` + Build/Deploy + FIX/IMPACT/RETEST/LIMIT + Odoo Fit + dòng `7. ISSUE HISTORY`). Idempotent; tự SKIP issue `Need BA Confirm=Yes`/`Need Clarification`.
- `task_sync.py --list` (task Ready-for-AI) / `--row N --status/--question/--result` (ghi O/P/Q/R).

**Nightly agent — INTERACTIVE trong tmux (default 2026-07-22).** Cron `0 22` → `cron-tmux-launch.sh` mở session `wujia-nightly` chạy `run-interactive.sh` (claude opus/xhigh/acceptEdits, seed `agent_prompt_interactive.md`). Trực: `tmux attach -t wujia-nightly`.

Phạm vi: Dev-actionable (`issue_queue.py --dev`) + review `Ready for Retest`; mỗi issue 1 branch, `-u` RC=0. Agent **hỏi trước push main** (1=push, 2=lặp); xong → ledger + `qa_sync --apply`.

Giới hạn: KHÔNG tự `Done` / force / no-verify / drop-DB. Fallback headless: `run.sh` (không còn cron default). Chi tiết phím tmux → `scripts/ba_spec/qa_nightly/USAGE.md`.

**Self-verify Issue List bằng headless Chromium (2026-07-23).** Env `odoo` có sẵn `playwright` + chromium → ĐO computed-style/bounding-box y như BA thay vì đoán/ghi LIMIT "không verify headless". Tool: `scripts/ba_spec/qa_visual_check.py` (login admin/UAT, `--url --w --h --measure "sel:prop" --inject "css"`). **Gotcha:** (1) portal có long-poll bus.bus → dùng `wait_until="load"` KHÔNG `networkidle`; (2) login submit bằng Enter (nút login trùng nút Search); (3) nếu inject `!important` ultra-spec mà computed KHÔNG đổi → element bị JS/plugin Vuexy điều khiển (sidebar `.main-menu` width, nút `.btn-primary.waves-effect`), CSS bất lực → defer, đừng cố. **Kinh nghiệm 07-23:** đa số "Retest Failed" của BA thực ra ĐÃ đúng trên server — BA test build cũ trước deploy; luôn đo lại server hiện tại trước khi kết luận.

**⚠️ Gotcha ghi sheet — filter ẩn Done làm LỆCH ROW (07-23, đã fix).** `sheet_io.read_values` cũ đọc bằng gviz/tq **tôn trọng filter** của BA (ẩn "Done") → trả THIẾU dòng → `find_row` đánh số theo view lọc, nhưng bridge Apps Script ghi theo **ROW TUYỆT ĐỐI** → ghi Done NHẦM sang dòng khác (đã từng hỏng UI-06/UI-PC-SHELL-001/UI-MOB-SHELL-002, khôi phục xong). ĐÃ SỬA: `read_values` cho tab có gid dùng `export?format=csv` (bỏ qua filter, row khớp tuyệt đối). Sau mọi lần ghi sheet PHẢI verify lại bằng `export?format=csv` (không dùng gviz để verify). Set Done: `scripts/ba_spec/qa_done.py` (override rule chỉ khi chủ dự án duyệt).

**⚠️ Gotcha TÊN TAB — đừng lấy tên từ `export?format=xlsx` (07-31).** Excel cấm `/` trong tên sheet nên Google **sanitize khi export**: tab thật `1. Model/ Field` ra thành `1. Model Field`; gửi tên đó cho bridge → `Error: Không tìm thấy tab`. Cũng đừng dò bằng gviz `sheet=<name>` — sai tên nó **im lặng trả tab đầu tiên** (MILESTONE), tưởng đúng mà đọc nhầm sạch. Cách đúng: `sheet_io._post({'action':'ping','sheet':'1. Model'})` → bridge trả `sheet.getName()` là tên THẬT (`_resolve` có fallback substring nên gõ một phần là đủ). `KNOWN_GID` đã có cả 2 key (thật + alias xlsx). gid: Tasks `1936593712` · `1. Model/ Field` `2041118658` · `3. Controller` `643561224`.

**⚠️ Gotcha — `Ready for Dev` KHÔNG có nghĩa là chưa làm (08-10).** WJ-ORD-023 (FilterBar PC 1 hàng) đã fix commit `63dc4bc` ngày 08-04 nhưng sheet vẫn `Ready for Dev` tới 08-10 vì không ai chạy `qa_sync.py` sau khi code. Ngược lại BA cũng hay retest build cũ (§12 kinh nghiệm 07-23). ⇒ **Nhận issue nào cũng reconcile trước**: `git log --all -S"<ISSUE-ID>"` + `grep -rn "<ISSUE-ID>" custom/` + đo lại UAT hiện tại. Đã fix rồi thì chỉ ledger + `qa_sync.py`, KHÔNG code lại.

**Spec F ↔ source (Sprint 41, 07-31).** Đồng bộ tên bằng `scripts/ba_spec/spec_f_sync.py` (dry-run → `--apply`) + `task_s41_rewrite.py` (P3/Q3 có dấu). Nguyên tắc user chốt: **chỉ đổi TÊN model/field — KHÔNG ghi thêm bất cứ thứ gì vào tài liệu BA.** Dev từng thêm cột ghi chú rồi phải xoá (`spec_f_wipe_notes.py`), xem §5. Việc tồn + prompt session sau: `docs/next-session-tasks-notification.md`. Ghi lên sheet thì ghi ít, đúng cái BA cần; phần còn lại để file task local.

---

## §13 wujia-ui-component-standard (tab `UI Component`, gid `488333015`)

**Tab này là chuẩn component dùng chung toàn portal** — mục tiêu: mọi page dùng CÙNG một component, một form. Sheet có **2 khối**: khối trên (dòng 1–23) = danh mục 23 component (ID · Nhóm · Nhận diện · Boundary · Ví dụ); khối dưới (từ dòng 29) = spec đầy đủ (Mục tiêu · Hiện trạng · Spec chuẩn hoá · Variants · Màn hình áp dụng · Acceptance · Out-of-scope · Priority · Status).

**Liên hệ với `5. Issue List`:** tab UI Component = **chuẩn**; Issue List = **kênh triển khai + retest**, nối bằng cột `Related Feature ID`.

**23 component** (Nhóm — ID — tên): Global Shell: `CMP-GH-001` GlobalHeader · `CMP-SCB-001` StoreContextBar. Navigation: `CMP-SN-001` SidebarNavigation (chỉ desktop) · `CMP-BN-001` BottomNavigation (chỉ mobile) · `CMP-BPH-001` BackPageHeader · `CMP-PGN-001` Pagination. Page Structure: `CMP-PC-001` PageContainer · `CMP-PG-001` PageHeader · `CMP-CM-001` CountMeta · `CMP-SH-001` SectionHeader. Container: `CMP-SC-001` SurfaceCard. Filter: `CMP-FB-001` FilterBar · `CMP-SF-001` SearchField · `CMP-FC-001` FilterChipGroup. Detail: `CMP-DS-001` DetailSummary. Status: `CMP-SB-001` StatusBadge. Data Display: `CMP-DL-001` DataList · `CMP-KPI-001` StatCard · `CMP-LC-001` ListCard. Feedback: `CMP-ES-001` EmptyState · `CMP-IB-001` InfoBanner. Action: `CMP-BTN-001` Button. Domain/Order: `CMP-PRD-001` ProductCard.

⚠️ **Mới 3/23 component có spec đầy đủ** — `CMP-PG-001` PageHeader, `CMP-BPH-001` BackPageHeader và **`CMP-SH-001` SectionHeader** (dòng 33, Status `BA Confirmed` 11/08). **20 cái còn lại chỉ có 1 dòng mô tả ⇒ CHỜ BA viết spec, KHÔNG tự đoán px/màu** (chủ dự án chốt 08-10, đúng rule Ask-don't-assume).

`CMP-SH-001` tóm tắt (để khỏi mở sheet): PC title 22/30/800, meta-action 14/20/700, section trước→header 20, header→content 12, cách right slot ≥16; mobile 20/28/800, spacing 16/8, cách ≥12, action tap ≥44. Màu title `#111827` · meta `#6B7280` · action `#28A9DF`. Variant default/meta/action/control — **tối đa MỘT right slot**. Title là **heading thật** (không `<span>`), mỗi màn truyền headingLevel riêng, wrap ≤2 dòng, right slot không wrap (thiếu chỗ thì xuống dòng căn phải, **không tự ẩn**). Count hiện cả khi = 0 và dùng **từ đầy đủ** "5 sản phẩm" (không "5 SP"). Heading **nằm trong card = CardHeader**, không phải SectionHeader. `/portal/delivery` "Danh sách chuyến giao" dùng variant `meta`.

### ~~Bảng batch B0–B4~~ ✅ ĐÓNG HẾT 08-10/08-11

B0 WJ-ORD-023 (reconcile 0 code) · B1 WJ-EXAM-001…006 (ch.64) · B2 WJ-NOTI-001 + WJ-PROD-001 (ch.65) · B3a+B3b UI-PAGEHEADER-001 `CMP-PG-001` đủ 17 route (ch.66, 34/34) · B4 UI-BACKPAGEHEADER-001 + FUNC-MOB-SHELL-005 (ch.67, 14/14 + 31/31 + 286/286). Chi tiết kỹ thuật → chapter tương ứng + `docs/qa-issue-ledger.yaml`.

### Bảng cụm C1–C10 (32 issue Ready for Dev, khảo sát 2026-08-14, STT 83–114)

BA audit UAT 11–13/08 mở **32 issue mới**. Đã reconcile `git log --all -S"<ID>"` + `grep -rn "<ID>" custom/` cho **cả 32 ID ⇒ 0 issue đã fix sẵn** (2 ID có hit chỉ là câu tham chiếu trong ledger B4).

📌 **Prompt sẵn cho từng session + ô tiến độ 10 cụm: `docs/next-session-clusters-C.md`.** Cách dùng: `/wujia-start` → nói **"làm cụm C&lt;n&gt;"** → đọc file đó lấy đúng khối prompt. Xong cụm nào thì đánh ✅ + ngày + commit vào dòng "Tiến độ cụm" của file.

**Thứ tự chạy:** C1 → C2 → C3 → C4 → C5 → C9 → C10 → **C6 → C7** → C8. C1 sửa gốc dữ liệu mà C2/C3/C5 cần để retest; C6 phải xong trước C7 (WJ-HOME-007 đòi trạng thái pressed/focus của component chung); C8 để cuối vì chạm heading mọi trang, chạy sau khi C7 sắp lại block Home thì khỏi làm hai lần.

| Cụm | Issue (STT) | Module `-u` | Gốc rễ + điểm đã soi trong source |
|---|---|---|---|
| **C1** Franchise backend | WJ-FRANCHISE-001 (85), WJ-FRANCHISE-002 (89), WJ-DEBT-006 (103) — High×3 | `wujia_sale`, `wujia_account` | `sale_order.py:145` chỉ có onchange chiều `franchise_id`→`franchise_partner_id`, **không có chiều ngược từ `partner_id`**; `account_move.py` chỉ khai field; constraint `_check_portal_*` chỉ áp `is_portal_order` nên đơn tạo tay ở backend confirm được với franchise trống; **không chỗ nào override `_reverse_moves`** ⇒ credit note mất franchise. Cần 1 helper partner→franchise dùng chung (1 query có index) + validate ở `action_confirm`/`button_validate`/`action_post` + propagation reversal. |
| **C2** Debt — số liệu & rule | WJ-DEBT-001 (98), 004 (101), 007 (104), 010 (113) — High×4 | `wujia_portal_debt` | Tầng model, không migration. `wujia_portal_debt.py:434` trả `state='partial'` khi `remaining>0` & không quá hạn **kể cả `paid==0`** (001). `get_payments` gọi `_currency_symbol()` **không truyền moves** ⇒ luôn ra tiền công ty (004). `remaining<=0` → `'paid'` nhưng dòng credit note qua `_invoice_status` ra `'unpaid'` vì `move_type != out_invoice` (007). `_resolve_week` fallback `options[0]` = tuần hiện tại; `_week_options` đã có `monday`/`offset` để tính hạn thứ Năm tuần kế (010). |
| **C3** Debt — trang thanh toán | WJ-DEBT-002 (99), 003 (100), 005 (102), 008 (105), 009 (112) — High×3, Med×2 | `wujia_portal_debt` | Template/CSS/JS, chạy **sau C2**. `get_bank_info` trả dict rỗng khi không có bank nhưng template vẫn dựng QR + 2 nút copy (002). `portal_debt_pay` (`portal_debt.xml:621`) bọc `.wj-debt .wj-debt--back` — **thiếu `d-lg-none` + class shell mobile** mà overview (`:68 .wj-debt--overview d-lg-none`) có (003). `copyText()` có fallback `execCommand` nhưng **nuốt lỗi im lặng** — phải đo UAT trước khi đoán nguyên nhân (005). Spacing mobile payment-history 12/24/12/12/12, padding ngang 16 (009). |
| **C4** Knowledge | WJ-KNW-001…004 (90–93) — High×2, Med×2 | `wujia_portal_knowledge` | `controllers/portal.py:65` `Category.browse(cat_id)` **không `.exists()`** ⇒ template chạm `.name` là `MissingError`, portal in `wujia.knowledge.category(999999,), User: 2` (001, lộ thông tin). 6 chỗ `publish_date.strftime()` trong `portal_knowledge.xml` (93/201/228/277/292/338) ⇒ lệch −7h — **tái dùng `fmt_local_dt`/`wj_dt`** y hệt WJ-NOTI-001 (002). Search chỉ `('name','ilike',kw)` (`:21`) (003). Detail `:76` redirect trần không thông báo (004). |
| **C5** Delivery & dữ liệu giao hàng | WJ-DELIVERY-005 (86), 006 (87), 007 (88), WJ-HOME-003 (108) — Med×4 | `wujia_portal_delivery`, `wujia_portal_base` | `_chip_counts` (`:146`) **cố ý bỏ `q`** khỏi domain đếm ⇒ badge lệch khi search (006). Portal in `planned_departure` ở 4 chỗ (`:171/175/300/305`) trong khi `stock.picking.batch.actual_departure` **đã có sẵn** (`wujia_delivery/models/stock_picking_batch.py:130`, set `:246`) (007). `get_upcoming_batches` (`wujia_portal_base/controllers/utils.py:459`) lọc `planned_departure >= đầu hôm nay` nhưng **không loại done/cancel**, và chỉ có nhánh mobile ⇒ **005 và HOME-003 là CÙNG một hàm**. |
| **C6** ✅ 19/08 `c2592b3` Interaction state toàn portal | WJ-PORTAL-UI-001 (97) — Low nhưng blast radius lớn | `wujia_portal_layout` | `_components.css` + `_pc_components.css` dùng chung: bỏ underline cho card/row/menu/button/chip/tab/icon-button (chỉ inline link giữ), hover PC `#EAF7FD`/`#28A9DF` + shadow nhẹ + 150ms, focus-visible outline 2px + offset 2px, mobile pressed. **Tái dùng token `--wujia-primary-border-soft` (B4)**, không đẻ token trùng nghĩa; hover không được làm dịch chuyển layout. Hồi quy rộng như B4 (17 route × 2 breakpoint). **Làm xong 19/08**: gốc rễ là MỘT rule `a:hover` của `web.assets_frontend` nạp sau CSS custom ⇒ layer mới `_interaction.css` nạp cuối; underline 18→0/34→0, ring 46→0/43→0, Tab thật 7/124→124/124, B4 282/286 y hệt. |
| **C7** ✅ 19/08 `b33b46f` Home mobile UI | WJ-HOME-001 (106), 002 (107), 006 (109), 007 (110), 008 (111) — High×1, Med×4 | `wujia_portal_base`, `wujia_portal_layout` | Chạy **sau C5 + C6**. 4 KPI bị ellipsis → BA chốt **ưu tiên giảm font/gap giữ 1 hàng**, fallback 2×2, cấm "…" (001). Mã đơn rút thành "S0…" (002). `_dashboard_values` đang `recent_orders` limit **5**, noti/returns/articles 3 → BA muốn **2** cho mọi block preview (006). Bỏ hết chevron cấp dòng/card, giữ "Xem tất cả", toàn vùng dòng là vùng bấm (007). Mỗi block danh sách = **1 card container + divider**, gộp "Yêu cầu đổi trả" đang tách card rời; Hành động nhanh + KPI giữ layout riêng (008). |
| **C8** SectionHeader `CMP-SH-001` | UI-SECTIONHEADER-001 (83) — Med | `wujia_portal_layout` + ~10 module gọi | Cụm TO NHẤT — làm như B3a/B3b: component + vài route mẫu, đo xong mới nhân ra; không xong thì tách C8a/C8b. Hiện chỉ có 4 class rời (`wujia-mhist-listhead`×4, `wujia-morder-listhead`×2, `wujia-mcart-listhead`, `wj-pc-order-cardhead`), còn lại là `<h2>/<h3>/<h4>` tự do — nhiều nhất `portal_exam.xml` (21), `portal_home.xml` (13), `portal_debt.xml` (12), `portal_franchise_information.xml` (10). Bước 1 của phiên: liệt kê đủ heading rồi **phân loại PageHeader / SectionHeader / CardHeader**. ✅ **C8a xong 22/08** — component + 13/18 call site; ✅ **C8b xong 23/08** — 6 chỗ còn lại (debt×3, exam×2 **kể cả chỗ kiểm kê sót**, return×1) ⇒ **19/19**, CSS rời đã xoá hẳn + test chống tái phát; `wujia_portal_inspection` kiểm kê xong (1 chỗ chờ BA). → `docs/c8-heading-inventory.md`, `docs/c8-acceptance-matrix.md` §6. |
| **C9** Order / History nhỏ | RESP-MOB-ORDER-002 (94), WJ-ORD-026 (95), WJ-PH-008 (96) — High×1, Med×1, Low×1 | `wujia_portal_sale`, `wujia_portal_purchase_history` | CTA "Gửi đơn đặt hàng" y≈722–768 bị Footer Action Bar (y≈761) che ~7px ở 391×844 — thêm safe-area theo **chiều cao footer sẵn có**, đừng gõ số cứng (026). Tên SP mobile chưa dùng hết chiều rộng card (RESP-002). `purchase_history/controllers/portal.py:15 PAGE_SIZE = 20` trong khi source PC v1.5 quy định **10** — nhớ sửa cả nhánh `page_size if page_size != PAGE_SIZE else ''` (`:299`) kẻo querystring đảo nghĩa (PH-008). |
| **C10** Exam + Ngôn ngữ | WJ-EXAM-007 (84), WJ-LANG-001 (114) — Med×2 | `wujia_portal_exam`, `wujia_portal_layout` | `portal_exam.xml:265` hard-code "tối đa 4 người" trong khi quota thật là `max_participants_per_registration` (controller trả `max_per_reg`; JS `portal_exam_pc.js:198` + `portal_exam_wizard.js:144` lại fallback `\|\| 4`) ⇒ 1 nguồn duy nhất cho hướng dẫn + ô quota + chặn nút + validate server (007). Bộ chọn ngôn ngữ hard-code 2 thẻ `<a>` ở `layouts.xml:78-82` + `mobile_header.xml:42-46` ⇒ lấy động, kiểm cả whitelist ở route `/portal/set-lang` (`controllers/portal.py:211`) (LANG-001). |

**3 quyết định chủ dự án chốt 08-14 (đừng hỏi lại):** (1) giữ **10 cụm**, mỗi cụm 1 session 1 lần `-u`; (2) **WJ-DEBT-008 = ẩn hẳn khối QR** theo nhánh OR của BA (gỡ tiêu đề "Quét QR để chuyển khoản", khung QR giả, nhãn "QR minh họa", nút "Tải mã QR"), **KHÔNG** sinh VietQR thật — QR thật mở issue riêng khi BA chốt tĩnh/động (câu hỏi CT-055 treo từ S48); (3) **WJ-DEBT-004 = tách tổng theo từng loại tiền**, mỗi currency 1 dòng ở khối Tổng cộng, **không** quy đổi tỷ giá.

### Chuẩn nghiệm thu mỗi issue — khớp ≥90% acceptance BA (chủ dự án yêu cầu 08-10)

Xong code **chưa phải xong**. Trước khi ghi ledger phải đối chiếu **từng gạch đầu dòng** cột `Kết quả mong muốn` của chính issue đó:
1. Build sạch `-u <mod> --stop-after-init` trên **DB copy cô lập** (port riêng, KHÔNG đụng `wujia_tea_19`/8019) — RC=0, 0 ERROR/Traceback.
2. **Đo bằng máy, không nhìn ảnh** — `scripts/ba_spec/qa_visual_check.py` (Playwright+chromium có sẵn env `odoo`), 391×844 / 1920×1080. Gotcha §12: `wait_until="load"`, login submit bằng Enter, set sẵn cookie `wujia_active_franchise_id` (overlay che ⇒ assert **pass rỗng**).
3. Bảng đối chiếu `Yêu cầu | Đo được | Pass/Fail`; **ngưỡng ≥90% Pass**, dòng Fail phải sửa hoặc ghi **LIMIT tường minh**.
4. Regression ≥3 trang khác × 2 viewport: 200, overflow ngang 0, 0 JS pageerror.
5. Harness sai thì sửa harness (L7/L9) — chân lý là spec BA/mockup, không phải kỳ vọng gõ tay.
6. Đạt rồi mới ledger → `qa_sync.py --dry-run` → `--apply` (`Ready for Retest`, **Dev không tự Done**), verify `export?format=csv`.

### Quy ước code khi fix (chủ dự án 08-10: "code hơi phình, ít comment thôi")

- 🔴 **Chốt 15/08/2026 — KHÔNG để phình code.** Fix bug là sửa **đúng chỗ gốc**, không copy-paste 3 bản cho 3 model — tách một helper dùng chung. Không viết thêm field/model/file khi cái sẵn có đủ dùng. Không đẻ file doc/harness rác trong repo (harness đo để `scratchpad/`). Comment **ít thôi**: chỉ ghi *tại sao* ở chỗ thật sự khó đoán, không ghi *cái gì* (code tự nói), không docstring dài lê thê, không comment kể lịch sử sprint.
- **Tái dùng trước khi viết mới**: `to_local_dt`/`portal_tz` (§11), regex phone PC, `wj_page_header`, `store_picker_modal`, `wj_ajax_list`.
- Comment **1 dòng đủ ý**; xoá comment kể lịch sử sprint (vd `store_picker_navbar.xml` đang có block 10 dòng).
- Không thêm CSS mới khi modifier hiện có đủ; không hex cứng, dùng `var(--wujia-*)`.
