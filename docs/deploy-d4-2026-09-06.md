# Deploy cụm D4 lượt 2 (D4e1 + D4e2 + D4f) lên UAT — 2026-09-06

`main` = merge `cbd9b21` (nhánh `dev/2026-09-05-d4c` + `origin/main` 084f7b3). Cụm SurfaceCard
`CMP-SC-001` phủ **144/384 lượt ≈ 37%**; issue `UI-SURFACECARD-001` (STT 127) **vẫn `Ready for Dev`**.

## Vì sao có lượt này

Đo chỉ-đọc 06/09 09:50: UAT = **D4d + `20b6de1`** (layout `19.0.35.0.0`, 13 module hậu tố `.1`,
md5 3 file CSS khớp D4d sau khi bỏ CRLF). D4e1/e2/f được commit trên máy Linux 00:21–09:37 06/09
từ gốc D4d **mà chưa fetch**, trong khi máy Mac đã merge D4d + vá deploy vào `main` tối 05/09.
Hai bên rẽ nhánh 4/4 từ đúng `5758495`; conflict chỉ ở `assets.xml`, 4 file docs, 15 manifest.

## Quy ước số phiên bản sau merge

- Lấy số **lớn hơn** của hai bên, rồi **bump patch thêm 1** cho mọi module D4e1/e2/f có đụng ⇒ 9 module
  `.1 → .2`; `wujia_portal_layout` giữ `19.0.38.0.0` (> 35.0.0).
- `assets.xml`: `_wujia_theme` / `_components` / `_pc_components` **`?v=1220`**, `_variables` `?v=1200`
  (khớp UAT, D4e/f không đổi file này).

## Lệnh deploy

```
git pull
python odoo-bin -c <conf> -d wujia_tea_19 --stop-after-init -u \
wujia_portal_layout,wujia_portal_base,wujia_portal_exam,wujia_portal_info_request,\
wujia_portal_inspection,wujia_portal_knowledge,wujia_portal_report,wujia_portal_return,\
wujia_portal_sale,wujia_portal_support,wujia_sale
```

**Không** module mới ⇒ không `-i`. **Không** migration. `wujia_sale` đi kèm vì `-u` chạm
`wujia_portal_return` (rename `description_ecommerce`, §5 compact summary).

Đã kiểm trên bản sao `wujia_tea_d4g` (clone `wujia_tea_d4c`, có `wujia_portal_inspection` installed
như UAT): `-u` 11 module **RC=0, 0 ERROR / 0 CRITICAL** (log thật 539 dòng ở thư mục ngày, không phải
47 dòng đầu — L15); `--test-tags wujia_surface_card_d4` **74 test, 0 failed / 0 error** — 2 lỗi đỏ
có sẵn của D4f (do inspection uninstalled trên DB dev) đã hết.

## Kiểm sau deploy (đo chỉ-đọc, L14/L10)

1. XML-RPC `ir.module.module`: `wujia_portal_layout` = `19.0.38.0.0`, 9 module còn lại đúng số `.2`,
   `installed_version == latest_version`.
2. HTML portal có `_components.css?v=1220`; giá trị **chỉ có sau D4f**: thẻ `/portal/support/<id>` viền
   `#EEF2F5` (trước là `rgba(0,0,0,.176)` của Odoo), `box-shadow: none`, số thẻ `.card` hiện = **0**.
3. Chạy harness đo trên chính UAT (`docs/d4f-acceptance-matrix.md` §13) rồi mới `qa_deploy_mark.py`.
