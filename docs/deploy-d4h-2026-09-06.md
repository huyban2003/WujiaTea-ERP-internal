# Deploy cụm D4 lượt 3 (D4h — nhóm Khảo sát) lên UAT — 2026-09-06

`main` = `7eb5d23` (nhánh `dev/2026-09-06-d4h`). Cụm SurfaceCard `CMP-SC-001` **khép**: 151/377 token
đã đi qua phân loại, không còn họ khung chưa có chủ. Issue `UI-SURFACECARD-001` (STT 127, dòng 120) chuyển
`Ready for Retest` **chỉ khi** chủ dự án chốt `qa_sync.py --apply`.

## Có gì trong lượt này

- `wujia_portal_inspection` **19.0.1.3.2 → 19.0.1.4.0**: 4 view (list mobile, detail PC + mobile, 4 hộp
  remediation) nướng shell `wj-surface-card`; `portal_inspection.css` bỏ dáng khung của 3 họ.
- `wujia_portal_layout` **không đổi** (19.0.38.0.0, `?v=1220` giữ nguyên) — chỉ thêm 9 test vào
  `tests/test_d4_surface_card.py`, không cần `-u` layout trên UAT.

## Lệnh deploy

```
git pull
python odoo-bin -c <conf> -d wujia_tea_19 --stop-after-init -u wujia_portal_inspection
```

Không `-i`, không migration. CSS module đi qua `web.assets_frontend` nên bundle tự đổi hash sau `-u`.

Đã kiểm trên bản sao `wujia_tea_d4g` (có inspection installed như UAT): `-u wujia_portal_layout,
wujia_portal_inspection --test-tags wujia_surface_card_d4` **RC=0, 0 ERROR, 83 test 0 failed**;
15/15 đột biến bị bắt (`docs/d4g-acceptance-matrix.md` §8).

## Kiểm sau deploy (đo chỉ-đọc)

1. XML-RPC `ir.module.module`: `wujia_portal_inspection` `latest_version == installed_version == 19.0.1.4.0`.
2. `/portal/inspection` (mobile 390/360), `/portal/inspection/detail/<id>` (1440 + 390),
   `/portal/inspection/remediation/<line_id>` (390): shell `1px #E5E7EB`·14px·shadow **none** mobile,
   ô tổng PC viền `#28A9DF` 1px · 16px; 0 phần tử `.card`; không tràn ngang.
3. Đếm lại bằng `scratchpad/d4h_inv.py` trỏ BASE UAT + admin + cookie cửa hàng (cần id phiếu thật của HCM-01).
4. Sau đó `qa_deploy_mark.py UI-SURFACECARD-001 --apply` lần nữa nếu cột P đang ghi "Chờ deploy UAT".
