# Sprint 9.7 — UI-07 Sidebar: logo area + menu align đồng nhất

Em tiếp tục Sprint 9. **Sprint con 9.7 = UI-07**.

## BA spec exact (sheet "5. Issue List" cột G + H — xlsm 2026-05-22)

- **Khu vực:** Sidebar
- **Vấn đề (cột E):** Khoảng cách từ logo đến menu hơi lớn / không đồng đều giữa các màn hình.
- **Đề xuất điều chỉnh (cột G):** Chuẩn sidebar: **logo area height khoảng 180–220px**, menu bắt đầu cùng vị trí trên mọi page.
- **Kết quả mong muốn (cột H):** Sidebar không bị page này cao page kia thấp.

**=> Cố định:** `.navbar-header` (logo wrapper) height = 200px (mid range), menu top-position = constant.

## Bước verify trước khi code

1. Mở xlsm row UI-07 → confirm cột G + H verbatim.
2. Xem token hiện tại:
   ```bash
   grep -n "sidebar-logo-h\|navbar-header" custom/wujia_portal_layout/static/assets/css/_variables.css
   grep -rn "navbar-header\|sidebar-header" custom/wujia_portal_layout/static/assets/css/
   ```
   Hiện tại `--wujia-sidebar-logo-h: 200px` đã có nhưng có thể chưa apply tới `.navbar-header`.
3. Inspect DevTools 3 page: `/portal`, `/portal/order`, `/portal/support` — đo height `.navbar-header` — có bằng nhau không?

## File chạm

- `custom/wujia_portal_layout/static/assets/css/_wujia_theme.css`:
  - `.main-menu .navbar-header { height: var(--wujia-sidebar-logo-h) !important; }` (override Bootstrap `mb-5`).
  - `.main-menu .navbar-header img { max-height: calc(var(--wujia-sidebar-logo-h) - 40px); object-fit: contain; }`.
  - Reset `margin-bottom` về consistent value.
- (kiểm tra UI-02 đã xoá user-pic block — nếu chưa, sprint con này không tiến được).

## Workflow

1. Read xlsm verify spec.
2. DevTools đo 3 page (báo cáo anh).
3. Plan ngắn → anh approve.
4. Edit `_wujia_theme.css` → `bash scripts/upgrade.sh "wujia_portal_layout"` RC=0.
5. Restart Odoo → screenshot 3 page sidebar.
6. Overlay 3 ảnh (hoặc đo top-position menu item đầu tiên) — bằng nhau ± 1px.
7. Anh OK → §9 bảng UI-07 ⬜ → ✅.

## Out-of-scope

- KHÔNG đụng menu item style (UI-01 done).
- KHÔNG đụng user info (UI-02 done).
- KHÔNG tạo `.po`.

## Done khi

- `.navbar-header` height = 200px constant.
- Top-position menu item đầu tiên bằng nhau mọi page.
- Screenshot 3 page.
- §9 bảng updated.
