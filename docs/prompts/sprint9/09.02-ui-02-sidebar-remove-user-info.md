# Sprint 9.2 — UI-02 Sidebar: bỏ block thông tin user

Em tiếp tục Sprint 9 (Issue List UI refactor). UI-01 đã DONE local. Em làm **Sprint con 9.2 = UI-02**.

## BA spec exact (sheet "5. Issue List" cột G + H — xlsm 2026-05-22)

- **Khu vực:** Sidebar
- **Vấn đề (cột E):** **Bỏ phần thông tin user** tại sidebar.
- **Đề xuất điều chỉnh (cột G):** (BA không ghi — implicit theo cột E)
- **Kết quả mong muốn (cột H):** (BA không ghi — implicit theo cột E)

**=> Action duy nhất: xoá block hiển thị user-pic + user-name trong sidebar.** Không thay thế bằng gì khác. Profile info đã có ở dropdown avatar header trên cùng (UI-03 sẽ refactor sau).

## Bước verify trước khi code

1. Mở `WujiaTea/docs/Wujia_Internal ERP Master Plan.xlsm` sheet "5. Issue List" → row UI-02 → confirm cột G/H vẫn rỗng (BA có thể đã update sau 2026-05-22). Nếu khác → DỪNG hỏi anh.
2. `grep -n "sidebar-header\|user-pic\|user-info\|user-name" custom/wujia_portal_layout/views/layouts.xml`

## File chạm

- `custom/wujia_portal_layout/views/layouts.xml` — xoá block `<div class="sidebar-header mt-3">…</div>` (currently lines 136-147).

## Workflow

1. Read xlsm verify spec (bước 1 trên).
2. Read `layouts.xml` xác định block cần xoá.
3. Đề xuất diff ngắn cho anh approve.
4. Edit → `bash scripts/upgrade.sh "wujia_portal_layout"` → expect RC=0.
5. Restart Odoo → mở `/portal` → screenshot sidebar.
6. Kiểm tra: KHÔNG còn user-pic, KHÔNG còn user-name trong sidebar. Logo + menu sát nhau hơn.
7. Anh OK → cập nhật `wujia-compact-summary.md` §9 bảng: UI-02 ⬜ → ✅ + ngày + Files đã chạm.

## Out-of-scope

- KHÔNG đụng header avatar dropdown (UI-03 sẽ làm).
- KHÔNG đụng logo area sidebar (UI-07 sẽ làm).
- KHÔNG tạo `.po`.

## Done khi

- Sidebar `/portal` không còn block user info.
- Upgrade RC=0, screenshot lưu lại.
- §9 bảng updated.
