# Sprint 9.17 — Push: commit Conventional EN + push main + deploy Windows

Em tiếp tục Sprint 9. **Sprint con 9.17 = Push & deploy** (cuối Sprint 9).

## Bước verify TRƯỚC khi commit

1. `git status` — review tất cả file changed.
2. `git diff --stat` — confirm chỉ Sprint 9 files (custom/wujia_portal_*, docs/chapters/18, 19, compact summary, scripts/test_sprint9.py).
3. Verify hooks pass: `git diff --check` (whitespace), `pre-commit run --all-files` nếu có.
4. Lần cuối: `bash scripts/reseed_full.sh` RC=0 (đã chạy Sprint 9.15 — confirm chưa break gì sau viết doc).

## Commit message (Conventional Commits, English)

```
feat(portal): Sprint 9 — Issue List UI refactor (13 issues + Empty state)

Implement BA sheet "5. Issue List" 13 portal UI issues per spec verbatim.

* UI-01: active sidebar item white text + icon on primary BG
* UI-02: remove sidebar user info block (moved to header avatar dropdown)
* UI-03: header (PC) Current Store block — [code] name + role badge + lang + avatar
* UI-04: header (mobile) Current Store responsive collapse
* UI-05: standardize Primary (h 42) + Secondary (white BG + grey border, h 38) buttons
* UI-06: unify page background #F5F7FA / card #FFFFFF
* UI-07: sidebar logo area fixed 200px — menu top-position constant across pages
* UI-09: header height 76 + padding-x 28 + align-items center
* UI-11: font Inter (resolved) + size hierarchy body 15 / h1 32 / card 20 / table 15
* UI-12: card border 1px #E5E7EB + radius 14 + shadow var(--wujia-card-shadow)
* UI-14: replace raw status text with .wujia-badge soft pills (6 portal pages)
* UI-15: responsive 2-pane — stack on tablet/mobile via .wujia-two-pane class
* Empty state: unified .wujia-empty-state (icon + text + spacing) across list pages
* Cleanup: 301 redirects for legacy v14 slugs + remove orphan wujia_account stub

Replace fa fa-* solid icons with feather icon-* outline across 11 sidenav_inherit.xml
+ layouts.xml. Role label EN ('Owner'/'Manager'/'Staff') for header role badge.

Verify: scripts/reseed_full.sh RC=0; scripts/test_sprint9.py 13 routes + 13 CSS + 3
redirects PASS; 8 screenshots in docs/screenshots/sprint9/.

Docs: chapters/18-sprint9-issue-list-ui-refactor.tex (13 sub-sections),
chapters/19-roadmap-v14-gaps.tex (deferred affiliate/dashboard/TOTP).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

## File chạm

- (commit only — không edit code mới)

## Workflow

1. Verify 4 bước trên — báo cáo anh trước khi `git add`.
2. `git add custom/wujia_portal_*/ custom/wujia_franchise/ docs/chapters/18-sprint9-issue-list-ui-refactor.tex docs/chapters/19-roadmap-v14-gaps.tex docs/wujia-tea-doc.tex docs/wujia-tea-doc.pdf docs/wujia-compact-summary.md docs/prompts/ docs/screenshots/sprint9/ scripts/test_sprint9.py scripts/reseed_full.sh` (selective — không `-A`).
3. `git commit -m "<heredoc message>"` — NO `--no-verify`, NO bypass.
4. Anh OK → `git push origin main`.
5. **Deploy command (anh chạy trên Windows server):**
   ```powershell
   nssm stop Odoo
   cd D:\wujia-tea
   git pull
   .\scripts\upgrade.ps1 wujia_portal_layout
   .\scripts\upgrade.ps1 wujia_portal_base
   .\scripts\upgrade.ps1 wujia_franchise
   .\scripts\upgrade.ps1 wujia_portal_purchase_history
   .\scripts\upgrade.ps1 wujia_portal_return
   .\scripts\upgrade.ps1 wujia_portal_exam
   .\scripts\upgrade.ps1 wujia_portal_support
   .\scripts\upgrade.ps1 wujia_portal_knowledge
   .\scripts\upgrade.ps1 wujia_portal_info_request
   .\scripts\upgrade.ps1 wujia_portal_notification
   .\scripts\upgrade.ps1 wujia_portal_sale
   .\scripts\upgrade.ps1 wujia_portal_delivery
   .\scripts\upgrade.ps1 wujia_portal_report
   nssm start Odoo
   ```
   (`reseed_full.ps1` KHÔNG cần — Sprint 9 chỉ CSS/XML/Python label, không schema change.)
6. Anh confirm prod OK → §9 bảng push ⬜ → ✅. Sprint 9 COMPLETE.

## Out-of-scope

- KHÔNG force push.
- KHÔNG amend trước commit.
- KHÔNG bypass hook (`--no-verify`).
- KHÔNG `reseed_full.ps1` trên prod (mất data).

## Done khi

- `main` có commit Sprint 9 push pass.
- Windows server `git pull` + `upgrade.ps1` toàn bộ 13 module RC=0.
- Anh kiểm tra `https://wujia-prod.<domain>/portal` UI khớp dev.
- §9 bảng push ⬜ → ✅. Toàn Sprint 9 17/17 ✅.
