# Sprint 9.3 — UI-03 Header (PC): block Current Store

Em tiếp tục Sprint 9. **Sprint con 9.3 = UI-03**.

## BA spec exact (sheet "5. Issue List" cột G + H — xlsm 2026-05-22)

- **Khu vực:** Header (PC)
- **Vấn đề (cột E):** Xây dựng lại phần hiển thị thông tin cửa hàng trên thanh header (PC).
- **Đề xuất điều chỉnh (cột G):** (BA không ghi)
- **Kết quả mong muốn (cột H):** Header nên có block **Current Store** rõ ràng: **[H000] Cửa hàng 125 Điện Biên Phủ**, **role badge**, **language**, **avatar**.

**=> 4 element trên header PC:** Current Store ([code] tên) | role badge | language dropdown | avatar dropdown.

## Bước verify trước khi code

1. Mở `WujiaTea/docs/Wujia_Internal ERP Master Plan.xlsm` sheet "5. Issue List" → row UI-03 → confirm cột H verbatim trùng prompt. Nếu BA edit sau 2026-05-22 → DỪNG hỏi anh.
2. Xem mockup col F: `WujiaTea/docs/Wujia_Internal ERP Master Plan.xlsm` có 12 ảnh anchored sheet này — extract qua openpyxl image28-29 (Current Store mockup).
3. Grep model active franchise:
   ```bash
   grep -rn "active_franchise\|franchise_id\|get_active_franchise" custom/wujia_portal_base/controllers/
   grep -rn "franchise_code\|name\|address" custom/wujia_franchise/models/wujia_franchise_management.py
   ```
4. Grep role helper:
   ```bash
   grep -rn "role\|has_role\|ROLE_" custom/wujia_franchise/models/wujia_franchise_member.py
   ```

## File chạm (dự kiến)

- `custom/wujia_portal_layout/views/layouts.xml` — template `layout_top_navbar` (currently lines 32-117): thêm block Current Store + role badge trước language dropdown.
- (có thể) `custom/wujia_franchise/models/wujia_franchise_member.py` — đổi label `Selection` role từ tiếng Việt sang **English** ('Owner', 'Manager', 'Staff') để hiển thị badge English.

## Workflow

1. Read xlsm verify spec.
2. Grep model + helper xác định cách lấy active franchise + role của user.
3. Đề xuất plan: HTML block structure + cách gọi `request.env` lấy data + class CSS dùng `.wujia-badge-*` (`wujia_portal_layout/static/assets/css/_components.css`).
4. Anh approve → edit.
5. `bash scripts/upgrade.sh "wujia_portal_layout,wujia_franchise"` RC=0.
6. Restart Odoo → mở `/portal` (PC viewport 1280+) → screenshot.
7. Kiểm tra mockup col F: [code] tên cửa hàng | role badge | language | avatar — cùng trục giữa (UI-09 sẽ chuẩn hoá thêm sau).
8. Anh OK → §9 bảng UI-03 ⬜ → ✅.

## Out-of-scope

- KHÔNG làm mobile responsive (UI-04 riêng).
- KHÔNG đụng align center / header height (UI-09 riêng).
- KHÔNG đụng button style (UI-05 riêng).
- KHÔNG tạo `.po`.

## Done khi

- Header PC hiển thị: [H000] tên + role badge + language + avatar.
- Role label English ('Owner'/'Manager'/'Staff').
- Upgrade RC=0, screenshot khớp mockup col F (anchored sheet).
- §9 bảng updated.
