# Sprint 9.8 — UI-09 Header: height + padding + align center

Em tiếp tục Sprint 9. **Sprint con 9.8 = UI-09**.

## BA spec exact (sheet "5. Issue List" cột G + H — xlsm 2026-05-22)

- **Khu vực:** Header
- **Vấn đề (cột E):** Header chưa có height/padding thống nhất.
- **Đề xuất điều chỉnh (cột G):** Set header **height khoảng 72–80px**, padding **trái/phải 24–32px**, **align center** toàn bộ item.
- **Kết quả mong muốn (cột H):** Avatar, language, store, role nằm cùng trục giữa.

**=> 3 thuộc tính:** height 72-80 + padding-x 24-32 + align-items: center.

## Bước verify trước khi code

1. Mở xlsm row UI-09 → confirm cột G + H verbatim.
2. Token hiện tại:
   ```bash
   grep -n "header-height\|header-padding" custom/wujia_portal_layout/static/assets/css/_variables.css
   ```
   Hiện: `--wujia-header-height: 76px` (mid range 72-80 ✓) + `--wujia-header-padding: 28px` (mid range 24-32 ✓). Token đúng — verify apply.
3. Inspect `/portal`:
   ```css
   .wujia-navbar { min-height: ?; padding-left: ?; align-items: ? }
   .wujia-navbar > * { align-self: center? }
   ```

## File chạm

- `custom/wujia_portal_layout/static/assets/css/_wujia_theme.css`:
  - `.wujia-navbar { display: flex; align-items: center; }` (đảm bảo cha flex + center).
  - `.wujia-navbar .navbar-wrapper, .wujia-navbar .navbar-container { display: flex; align-items: center; }`.
  - Áp dụng `align-items: center` cho `.navbar-collapse > *` (mr-auto + float-right blocks).
- (verify dùng `--wujia-header-height` + `--wujia-header-padding` thực tế).

## Workflow

1. Read xlsm verify spec.
2. DevTools đo current header height/padding/align (báo cáo anh).
3. Plan ngắn → anh approve.
4. Edit + `bash scripts/upgrade.sh "wujia_portal_layout"` RC=0.
5. Restart Odoo → mở `/portal` PC + mobile → screenshot.
6. Overlay 4 element (avatar, language, store, role badge) — y-center align ± 1px.
7. Anh OK → §9 bảng UI-09 ⬜ → ✅.

## Out-of-scope

- KHÔNG đụng Current Store block structure (UI-03/04 done).
- KHÔNG tạo `.po`.

## Done khi

- Header 76px height, 28px padding-x, items center-aligned.
- Screenshot PC + mobile, overlay check pass.
- §9 bảng updated.
