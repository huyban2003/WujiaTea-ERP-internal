# Sprint 9.4 — UI-04 Header (mobile): block Current Store responsive

Em tiếp tục Sprint 9. **Sprint con 9.4 = UI-04**.

## BA spec exact (sheet "5. Issue List" cột G + H — xlsm 2026-05-22)

- **Khu vực:** Header (mobile)
- **Vấn đề (cột E):** Xây dựng lại phần hiển thị thông tin cửa hàng trên thanh header (mobile).
- **Đề xuất điều chỉnh (cột G):** (BA không ghi)
- **Kết quả mong muốn (cột H):** Header nên có block Current Store rõ ràng: [H000] Cửa hàng…, role badge, language, avatar.

**=> Cùng spec UI-03 nhưng đảm bảo hiển thị OK trên mobile (≤ 575.98px) + tablet (576-991.98px).**

## Bước verify trước khi code

1. Mở `WujiaTea/docs/Wujia_Internal ERP Master Plan.xlsm` sheet "5. Issue List" → row UI-04 → confirm cột H verbatim. Nếu BA edit sau 2026-05-22 → DỪNG hỏi anh.
2. Xem mockup col F (mobile mockup — image số khác image28-29 PC).
3. Test viewport: 360px, 414px, 768px.

## File chạm

- `custom/wujia_portal_layout/static/assets/css/_components.css` — bổ sung media query cho block Current Store (đã thêm ở UI-03).
- (có thể) `custom/wujia_portal_layout/views/layouts.xml` — bọc nhóm element bằng class collapse/dropdown cho mobile.

## Workflow

1. Read xlsm verify spec.
2. Đặt breakpoint:
   - `≤ 575.98px`: Current Store rút gọn thành `[H000]` (ẩn tên dài) + role badge mini + avatar; language vào dropdown avatar.
   - `576-991.98px`: hiện đủ.
   - `≥ 992px`: full (UI-03).
3. Plan ngắn → anh approve.
4. Edit CSS + (nếu cần) HTML wrapper class.
5. `bash scripts/upgrade.sh "wujia_portal_layout"` RC=0.
6. Restart Odoo → mở `/portal` ở 3 viewport → screenshot 3 ảnh.
7. So với mockup col F mobile → 100% khớp.
8. Anh OK → §9 bảng UI-04 ⬜ → ✅.

## Out-of-scope

- KHÔNG đụng sidebar mobile drawer (đã có).
- KHÔNG đụng detail panel responsive (UI-15 riêng).
- KHÔNG tạo `.po`.

## Done khi

- 3 viewport hiển thị đúng Current Store.
- Upgrade RC=0, 3 screenshot.
- §9 bảng updated.
