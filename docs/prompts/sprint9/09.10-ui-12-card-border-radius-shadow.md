# Sprint 9.10 — UI-12 Card: border + radius + shadow thống nhất

Em tiếp tục Sprint 9. **Sprint con 9.10 = UI-12**.

## BA spec exact (sheet "5. Issue List" cột G + H — xlsm 2026-05-22)

- **Khu vực:** Card
- **Vấn đề (cột E):** Card Home và Support chưa cùng radius/shadow/border.
- **Đề xuất điều chỉnh (cột G):** Chuẩn card: **border 1px solid #E5E7EB**, **radius 12–16px**, **shadow rất nhẹ hoặc không shadow**.
- **Kết quả mong muốn (cột H):** Card toàn portal nhìn cùng hệ.

**=> 3 thuộc tính:** border 1px #E5E7EB + radius 12-16 + shadow rất nhẹ/không.

## Bước verify trước khi code

1. Mở xlsm row UI-12 → confirm cột G + H verbatim.
2. Token hiện tại:
   ```bash
   grep -n "card-radius\|card-shadow\|wujia-border" custom/wujia_portal_layout/static/assets/css/_variables.css
   ```
   Hiện: `--wujia-card-radius: 14px` (✓ mid range) + `--wujia-border: #E5E7EB` (✓) + `--wujia-card-shadow: 0 2px 6px rgba(15,23,42,0.04)` (rất nhẹ — ✓).
3. Apply check:
   ```bash
   grep -rn ".card {" custom/wujia_portal_layout/static/assets/css/
   grep -rn "border-radius\|box-shadow" custom/*/static/src/css/ custom/*/static/assets/css/ | grep -v _variables.css
   ```
4. Inspect DevTools 2 page key: `/portal` (Home) + `/portal/support` — đo `.card { border-radius, border, box-shadow }` — có bằng nhau không?

## File chạm

- `custom/wujia_portal_layout/static/assets/css/_wujia_theme.css`:
  - `.card { border-radius: var(--wujia-card-radius); border: 1px solid var(--wujia-border); box-shadow: var(--wujia-card-shadow); }` — verify duy nhất rule này áp .card, không có override khác.
- `custom/wujia_portal_layout/static/assets/css/_components.css` line 14: hiện có `.card { box-shadow: var(--wujia-card-shadow); }` — gộp về `_wujia_theme.css` cho gọn.
- Nếu module nào override `.card` riêng (Home dashboard, Support detail) → xoá / dùng class extension thay vì re-rule.

## Workflow

1. Read xlsm verify spec.
2. Grep audit + DevTools đo 2 page (báo cáo anh).
3. Plan: ưu tiên gộp rule về `_wujia_theme.css`, xoá duplicate.
4. Anh approve → edit + `bash scripts/upgrade.sh "wujia_portal_layout"` (+ module override nếu có) RC=0.
5. Restart Odoo → screenshot Home + Support card.
6. Side-by-side: border + radius + shadow giống hệt.
7. Anh OK → §9 bảng UI-12 ⬜ → ✅.

## Out-of-scope

- KHÔNG đụng card-body padding (đã có `--wujia-card-padding: 24px`).
- KHÔNG đụng card-header (Sprint 4.3 flush convention giữ nguyên).
- KHÔNG đụng badge (UI-14 riêng).
- KHÔNG tạo `.po`.

## Done khi

- 1 rule `.card` duy nhất trong `_wujia_theme.css`, dùng 3 var.
- 2 page Home + Support side-by-side identical.
- §9 bảng updated.
