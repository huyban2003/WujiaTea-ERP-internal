<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Portal user: chỉ xem yêu cầu của franchise mình access. -->
        <record id="rule_info_request_portal_read" model="ir.rule">
            <field name="name">Info Update Request: portal sees accessible franchise</field>
            <field name="model_id" ref="model_wujia_info_update_request"/>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
            <field name="domain_force">
                [('franchise_id', 'in', user._get_accessible_franchise_ids())]
            </field>
        </record>

        <!-- Portal user: chỉ write yêu cầu của mình tự tạo (state=draft). -->
        <record id="rule_info_request_portal_write" model="ir.rule">
            <field name="name">Info Update Request: portal writes own draft</field>
            <field name="model_id" ref="model_wujia_info_update_request"/>
            <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
            <field name="perm_read" eval="False"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="False"/>
            <field name="domain_force">
                [('created_by_user_id', '=', user.id)]
            </field>
        </record>

        <!-- Internal user: full. -->
        <record id="rule_info_request_internal" model="ir.rule">
            <field name="name">Info Update Request: internal user sees all</field>
            <field name="model_id" ref="model_wujia_info_update_request"/>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="domain_force">[(1, '=', 1)]</field>
        </record>

    </data>
</odoo>
