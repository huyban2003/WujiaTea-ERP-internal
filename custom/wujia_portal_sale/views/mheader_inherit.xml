<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- F5a — nút tắt tới giỏ hàng ở header mobile: route /portal/order/cart là của
         module này. Badge dùng lại .wujia-header-cart-count (header_cart_badge.js).
         Markup giữ nguyên bản Sprint 12 (UI-04: SVG stroke 2.4 thay icon font). -->
    <template id="mobile_header_cart"
              inherit_id="wujia_portal_layout.mobile_header"
              priority="20"
              name="Wujia Mobile Header — nút giỏ hàng">
        <xpath expr="//div[hasclass('dropdown-user')]" position="before">
            <a class="wujia-mheader-action" href="/portal/order/cart" aria-label="Giỏ hàng">
                <svg class="wujia-mheader-action-svg" xmlns="http://www.w3.org/2000/svg"
                     viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4"
                     stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                    <circle cx="9" cy="21" r="1"/>
                    <circle cx="20" cy="21" r="1"/>
                    <path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/>
                </svg>
                <!-- WJ-ORD-020: `hidden` thay cho việc render sẵn "0" rồi trông chờ CSS ẩn. -->
                <span class="wujia-header-badge wujia-header-cart-count" hidden="hidden"/>
            </a>
        </xpath>
    </template>

</odoo>
