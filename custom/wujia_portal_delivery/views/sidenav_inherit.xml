<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- F5a — mục sidebar của màn do CHÍNH module sở hữu route khai; khung
         (wujia_portal_layout) chỉ giữ tiêu đề nhóm làm neo. Gỡ module này ⇒ mục biến mất. -->
    <template id="layout_sidenav_delivery"
              inherit_id="wujia_portal_layout.layout_sidenav"
              priority="40"
              name="Wujia Sidenav — Giao hàng">
        <xpath expr="//li[@id='nav_header_utils']" position="before">
            <li t-attf-class="nav-item #{_p.startswith('/portal/delivery') and 'active' or ''}" id="nav_item_delivery">
                <t t-call="wujia_portal_layout.wj_nav_item">
                    <t t-set="ni_href" t-value="'/portal/delivery'"/>
                    <t t-set="ni_icon" t-value="'feather icon-truck'"/>
                    <t t-set="ni_label">Giao hàng</t>
                </t>
            </li>
        </xpath>
    </template>

</odoo>
