<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="portal_return_form" name="Wujia Portal — Return Form">
        <t t-call="wujia_portal_layout.app_layout">
            <t t-set="title">Tạo yêu cầu bù hàng</t>

            <!-- Desktop ≥992px -->
            <div class="content-wrapper d-none d-lg-block">
                <t t-call="wujia_portal_layout.wj_page_header">
                    <t t-set="ph_platform" t-value="'pc'"/>
                    <t t-set="ph_variant" t-value="'back'"/>
                    <t t-set="ph_crumb">Bù hàng / Tạo yêu cầu bù hàng</t>
                    <t t-set="ph_title">Tạo yêu cầu bù hàng</t>
                    <t t-set="ph_back_url" t-value="'/portal/return'"/>
                </t>

                <div class="content-body">
                    <p t-if="error" class="alert alert-danger" t-out="error"/>
                    <form method="post" action="/portal/return/new" data-wj-dirty-guard="1"
                          enctype="multipart/form-data" class="card wujia-return-form">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <div class="alert alert-info mb-0 rounded-0">
                            <i class="feather icon-info me-1"/>
                            Mỗi yêu cầu chỉ áp dụng cho <b>1 sản phẩm</b> trong đơn đặt.
                        </div>
                        <div class="card-body">
                            <div class="row g-2">
                                <div class="col-md-6 col-12">
                                    <label class="form-label">Cửa hàng</label>
                                    <select name="franchise_id" class="form-select" required="required">
                                        <t t-foreach="franchises" t-as="f">
                                            <option t-att-value="f.id" t-out="f.name"/>
                                        </t>
                                    </select>
                                </div>
                                <div class="col-md-6 col-12">
                                    <label class="form-label">Loại lỗi</label>
                                    <select name="issue_type_id" class="form-select" required="required">
                                        <option value="">— Chọn loại lỗi —</option>
                                        <t t-foreach="issue_types" t-as="it">
                                            <option t-att-value="it.id" t-out="it.name"/>
                                        </t>
                                    </select>
                                </div>
                                <div class="col-md-6 col-12">
                                    <label class="form-label">Đơn hàng gốc</label>
                                    <select name="sale_order_id" class="form-select wj-return-order"
                                            t-att-data-lines="order_lines_json" required="required">
                                        <option value="">— Chọn đơn —</option>
                                        <t t-foreach="orders" t-as="o">
                                            <option t-att-value="o.id">
                                                <t t-out="o.name"/> — <t t-out="wj_dt(o.date_order, '%d/%m/%Y')"/>
                                            </option>
                                        </t>
                                    </select>
                                    <small t-if="not orders" class="text-danger">
                                        Không tìm thấy đơn hàng đủ điều kiện trong
                                        <t t-out="window_days"/> ngày gần nhất.
                                    </small>
                                </div>
                                <div class="col-md-6 col-12">
                                    <label class="form-label">Sản phẩm (trong đơn)</label>
                                    <select name="sale_order_line_id" class="form-select wj-return-line" required="required">
                                        <option value="">— Chọn đơn trước —</option>
                                    </select>
                                </div>
                                <div class="col-md-6 col-12">
                                    <label class="form-label">Số lượng yêu cầu</label>
                                    <input type="number" name="request_qty" class="form-control"
                                           min="0.01" step="0.01" value="1" required="required"/>
                                </div>
                                <div class="col-md-6 col-12">
                                    <label class="form-label">Thời gian mở hàng</label>
                                    <input type="datetime-local" name="opening_datetime"
                                           class="form-control" required="required"/>
                                </div>
                                <div class="col-md-6 col-12">
                                    <label class="form-label">Ngày sản xuất</label>
                                    <input type="date" name="production_date" class="form-control"/>
                                </div>
                                <div class="col-12">
                                    <label class="form-label">Ghi chú</label>
                                    <textarea name="note" rows="3" class="form-control"
                                              placeholder="Mô tả chi tiết tình trạng hàng cần bù/đổi..."/>
                                </div>
                                <div class="col-12">
                                    <label class="form-label">
                                        Ảnh minh chứng (<t t-out="min_images"/>–<t t-out="max_images"/> ảnh)
                                    </label>
                                    <input type="file" name="images" class="form-control"
                                           accept="image/jpeg,image/png" multiple="multiple"/>
                                    <small class="text-muted">
                                        JPG/PNG, tối đa 5MB mỗi ảnh · tổng minh chứng ≤ <t t-out="max_total_mb"/>MB.
                                    </small>
                                    <div id="image-preview-grid" class="row g-2 mt-2"></div>
                                </div>
                                <div class="col-12">
                                    <label class="form-label">Video minh chứng (không bắt buộc)</label>
                                    <input type="file" name="video" class="form-control"
                                           accept="video/mp4,video/quicktime"/>
                                    <small class="text-muted">
                                        Tối đa 1 video MP4/MOV, ≤ <t t-out="max_video_mb"/>MB.
                                    </small>
                                </div>
                            </div>
                        </div>
                        <div class="card-footer text-end">
                            <a href="/portal/return" class="btn btn-secondary me-2">Hủy</a>
                            <button type="submit" name="action" value="draft" class="btn btn-outline-primary me-2">Lưu nháp</button>
                            <button type="submit" name="action" value="send" class="btn btn-primary">
                                <i class="fa fa-paper-plane me-1"/>Gửi yêu cầu
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Mobile <992px -->
            <div class="d-lg-none wujia-mpage wujia-mreturn">
                <t t-call="wujia_portal_layout.wj_page_header">
                    <t t-set="ph_platform" t-value="'m'"/>
                    <t t-set="ph_variant" t-value="'back'"/>
                    <t t-set="ph_title">Tạo yêu cầu bù hàng</t>
                    <t t-set="ph_back_url" t-value="'/portal/return'"/>
                </t>

                <div class="wujia-mreturn-banner">
                    <i class="feather icon-info"/>
                    <span>Mỗi yêu cầu chỉ áp dụng cho 1 sản phẩm trong đơn đặt.</span>
                </div>

                <p t-if="error" class="text-danger small mb-2" t-out="error"/>

                <form method="post" action="/portal/return/new" enctype="multipart/form-data"
                      data-wj-dirty-guard="1">
                    <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                    <t t-if="franchises and len(franchises) == 1">
                        <input type="hidden" name="franchise_id" t-att-value="franchises[0].id"/>
                    </t>

                    <t t-call="wujia_portal_layout.wj_surface_card">
                        <t t-set="sc_variant" t-value="'section'"/>
                        <t t-set="sc_class" t-value="'wujia-mdash-card'"/>
                        <!-- CMP-CH-001: `<p>` giả heading → heading THẬT; `mb-3` gỡ hẳn để
                             không cộng chồng margin của header với margin body (spec). -->
                        <t t-call="wujia_portal_layout.wj_card_header">
                            <t t-set="ch_title">Thông tin đơn hàng</t>
                            <t t-set="ch_platform" t-value="'m'"/>
                        </t>
                        <t t-if="franchises and len(franchises) &gt; 1">
                            <label class="wujia-mticket-label">Cửa hàng</label>
                            <select name="franchise_id" class="form-select mb-3" required="required">
                                <t t-foreach="franchises" t-as="f"><option t-att-value="f.id" t-out="f.name"/></t>
                            </select>
                        </t>
                        <label class="wujia-mticket-label">Đơn hàng gốc</label>
                        <select name="sale_order_id" class="form-select mb-3 wj-return-order"
                                t-att-data-lines="order_lines_json" required="required">
                            <option value="">— Chọn đơn —</option>
                            <t t-foreach="orders" t-as="o">
                                <option t-att-value="o.id">
                                    <t t-out="o.name"/> — <t t-out="wj_dt(o.date_order, '%d/%m/%Y')"/>
                                </option>
                            </t>
                        </select>
                        <p t-if="not orders" class="wujia-mticket-hint text-danger">
                            Không tìm thấy đơn hàng đủ điều kiện trong
                            <t t-out="window_days"/> ngày gần nhất.
                        </p>
                        <label class="wujia-mticket-label">Sản phẩm (trong đơn)</label>
                        <select name="sale_order_line_id" class="form-select wj-return-line" required="required">
                            <option value="">— Chọn đơn trước —</option>
                        </select>
                    </t>

                    <t t-call="wujia_portal_layout.wj_surface_card">
                        <t t-set="sc_variant" t-value="'section'"/>
                        <t t-set="sc_class" t-value="'wujia-mdash-card'"/>
                        <t t-call="wujia_portal_layout.wj_card_header">
                            <t t-set="ch_title">Số lượng &amp; lô</t>
                            <t t-set="ch_platform" t-value="'m'"/>
                        </t>
                        <div class="wujia-mreturn-grid2">
                            <div>
                                <label class="wujia-mticket-label">SL yêu cầu</label>
                                <input type="number" name="request_qty" class="form-control"
                                       min="0.01" step="0.01" value="1" required="required"/>
                            </div>
                            <div>
                                <label class="wujia-mticket-label">Ngày sản xuất</label>
                                <input type="date" name="production_date" class="form-control"/>
                            </div>
                        </div>
                        <label class="wujia-mticket-label">Thời gian mở hàng</label>
                        <input type="datetime-local" name="opening_datetime" class="form-control" required="required"/>
                    </t>

                    <t t-call="wujia_portal_layout.wj_surface_card">
                        <t t-set="sc_variant" t-value="'section'"/>
                        <t t-set="sc_class" t-value="'wujia-mdash-card'"/>
                        <t t-call="wujia_portal_layout.wj_card_header">
                            <t t-set="ch_title">Nội dung lỗi</t>
                            <t t-set="ch_platform" t-value="'m'"/>
                        </t>
                        <label class="wujia-mticket-label">Loại lỗi</label>
                        <select name="issue_type_id" class="form-select mb-3" required="required">
                            <option value="">— Chọn loại lỗi —</option>
                            <t t-foreach="issue_types" t-as="it"><option t-att-value="it.id" t-out="it.name"/></t>
                        </select>
                        <label class="wujia-mticket-label">Ghi chú</label>
                        <textarea name="note" rows="3" class="form-control"
                                  placeholder="Mô tả chi tiết tình trạng hàng…"/>
                    </t>

                    <t t-call="wujia_portal_layout.wj_surface_card">
                        <t t-set="sc_variant" t-value="'section'"/>
                        <t t-set="sc_class" t-value="'wujia-mdash-card'"/>
                        <!-- Dòng hint 11px bên dưới CỐ Ý giữ nguyên là body, không đưa vào
                             ch_subtitle: subtitle của spec là 13/18, to hơn hint hiện tại
                             ⇒ đưa vào sẽ làm thẻ THƯA ra, trái acceptance BA. -->
                        <t t-call="wujia_portal_layout.wj_card_header">
                            <t t-set="ch_title">Hình ảnh đính kèm</t>
                            <t t-set="ch_platform" t-value="'m'"/>
                        </t>
                        <p class="wujia-mticket-hint">
                            <t t-out="min_images"/>–<t t-out="max_images"/> ảnh JPG/PNG (≤5MB mỗi ảnh),
                            chụp rõ bao bì &amp; lỗi sản phẩm.
                        </p>
                        <input type="file" name="images" class="form-control"
                               accept="image/jpeg,image/png" multiple="multiple"/>
                        <p class="wujia-mticket-hint mt-3 mb-1">
                            Video minh chứng (không bắt buộc) — tối đa 1 file MP4/MOV
                            ≤ <t t-out="max_video_mb"/>MB, tổng minh chứng ≤ <t t-out="max_total_mb"/>MB.
                        </p>
                        <input type="file" name="video" class="form-control"
                               accept="video/mp4,video/quicktime"/>
                    </t>

                    <div class="wujia-mreturn-cta">
                        <a href="/portal/return" class="wujia-mreturn-btn-cancel">Hủy</a>
                        <button type="submit" name="action" value="send" class="wujia-mreturn-btn-submit">
                            Gửi yêu cầu
                        </button>
                    </div>
                </form>
            </div>
        </t>
    </template>

</odoo>
