# Local secrets (never commit)
.env.local

# Local config (Linux paths - không push lên server)
config/odoo.conf

# Odoo
data/filestore/
data/sessions/
logs/
*.log
*.pyc
__pycache__/
.env

# Odoo core (không push lên git, quá nặng)
odoo19/

# Python
*.egg-info/
.venv/
venv/
dist/
build/

# OS
.DS_Store
Thumbs.db

# LaTeX build artifacts (giữ .tex + .pdf, bỏ phần còn lại)
docs/*.aux
docs/*.toc
docs/*.out
docs/*.fls
docs/*.fdb_latexmk
docs/*.synctex.gz
docs/*.bak
docs/chapters/*.aux

# BA-spec ingestion toolchain — dev-only, không lên server
scripts/ba_spec/
# Dấu / ở cuối KHÔNG khớp symlink: trên máy Mac scripts/ba_spec là symlink trỏ ra
# ~/wujia-devkit/ba_spec, nên git vẫn theo dõi chính cái symlink. Thêm dòng không
# có dấu / và cả bản sao lưu — bộ này là dev-only, tuyệt đối không lên server.
scripts/ba_spec
scripts/ba_spec.bak.*/
ks_dashboard_ninja_v18/

# Harness đo đạc dev-only (§13): KHÔNG lên server, KHÔNG vào repo.
# Phiên 04/09 suýt commit nhầm 250 file ảnh/JSON từ đây.
scratchpad/
