name: Deploy
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest code
        run: |
          cd /d D:\wujia-tea
          git pull origin main
        shell: cmd

      - name: Restart Odoo
        run: nssm restart Odoo
        shell: cmd
